version: 1
allowed:
  - file: crates/app-data/src/metadata/flashloan.rs
    item: FlashloanHints
    reason: SDK-owned app-data metadata schema; unknown fields are contributor mistakes.
  - file: crates/app-data/src/metadata/hooks.rs
    item: HookList
    reason: SDK-owned app-data metadata schema with a closed request shape.
  - file: crates/app-data/src/metadata/hooks.rs
    item: Hook
    reason: SDK-owned app-data metadata schema with a closed hook entry shape.
  - file: crates/contracts/src/deployments/registry.rs
    item: ManifestSchema
    reason: Build-time TOML manifest; unknown fields indicate a repository bug.
  - file: crates/contracts/src/deployments/registry.rs
    item: ManifestEntry
    reason: Build-time TOML manifest entry; unknown fields indicate a repository bug.
  - file: crates/contracts/src/deployments/registry.rs
    item: ManifestVerification
    reason: Build-time TOML manifest verification block; unknown fields indicate a repository bug.
  - file: crates/contracts/src/deployments/coverage.rs
    item: CoverageManifest
    reason: Build-time YAML deployment-coverage manifest; unknown fields indicate a repository bug.
  - file: crates/contracts/src/deployments/coverage.rs
    item: DeploymentCoverageRecord
    reason: Build-time YAML deployment-coverage row; unknown fields indicate a repository bug.
  - file: crates/wasm/src/exports/dto/transport.rs
    item: RetryPolicyConfig
    reason: JS-facing retry-policy override request DTO; an unknown field is a caller typo that must fail loudly rather than silently drop the override.
  - file: crates/wasm/src/exports/dto/transport.rs
    item: RequestRateLimiterConfig
    reason: JS-facing rate-limiter override request DTO; an unknown field is a caller typo that must fail loudly rather than silently drop the override.
  - file: crates/wasm/src/exports/dto/transport.rs
    item: TransportPolicyConfig
    reason: JS-facing transport-policy override request DTO; an unknown field is a caller typo that must fail loudly rather than silently drop the override.
