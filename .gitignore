# Rust build output
target/

# Fuzz corpora and artifacts are regenerated on demand; the harnesses under
# fuzz/fuzz_targets/ and PROPERTIES.md are the tracked record (see fuzz/README.md).
fuzz/corpus/
fuzz/artifacts/
fuzz/target/

# JavaScript / WebAssembly dependencies and build output
node_modules/
pkg/
dist/
*.tgz
.wrangler/

# Generated from package.template.json by crates/wasm/npm/scripts/render-package-json.mjs
crates/wasm/npm/package.json

# Test reports and results
playwright-report/
.blob-report/
test-results/

# Local tooling and working notes (not part of the published project)
.local
.agents
.claude