{
  "surface": "app-data-schema-flashloan",
  "sources": {
    "services": {
      "commit": "65b6953bfc2e96b2791cfb2382c7309d1fb19b99",
      "refs": [
        "crates/app-data/src/app_data.rs#Flashloan"
      ]
    }
  },
  "derivation": "Drift schema for the services-defined flash-loan hint shape; cowprotocol/app-data has no schema with these fields (its flashloan/v0.1.0 uses lender/borrower).",
  "payload": {
    "$id": "flashloan.json",
    "$schema": "http://json-schema.org/draft-07/schema",
    "$comment": "Mirrors the services flash-loan hint shape; shared primitives inlined from upstream app-data definitions.json. Not resolved at runtime (ADR 0064).",
    "title": "Flashloan",
    "description": "Information about flashloan parameters for the order",
    "type": "object",
    "required": [
      "amount",
      "liquidityProvider",
      "protocolAdapter",
      "receiver",
      "token"
    ],
    "properties": {
      "amount": {
        "title": "Loan amount",
        "description": "The amount of tokens to be flash loaned (in token units, as uint256)",
        "type": "string",
        "pattern": "^[1-9]\\d*$"
      },
      "liquidityProvider": {
        "title": "Liquidity provider address",
        "description": "The Ethereum address of the flashloan liquidity provider",
        "type": "string",
        "pattern": "^0x[a-fA-F0-9]{40}$"
      },
      "protocolAdapter": {
        "title": "Protocol adapter address",
        "description": "The Ethereum address of the protocol adapter",
        "type": "string",
        "pattern": "^0x[a-fA-F0-9]{40}$"
      },
      "receiver": {
        "title": "Receiver address",
        "description": "The Ethereum address of the flashloan receiver",
        "type": "string",
        "pattern": "^0x[a-fA-F0-9]{40}$"
      },
      "token": {
        "title": "Token address",
        "description": "The Ethereum address of the token being borrowed",
        "type": "string",
        "pattern": "^0x[a-fA-F0-9]{40}$"
      }
    },
    "additionalProperties": false
  }
}
