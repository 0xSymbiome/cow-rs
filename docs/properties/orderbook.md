---
type: Property
id: orderbook
title: "Orderbook client invariants"
description: "The `cow-sdk-orderbook` client: typed request/response modeling, retry termination and cooldown, rejection decoding, content-addressed app-data writes, and the sole typestate builder construction path."
resource: https://github.com/0xSymbiome/cow-rs/blob/main/docs/properties/orderbook.md
families: [PROP-ORD]
tags: [property, invariants]
timestamp: 2026-06-29T00:00:00Z
---

# Orderbook client invariants

The `cow-sdk-orderbook` client: typed request/response modeling, retry termination and cooldown, rejection decoding, content-addressed app-data writes, and the sole typestate builder construction path. Part of the [Properties Registry](index.md): 17 invariant(s), 17 covered.

## Request building & quote echo

| Id | Crate | Property | Type | Covered | Evidence | Last reviewed |
| --- | --- | --- | --- | --- | --- | --- |
| `PROP-ORD-001` | `cow-sdk-orderbook` | Request builders preserve explicit field shape, pagination defaults, and per-form `appData` transport (hash-only, document-only, and document-plus-hash) without silently coercing unsupported inputs. | Invariant | Yes | `crates/orderbook/tests/invariant_contract.rs`, `crates/orderbook/tests/request_contract.rs`, `crates/orderbook/tests/api_contract.rs`, `crates/orderbook/tests/types_contract.rs` | 2026-05-30 |
| `PROP-ORD-003` | `cow-sdk-orderbook` | `OrderbookApi::quote` binds every request-determined field of the `/quote` response to the request through `OrderQuoteResponse::ensure_matches`, failing closed with `OrderbookError::QuoteEchoMismatch` when the token pair, order kind, owner, balance sources, the effective receiver, the app-data hash, an absolute `validTo`, or the fixed amount leg did not come back unchanged; the variable price leg stays free and the fixed-leg fold follows the services arithmetic per side basis (sell-before-fee `sellAmount + feeAmount == requested`, sell-after-fee `sellAmount == requested`, buy `buyAmount == requested`). The signed order binds the caller's requested balance sources rather than the response echo. The receiver is reconciled as the effective receiver (an unset or zero receiver resolves to the owner, so a fabricated receiver on an unpinned request fails closed), and the app-data hash is reconciled for every request form (an explicit pin, the keccak digest of a full document, or the zero hash for an omitted pair); `OrderCreation::from_quote` additionally binds the caller's receiver rather than the echoed `quote.receiver` so the projected payload cannot inherit a server-chosen address even on a raw lane. Governed by [ADR 0058](../adr/0058-typed-quote-request-response-surface.md). | Contract | Yes | `crates/orderbook/tests/quote_echo_contract.rs::honest_sell_before_fee_response_passes`, `crates/orderbook/tests/quote_echo_contract.rs::inflated_fixed_sell_leg_fails`, `crates/orderbook/tests/quote_echo_contract.rs::quote_fails_closed_end_to_end_on_a_tampered_fixed_leg`, `crates/orderbook/tests/quote_echo_contract.rs::fabricated_receiver_echo_on_an_unpinned_request_fails`, `crates/orderbook/tests/quote_echo_contract.rs::owner_equivalent_receiver_echoes_pass`, `crates/orderbook/tests/quote_echo_contract.rs::full_document_request_binds_its_digest`, `crates/orderbook/tests/quote_echo_contract.rs::omitted_app_data_must_echo_the_zero_hash`, `crates/orderbook/tests/quote_echo_contract.rs`, `crates/orderbook/src/types/quote.rs`, `crates/orderbook/src/types/order.rs`, `crates/orderbook/src/api.rs`, `crates/orderbook/tests/types_contract.rs::from_quote_binds_caller_receiver_not_the_quote_echo`, `crates/trading/tests/post_contract.rs::signed_balance_sources_bind_to_the_request_not_the_quote_response` | 2026-06-14 |
| `PROP-ORD-005` | `cow-sdk-orderbook` | Orderbook quote-side, quote, order, trade, solver-competition, total-surplus, and solver-execution wire DTO amount fields store promoted amounts as `cow_sdk_core::Amount` while preserving the decimal-string JSON wire shape through custom serde and failing malformed amount strings at deserialization. | Contract | Yes | `crates/orderbook/src/types/quote.rs`, `crates/orderbook/src/types/order.rs`, `crates/orderbook/src/types/lists.rs`, `crates/orderbook/src/types/prices.rs`, `crates/orderbook/src/types/auction.rs`, `crates/orderbook/tests/wire_contract.rs::openapi_response_dtos_roundtrip_required_fixture_fields`, `crates/orderbook/tests/wire_contract.rs::order_quote_response_amount_fields_deserialize_through_typed_amount`, `crates/orderbook/tests/transform_contract.rs::total_fee_x_executed_fee_amount_matrix_holds_for_zero_legacy_zero_canonical_legacy_only_canonical_only`, `crates/orderbook/tests/api_contract.rs::solver_competition_response_decodes_typed_with_reference_scores_and_orders`, `crates/orderbook/tests/transform_contract.rs::solver_competition_response_fixture_roundtrips_upstream_producer_vector`, `xtask/tests/openapi_coverage.rs::openapi_coverage_validate_reports_structured_field_mismatches` | 2026-06-07 |
| `PROP-ORD-016` | `cow-sdk-orderbook`, `cow-sdk-trading` | `OrderQuoteRequest` models the orderbook quote `oneOf`s as typed Rust so an invalid request is unrepresentable rather than pre-validated at runtime: `QuoteValidity` carries `validTo` or `validFor` (never both, defaulting to the 30-minute relative window when neither is on the wire), `OrderQuoteSide` carries exactly one side with `SellAmount` distinguishing the before-fee and after-fee sell amount, and `QuoteSigningScheme` keeps `verificationGasLimit` on EIP-1271 only and makes an ECDSA (`Eip712` / `EthSign`) on-chain order unrepresentable while `Eip1271` and `PreSign` retain services-compatible on-chain behavior. Conflicting or malformed wire input is rejected during deserialization — the `SellAmount` deserializer requires exactly one sell-amount key and the `QuoteSigningScheme` `try_from` guard rejects an ECDSA on-chain scheme or a stray `verificationGasLimit` — so the retained `OrderQuoteRequest::validate` hook is infallible. App-data stays modeled as the `appData` / `appDataHash` pair (`QuoteAppData`) routed identically to the signed `OrderCreation` payload, and the trading layer rejects an attempted ECDSA on-chain override before transport with `OrderbookError::IncompatibleSigningScheme`. Governed by [ADR 0058](../adr/0058-typed-quote-request-response-surface.md). | Contract | Yes | `crates/orderbook/src/types/quote.rs`, `crates/orderbook/src/types/app_data.rs`, `crates/orderbook/tests/types_contract.rs::quote_signing_scheme_rejects_incompatible_onchain_ecdsa_wire_pairs`, `crates/orderbook/tests/types_contract.rs::quote_signing_scheme_rejects_verification_gas_limit_without_eip1271`, `crates/orderbook/tests/types_contract.rs::quote_request_app_data_routes_to_server_valid_wire_shapes`, `crates/orderbook/tests/types_contract.rs::quote_request_supports_buy_side_and_context_overrides`, `crates/trading/tests/quote_contract.rs::quote_request_validation_runs_before_orderbook_transport`, `docs/adr/0058-typed-quote-request-response-surface.md` | 2026-06-06 |

## Response decoding & retry

| Id | Crate | Property | Type | Covered | Evidence | Last reviewed |
| --- | --- | --- | --- | --- | --- | --- |
| `PROP-ORD-002` | `cow-sdk-orderbook` | Response decoding, retry termination, `Retry-After` cooldown honor on `429` and `503`, and transform layers fail closed on malformed upstream payloads. | Invariant | Yes | `crates/orderbook/tests/invariant_contract.rs`, `crates/orderbook/tests/transform_contract.rs`, `crates/orderbook/tests/types_contract.rs`, `crates/orderbook/tests/request_contract.rs`, `crates/orderbook/tests/api_contract.rs::service_unavailable_retry_after_header_delays_retry_for_at_least_server_cooldown`, `crates/core/tests/policy_contract.rs::retry_after_only_affects_rate_limit_and_unavailable_statuses` | 2026-05-28 |
| `PROP-ORD-006` | `cow-sdk-orderbook` | Retry observability stays aligned with the public field registry: retry events carry `attempt_index`, `backoff_ms`, and either `status` or `transport_error_class`, and quote/order spans populate `quote_id`, `attempts`, and `status` when the `tracing` feature is enabled. | Contract | Yes | `crates/orderbook/tests/request_contract.rs::tracing_contract::execute_with_emits_retry_events_with_status_and_transport_error_fields`, `crates/orderbook/tests/request_contract.rs::tracing_contract::send_order_span_records_quote_id_attempts_and_status`, `docs/guides/observability.md` | 2026-04-24 |

## Rejection taxonomy

| Id | Crate | Property | Type | Covered | Evidence | Last reviewed |
| --- | --- | --- | --- | --- | --- | --- |
| `PROP-ORD-007` | `cow-sdk-orderbook` | `InvalidTradeFilter`, `InvalidLimit`, and the `LIMIT_OUT_OF_BOUNDS` wire tag parse to dedicated `OrderbookRejection` variants; account-order pagination stays within the documented services limit; the enum surface remains 49 variants including the forward-compatible `Unknown` fallback. | Contract | Yes | `crates/orderbook/tests/rejection_contract.rs::every_known_services_tag_parses_to_its_typed_variant`, `crates/orderbook/tests/api_contract.rs::account_orders_pagination_boundary_table`, `crates/orderbook/src/rejection.rs` | 2026-05-01 |
| `PROP-ORD-008` | `cow-sdk-orderbook` | `fuzz_orderbook_rejection_decode` carries a documented seed-corpus contract anchored to services rejection fixtures covering every known `errorType` tag plus malformed-envelope and non-UTF-8 body boundaries. The classifier sanitizer surface is also exercised through `fuzz_orderbook_rejection_code` against the public allowlist gate. | Contract | Yes | `fuzz/fuzz_targets/fuzz_orderbook_rejection_decode.rs`, `fuzz/fuzz_targets/fuzz_orderbook_rejection_code.rs`, `crates/orderbook/tests/rejection_contract.rs::every_known_services_tag_parses_to_its_typed_variant` | 2026-05-13 |
| `PROP-ORD-012` | `cow-sdk-orderbook` | `OrderbookRejection::category()` returns a coarse, action-oriented `OrderbookRejectionCategory` (`Authorization`, `InsufficientFunds`, `InvalidOrder`, `NotFound`, `Conflict`, `Unfulfillable`, `Server`, `Unknown`) as an additive accessor over the typed taxonomy: it keeps every variant, partitions all of them with no wildcard arm so a new wire tag must be assigned a category at the source, and returns a fieldless `Copy` enum carrying no message or code so it cannot re-expose a redacted rejection payload. Governed by [ADR 0017](../adr/0017-typed-orderbook-rejection-parser.md). | Contract | Yes | `crates/orderbook/tests/rejection_category_contract.rs::category_maps_representative_and_borderline_variants`, `crates/orderbook/src/rejection.rs` | 2026-06-08 |
| `PROP-ORD-015` | `cow-sdk-orderbook` | `OrderbookRejection` decodes every services `errorType` to a typed variant; unknown codes route to `Unknown { code, message }` with `#[non_exhaustive]` at the enum head. The closed set of `errorType` tags the orderbook OpenAPI documents is pinned with a provenance header and asserted to classify to a typed (non-`Unknown`) variant, so an upstream enum addition surfaces when the vendored OpenAPI is re-stamped. Governed by [ADR 0017](../adr/0017-typed-orderbook-rejection-parser.md). | Contract | Yes | `crates/orderbook/tests/rejection_contract.rs::every_known_services_tag_parses_to_its_typed_variant`, `crates/orderbook/tests/rejection_contract.rs::every_documented_error_type_classifies_to_a_typed_variant`, `parity/fixtures/orderbook/rejection_error_types.json`, `crates/orderbook/tests/rejection_contract.rs::app_data_invalid_tag_preserves_typed_message_and_display`, `crates/orderbook/tests/rejection_contract.rs::app_data_mismatch_tag_preserves_typed_message_and_display`, `crates/orderbook/tests/rejection_contract.rs::not_found_tag_preserves_typed_message_and_display` | 2026-06-17 |

## Order creation & app-data

| Id | Crate | Property | Type | Covered | Evidence | Last reviewed |
| --- | --- | --- | --- | --- | --- | --- |
| `PROP-ORD-009` | `cow-sdk-orderbook` | `OrderCreation` accepts omitted or zero `feeAmount`, always serializes order-level `feeAmount` as `"0"`, and rejects inbound non-zero `feeAmount` with the stable diagnostic substring `non-zero feeAmount is not accepted for OrderCreation`; `QuoteData` keeps accepting non-zero network-cost `feeAmount`. | Contract | Yes | `crates/orderbook/tests/order_creation_fee_deserialize.rs`, `crates/orderbook/tests/fee_amount_is_not_a_public_builder_setter.rs`, `crates/orderbook/src/types/order.rs`, `docs/guides/parity.md` | 2026-05-12 |
| `PROP-ORD-010` | `cow-sdk-orderbook` | `OrderCreation` `Serialize` routes the `(app_data, app_data_hash)` pair onto the services `OrderCreationAppData` untagged-enum variants (`Both` / `Hash` / `Full`): present-full with present-hash emits both `appData` and `appDataHash` per the services `Both` variant, full-only emits `appData` per `Full`, hash-only keys the hash under the `appData` JSON key per `Hash` (rather than emitting a `appDataHash`-only object that matches no variant), and the all-absent case omits both fields. `OrderCreation::from_quote` keeps producing `(None, Some(quote.app_data))` and is routed through the services `Hash` variant by the custom `Serialize` impl without any `from_quote` body change. | Contract | Yes | `crates/orderbook/tests/types_contract.rs::order_creation_serialize_routes_app_data_combinations_to_services_variants`, `crates/orderbook/tests/types_contract.rs::order_creation_from_quote_serialize_emits_services_hash_variant`, `crates/orderbook/src/types/order.rs`, `parity/fixtures/orderbook-requests/order_creation.json`, `docs/audit/contract-bindings-parity-audit.md` | 2026-05-21 |
| `PROP-ORD-011` | `cow-sdk-orderbook`, `cow-sdk-core` | `OrderbookApi::upload_app_data` enforces the content-addressed-write invariant at both boundaries. The SDK re-derives the digest with `AppDataHash::from_full_app_data` before dispatching and rejects with `OrderbookError::AppDataHashMismatch { stage: HashMismatchStage::ClientPrecheck }` when the caller-supplied hash disagrees, without performing a network call. On a successful response it decodes the bare hex-string body as `cow_sdk_core::AppDataHash` (matching the services PUT response schema for both HTTP 200 and HTTP 201) and rejects with `HashMismatchStage::ServerEcho` when the returned hash differs from the caller-supplied value. Governed by [ADR 0017](../adr/0017-typed-orderbook-rejection-parser.md) and [ADR 0031](../adr/0031-wire-dto-openapi-driven-with-order-auction-order-split.md). | Contract | Yes | `crates/orderbook/tests/api_contract.rs::upload_app_data_rejects_client_precheck_mismatch_without_network`, `crates/orderbook/tests/api_contract.rs::upload_app_data_rejects_server_echo_mismatch`, `crates/orderbook/tests/api_contract.rs::upload_app_data_accepts_status_200_for_already_existing_documents`, `crates/orderbook/tests/api_contract.rs::app_data_transport_helpers_use_get_and_put_hash_routes`, `crates/orderbook/tests/error_variant_shape.rs::app_data_hash_mismatch_carries_typed_hashes_and_stage_discriminator`, `crates/core/tests/types_contract.rs::app_data_hash_from_full_app_data_matches_keccak256_of_bytes` | 2026-05-28 |

## Construction, status & signing

| Id | Crate | Property | Type | Covered | Evidence | Last reviewed |
| --- | --- | --- | --- | --- | --- | --- |
| `PROP-ORD-004` | `cow-sdk-orderbook` | The `cow_sdk_orderbook::SigningScheme` wire enum and the cancellation-only `EcdsaSigningScheme` subset are connected by typed conversions internal to the orderbook crate: `From<EcdsaSigningScheme> for SigningScheme` widens every ECDSA variant to its wire-form parent, and the fallible inverse `TryFrom<SigningScheme> for EcdsaSigningScheme` narrows back, rejecting `Eip1271` and `PreSign` with a typed `SigningSchemeNotEcdsa` error rather than silently dropping data. The orderbook scheme enum stays distinct from `cow_sdk_contracts::SigningScheme` because their wire serde shapes diverge (`#[serde(rename_all = "lowercase")]` HTTP wire JSON versus the protocol `#[repr(u8)]` discriminant), and the orderbook crate carries no conversion to, and no dependency on, the contracts enum, keeping its graph free of the signing-recovery primitive stack. | Contract | Yes | `crates/orderbook/tests/ecdsa_scheme_conversion_contract.rs`, `crates/orderbook/src/types/enums.rs` | 2026-06-15 |
| `PROP-ORD-013` | `cow-sdk-orderbook` | `OrderStatus::is_terminal()` returns `true` for `Fulfilled`, `Cancelled`, and `Expired`, and `is_open()` returns `true` for `Open` and `PresignaturePending`; the two `const fn` predicates partition every variant known today. Because `OrderStatus` is `#[non_exhaustive]` and services-owned, the predicates centralize terminal/live classification in the defining crate so a future status variant is reclassified at the source rather than silently misread by a hand-rolled consumer match. Governed by [ADR 0011](../adr/0011-typed-amount-boundary-and-typestate-ready-state-construction.md). | Contract | Yes | `crates/orderbook/tests/types_contract.rs::order_status_terminal_and_open_predicates_partition_known_variants`, `crates/orderbook/src/types/enums.rs` | 2026-06-08 |
| `PROP-ORD-014` | `cow-sdk-orderbook` | `OrderbookApi::builder()` is the sole production construction path; the typestate encodes the required inputs (chain, environment, transport) at compile time and no free-function constructor remains; `trybuild` compile-fail witnesses assert `.build()` is unreachable until the chain id and environment are supplied. Governed by [ADR 0013](../adr/0013-http-transport-injection-and-typestate-builders.md). | Contract | Yes | `crates/orderbook/tests/builder_contract.rs`, `crates/orderbook/tests/ui/build_without_chain.rs`, `crates/orderbook/tests/ui/build_without_environment.rs`, `crates/orderbook/tests/ui/build_on_empty_builder.rs` | 2026-06-02 |
| `PROP-ORD-017` | `cow-sdk-orderbook`, `cow-sdk-trading` | The `OrderbookClient` trait is owned by `cow-sdk-orderbook` and reachable as `cow_sdk_orderbook::OrderbookClient` without a trading-crate dependency. `cow-sdk-trading` re-exports the same trait as an additive convenience so trading-crate consumers can compose against it without an explicit orderbook-crate import. | Public API | Yes | `crates/orderbook/src/lib.rs`, `crates/trading/src/lib.rs`, `fuzz/fuzz_targets/fuzz_decoded_body_canonical_status_text.rs` | 2026-05-13 |
