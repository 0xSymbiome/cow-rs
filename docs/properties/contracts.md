---
type: Property
id: contracts
title: "Contract binding invariants"
description: "Typed contract bindings and calldata encoding in `cow-sdk-contracts`, including the COW Shed owner-agnostic `executeHooks` encoding and its single signer-mediated span."
resource: https://github.com/0xSymbiome/cow-rs/blob/main/docs/properties/contracts.md
families: [PROP-CON, PROP-SHED]
tags: [property, invariants]
timestamp: 2026-06-29T00:00:00Z
---

# Contract binding invariants

Typed contract bindings and calldata encoding in `cow-sdk-contracts`, including the COW Shed owner-agnostic `executeHooks` encoding and its single signer-mediated span. Part of the [Properties Registry](index.md): 22 invariant(s), 22 covered.

## Order hashing & EIP-712

| Id | Crate | Property | Type | Covered | Evidence | Last reviewed |
| --- | --- | --- | --- | --- | --- | --- |
| `PROP-CON-001` | `cow-sdk-contracts` | Order hashing remains deterministic across semantically equivalent normalized inputs. | Property | Yes | `crates/contracts/tests/property_contract.rs`, `crates/contracts/tests/order_contract.rs`, `fuzz/fuzz_targets/fuzz_hash_order_cancellations.rs` | 2026-06-08 |
| `PROP-CON-002` | `cow-sdk-contracts` | Signature payload codecs and interaction normalization preserve explicit boundary semantics for signature and interaction payloads. | Property | Yes | `crates/contracts/tests/signature_contract.rs::eip1271_signature_payloads_roundtrip_with_variable_lengths`, `crates/contracts/tests/signature_contract.rs`, `crates/contracts/tests/interaction_contract.rs`, `crates/contracts/src/primitives.rs (tests)`, `crates/contracts/src/order.rs (tests)` | 2026-06-08 |
| `PROP-CON-006` | `cow-sdk-contracts` | Contracts-crate digest computation flows directly through the concrete `cow_sdk_core::OrderData`; the legacy compatibility shims that zeroed digest inputs are removed from the public surface, and the contracts crate exposes no order type of its own. Governed by [ADR 0059](../adr/0059-hash-concrete-orderdata-directly.md). | Contract | Yes | `crates/contracts/tests/order_contract.rs`, `crates/contracts/tests/order_digest_parity_contract.rs` | 2026-05-30 |
| `PROP-CON-007` | `cow-sdk-contracts` | The order UID pack/unpack fuzz target carries a documented seed-corpus contract anchored to parity fixture data plus edge-shaped 56-byte triples, so fuzzing starts from known codec boundaries instead of random-only inputs. | Contract | Yes | `fuzz/fuzz_targets/fuzz_order_uid_pack_unpack.rs`, `parity/fixtures/contracts.json` | 2026-04-24 |
| `PROP-CON-023` | `cow-sdk-contracts` | The order EIP-712 signing hash computed through the public `hash_order` entrypoint stays byte-stable against pinned per-chain fixtures (mainnet, Gnosis Chain, Sepolia, Arbitrum One, and Base), exercising the receiver field (including the pay-to-owner zero-address sentinel) and the order-kind and token-balance enum-to-string mapping, and the `keccak256(0x19 \|\| 0x01 \|\| domain_separator \|\| struct_hash)` composition path; the fixture header type hash matches `order_eip712_type_hash()`. | Contract | Yes | `crates/contracts/tests/order_digest_parity_contract.rs::order_digest_fixture_rows_hold`, `parity/fixtures/eip712/order_digests.json` | 2026-05-30 |

## ABI bindings & registry

| Id | Crate | Property | Type | Covered | Evidence | Last reviewed |
| --- | --- | --- | --- | --- | --- | --- |
| `PROP-CON-003` | `cow-sdk-contracts` | Every ABI binding is authored as an inline `alloy::sol!` interface in `cow-sdk-contracts` and preserves byte-identity on the encoded call-data and hashed payloads of `GPv2Settlement`, `CoWSwapEthFlow`, `CoWSwapOnchainOrders` events, `IWrappedNativeToken`, and `IERC20`, proven against TypeScript-SDK-derived call-data fixtures pinned by upstream commit in `parity/source-lock.yaml`. Governed by [ADR 0012](../adr/0012-alloy-sol-bindings-and-registry-authority.md). | Contract | Yes | `crates/contracts/tests/parity_contract.rs::parity_fixture_cases_hold`, `crates/contracts/tests/parity_contract.rs` | 2026-06-08 |
| `PROP-CON-004` | `cow-sdk-contracts` | `Registry` resolves every deployed contract address from the typed `(ContractId, SupportedChainId, CowEnv)` triple through a const table of CREATE2 singletons: the settlement, vault-relayer, and eth-flow contracts each resolve a distinct production or staging deployment, every address is chain-invariant within its environment, and the deployment-only Lens chain resolves to `None`. Governed by [ADR 0012](../adr/0012-alloy-sol-bindings-and-registry-authority.md) and [ADR 0032](../adr/0032-deployment-authority-machine-readable-provenance.md). | Contract | Yes | `crates/contracts/src/deployments.rs (tests)`, `xtask/tests/registry_confirm.rs` | 2026-06-10 |
| `PROP-CON-020` | `cow-sdk-contracts` | The `IWrappedNativeToken` `deposit()` / `withdraw(uint256)` selectors are byte-locked against an independent keccak of the canonical signatures, and `wrap_interaction` / `unwrap_interaction` emit the canonical settlement interaction — `deposit()` with the wrapped amount as native value and `withdraw(amount)` with zero native value. The `wrap_transaction` / `unwrap_transaction` builders resolve the chain's canonical wrapped-native address through `cow_sdk_core::wrapped_native_token` and return that interaction as an infallible gas-free `UnsignedTransaction`; they live in `cow-sdk-contracts` and are re-exported from `cow-sdk-trading`. Governed by [ADR 0012](../adr/0012-alloy-sol-bindings-and-registry-authority.md) and [ADR 0070](../adr/0070-onchain-transaction-helper-boundary.md). | Contract | Yes | `crates/contracts/tests/tokens_contract.rs::deposit_selector_matches_canonical_keccak`, `crates/contracts/tests/tokens_contract.rs::withdraw_selector_matches_canonical_keccak`, `crates/contracts/tests/tokens_contract.rs::wrap_interaction_calls_deposit_with_amount_as_value`, `crates/contracts/tests/tokens_contract.rs::unwrap_interaction_calls_withdraw_with_zero_value`, `crates/contracts/src/tokens.rs::tests::wrap_transaction_resolves_canonical_address_and_sends_amount_as_value`, `crates/contracts/src/tokens.rs::tests::unwrap_transaction_resolves_canonical_address_with_zero_value`, `crates/contracts/src/tokens.rs::tests::wrap_transaction_resolves_a_distinct_address_per_chain` | 2026-06-22 |
| `PROP-CON-024` | `cow-sdk-contracts` | The `GPv2Settlement` `setPreSignature(bytes,bool)` / `invalidateOrder(bytes)` selectors are byte-locked against an independent keccak of the canonical signatures, and `encode_set_pre_signature` / `encode_invalidate_order` emit the canonical call-data — the 56-byte order UID as the dynamic `bytes` argument and the pre-sign `signed` flag in the second head word. These shipped `cow-sdk-contracts` encoders are the single source for the settlement pre-sign and on-chain-cancel call data and drive the settlement call-data parity fixtures; `cow-sdk-trading` (`pre_sign_transaction`, `onchain_cancel_order`) and the wasm `buildPresignTx` / `buildCancelOrderTx` exports encode through them. Governed by [ADR 0012](../adr/0012-alloy-sol-bindings-and-registry-authority.md) and [ADR 0070](../adr/0070-onchain-transaction-helper-boundary.md). | Contract | Yes | `crates/contracts/src/settlement.rs::call_tests::set_pre_signature_starts_with_the_canonical_upstream_selector`, `crates/contracts/src/settlement.rs::call_tests::invalidate_order_starts_with_the_canonical_upstream_selector`, `crates/contracts/src/settlement.rs::call_tests::set_pre_signature_encodes_the_signed_flag_in_the_second_head_word`, `crates/contracts/tests/parity_contract.rs::parity_fixture_cases_hold`, `parity/fixtures/contracts.json` | 2026-06-22 |

## Signature recovery & EIP-1271

| Id | Crate | Property | Type | Covered | Evidence | Last reviewed |
| --- | --- | --- | --- | --- | --- | --- |
| `PROP-CON-005` | `cow-sdk-contracts` | Every ECDSA signature returned by `cow_sdk_contracts::RecoverableSignature` carries a canonical recovery byte in the legacy `{27, 28}` range. Inputs with `v ∈ {0, 1}` are canonicalized; any other byte (including the EIP-155 chain-encoded range `35..=255`) is rejected with `ContractsError::InvalidSignatureRecoveryByte`. Governed by [ADR 0022](../adr/0022-ecdsa-signature-v-normalization.md). | Contract | Yes | `crates/contracts/tests/signature_contract.rs`, `crates/contracts/tests/recoverable_signature_contract.rs`, `crates/contracts/tests/v_normalization_contract.rs`, `crates/contracts/tests/property_contract.rs::ecdsa_v_normalization_rejects_every_excluded_byte_value`, `parity/fixtures/ecdsa/v_normalization.json`, `fuzz/fuzz_targets/fuzz_ecdsa_v_normalization.rs`, `fuzz/fuzz_targets/fuzz_recoverable_signature_differential.rs`, `fuzz/fuzz_targets/fuzz_recoverable_signature_parse_hex.rs`, `fuzz/fuzz_targets/fuzz_eip1271_signature_data_codec.rs` | 2026-05-28 |
| `PROP-CON-009` | `cow-sdk-contracts` | `Signature::recover_ecdsa_address` recovers EIP-712 and EthSign ECDSA signers from the supplied digest (delegating to `RecoverableSignature::recover`), rejects non-ECDSA variants, and `Signature::declared_address` reports only declared-owner signature schemes. | Contract | Yes | `crates/contracts/tests/signature_contract.rs::signature_helpers_preserve_public_contract_surface`, `crates/contracts/tests/recoverable_signature_contract.rs::recover_eip712_round_trip_returns_the_signing_address`, `crates/contracts/tests/recoverable_signature_contract.rs::recover_eth_sign_round_trip_applies_eip191_prehash_internally`, `crates/contracts/tests/signature_contract.rs::recover_ecdsa_address_rejects_non_ecdsa_variants`, `fuzz/fuzz_targets/fuzz_decode_magic_value_response.rs`, `fuzz/fuzz_targets/fuzz_order_signature_classify.rs` | 2026-06-14 |
| `PROP-CON-012` | `cow-sdk-contracts` | `fuzz_recover_ecdsa_address` carries a documented seed-corpus contract anchored to the ECDSA v-normalization parity vectors covering canonical ECDSA recovery bytes plus all-zero, all-`0xff`, and rejected-`v` boundaries. | Contract | Yes | `fuzz/fuzz_targets/fuzz_recover_ecdsa_address.rs`, `parity/fixtures/ecdsa/v_normalization.json`, `crates/contracts/tests/signature_contract.rs` | 2026-05-01 |
| `PROP-CON-008` | `cow-sdk-contracts` | `verify_eip1271_signature_cached` emits the canonical tracing span and cache telemetry events without recording verifier addresses, digests, signatures, or response bytes. | Contract | Yes | `crates/contracts/tests/verify_telemetry_contract.rs::verifier_emits_canonical_span_and_safe_miss_store_events`, `crates/contracts/tests/verify_telemetry_contract.rs::verifier_emits_hit_event_without_reaching_provider`, `crates/contracts/tests/verify_telemetry_contract.rs::verifier_emits_skip_event_for_non_cacheable_errors` | 2026-05-27 |

## Log decoders (fail-closed)

| Id | Crate | Property | Type | Covered | Evidence | Last reviewed |
| --- | --- | --- | --- | --- | --- | --- |
| `PROP-CON-019` | `cow-sdk-contracts` | `decode_order_placement` and `decode_order_invalidation` are fail-closed on untrusted `CoWSwapOnchainOrders` logs: topic count and topic-0, the on-chain signing scheme, the EIP-1271 owner-payload length, the eth-flow trailing-data length, and the 56-byte UID length are each validated; every malformed log returns a typed `ContractsError`; no input panics the decoder; `OrderPlacement` / `OrderInvalidation` topic-0 are byte-locked against an independent keccak of the canonical signatures; and a decoded order's UID reuses `compute_order_uid`. Governed by [ADR 0054](../adr/0054-onchain-order-event-decoding-is-fail-closed.md). | Contract | Yes | `crates/contracts/tests/onchain_orders.rs::order_placement_topic0_matches_canonical_hash`, `crates/contracts/tests/onchain_orders.rs::order_hash_matches_canonical_ethflow_foundry_vector`, `crates/contracts/tests/onchain_orders.rs::eip1271_placement_decodes_owner_uid_and_trailer`, `crates/contracts/tests/onchain_orders.rs::eip1271_owner_requires_twenty_byte_signature_payload`, `crates/contracts/tests/onchain_orders.rs::invalid_signing_scheme_is_rejected`, `crates/contracts/tests/onchain_orders.rs::order_invalidation_rejects_wrong_uid_length`, `crates/contracts/src/primitives.rs::tests::order_kind_marker_round_trips_and_rejects_unknown`, `fuzz/fuzz_targets/fuzz_onchain_order_log_decode.rs` | 2026-05-28 |
| `PROP-CON-021` | `cow-sdk-contracts` | `decode_settlement_log` is fail-closed on untrusted `GPv2Settlement` logs: topic count and topic-0 are validated through the shared `check_topics` guard and the 56-byte order-UID length is checked before mapping; every malformed log returns a typed `ContractsError` and no input panics the decoder; the `Trade` / `Interaction` / `Settlement` / `OrderInvalidated` / `PreSignature` topic-0 are byte-locked against an independent keccak of the canonical signatures; and each event maps into the `#[non_exhaustive]` `SettlementEvent` domain type. Governed by [ADR 0054](../adr/0054-onchain-order-event-decoding-is-fail-closed.md). | Contract | Yes | `crates/contracts/tests/settlement_events_contract.rs::settlement_event_topic0_byte_locks_match_canonical_keccak`, `crates/contracts/tests/settlement_events_contract.rs::trade_round_trips`, `crates/contracts/tests/settlement_events_contract.rs::interaction_round_trips_including_bytes4_selector`, `crates/contracts/tests/settlement_events_contract.rs::settlement_invalidated_and_presignature_round_trip`, `crates/contracts/tests/settlement_events_contract.rs::unknown_topic0_is_rejected`, `crates/contracts/tests/settlement_events_contract.rs::missing_indexed_topic_is_rejected`, `crates/contracts/tests/settlement_events_contract.rs::wrong_order_uid_length_is_rejected`, `fuzz/fuzz_targets/fuzz_settlement_event_log_decode.rs` | 2026-05-29 |
| `PROP-CON-022` | `cow-sdk-contracts` | `decode_order_refund` and the unified `decode_eth_flow_log` dispatcher are fail-closed on untrusted eth-flow lifecycle logs: topic count and topic-0 are validated and the 56-byte order UID is length-checked before mapping; `decode_eth_flow_log` routes the `OrderPlacement` / `OrderInvalidation` / `OrderRefund` topic-0 to the matching decoder and returns the typed `EthFlowEvent`; every malformed log returns a typed `ContractsError` and no input panics the decoder; the `OrderRefund` topic-0 is byte-locked against an independent keccak of the canonical signature. Governed by [ADR 0054](../adr/0054-onchain-order-event-decoding-is-fail-closed.md). | Contract | Yes | `crates/contracts/tests/eth_flow_events_contract.rs::order_refund_topic0_matches_canonical_hash`, `crates/contracts/tests/eth_flow_events_contract.rs::order_refund_round_trips`, `crates/contracts/tests/eth_flow_events_contract.rs::decode_eth_flow_log_dispatches_all_three_events`, `crates/contracts/tests/eth_flow_events_contract.rs::order_refund_missing_indexed_topic_is_rejected`, `crates/contracts/tests/eth_flow_events_contract.rs::order_refund_wrong_uid_length_is_rejected`, `fuzz/fuzz_targets/fuzz_eth_flow_event_log_decode.rs` | 2026-05-29 |

## Eth-flow & calldata builders

| Id | Crate | Property | Type | Covered | Evidence | Last reviewed |
| --- | --- | --- | --- | --- | --- | --- |
| `PROP-CON-016` | `cow-sdk-contracts` | `fuzz_ethflow_create_order_encode` carries a documented seed-corpus contract anchored to contracts parity fixture data covering canonical EthFlow order ABI shapes plus zero-amount, maximum-timestamp, all-zero, and all-`0xff` boundaries. | Contract | Yes | `fuzz/fuzz_targets/fuzz_ethflow_create_order_encode.rs`, `parity/fixtures/contracts.json` | 2026-05-01 |
| `PROP-CON-018` | `cow-sdk-contracts` | `EthFlowOrderData::new` and `EthFlowOrderData::from_unsigned_order` reject `receiver == Address::ZERO` with `ContractsError::ZeroReceiver`, pre-empting the deployed `CoWSwapEthFlow` contract's `ReceiverMustBeSet()` revert (selector `0xefc9ccdf`) raised by `EthFlowOrder.toCoWSwapOrder` on both the `createOrder` and `invalidateOrder` write paths. The rule lives in the private `reject_zero_receiver` helper invoked by the `EthFlowOrderData` construction paths; the general order hash path treats `address(0)` as the pay-to-owner sentinel and hashes it verbatim. Governed by [ADR 0020](../adr/0020-ethflow-owner-threading.md). | Contract | Yes | `crates/contracts/src/eth_flow.rs::tests::new_rejects_zero_receiver_with_zero_receiver_error`, `crates/contracts/src/eth_flow.rs::tests::from_unsigned_order_rejects_zero_receiver_with_zero_receiver_error`, `crates/contracts/src/eth_flow.rs::tests::zero_receiver_invariant_matches_ethflow_on_chain_revert_selector`, `crates/contracts/tests/property_contract.rs::ethflow_order_data_new_rejects_zero_receiver_iff_address_is_zero` | 2026-05-30 |
| `PROP-CON-025` | `cow-sdk-contracts` | The gas-free transaction builders return an `UnsignedTransaction { to, data, value }` carrying no gas limit (the upstream services `eth::Tx` shape; the caller estimates gas, signs, and submits). `approve_transaction` targets the ERC-20 token with zero native value and the `encode_approve` `approve(spender, amount)` call-data; `ethflow_invalidate_order_transaction` targets the resolved eth-flow deployment with zero native value and the `encode_invalidate_order_calldata` payload projected from the order, failing closed with `ContractsError::DeploymentNotFound` when the deployment is absent and `ContractsError::ZeroReceiver` on a zero receiver. Governed by [ADR 0070](../adr/0070-onchain-transaction-helper-boundary.md). | Contract | Yes | `crates/contracts/src/tokens.rs::tests::approve_transaction_targets_token_with_zero_value_and_encodes_spender`, `crates/contracts/src/tx.rs::tests::ethflow_invalidate_targets_eth_flow_with_zero_value_and_invalidate_calldata` | 2026-06-22 |
| `PROP-CON-026` | `cow-sdk-contracts` | Composable conditional-order encoding is contract-faithful and the merkle multiplexer round-trips. `conditional_order_id` reproduces the on-chain `ComposableCoW.hash` formula `keccak256(abi.encode(params))` and the merkle leaf double-hashes it as `ComposableCoW._auth` does; the TWAP builder lowers totals into the upstream `TWAPOrder.Data` per-part field order as a 320-byte static input, validating against the handler revert sites and rejecting indivisible totals; `twap_create_transaction` routes start-at-mining-time orders through `createWithContext` and start-at-epoch orders through `create`. The hand-rolled multiplexer's proofs verify against its root through the `OpenZeppelin` sorted-pair algorithm the contract uses, over arbitrary tree sizes, verified from an order's params. `TwapStaticInput::timing_at` selects the live discrete part from the schedule exactly as `TWAPOrderMathLib.calculateValidTo` does — a part opens each interval and the schedule ends at `start + n * t` regardless of the span. Governed by [ADR 0048](../adr/0048-composable-conditional-order-framework.md). | Property | Yes | `crates/contracts/src/composable/registry.rs::tests::conditional_order_id_matches_onchain_hash_formula`, `crates/contracts/src/composable/multiplexer.rs::tests::merkle_leaf_double_hashes_the_id`, `crates/contracts/src/composable/twap.rs::tests::static_input_is_320_bytes_of_static_words`, `crates/contracts/src/composable/twap.rs::tests::validation_mirrors_upstream_revert_sites`, `crates/contracts/src/composable/twap.rs::tests::epoch_start_routes_to_create`, `crates/contracts/src/composable/twap.rs::tests::timing_selects_the_part_and_valid_to`, `crates/contracts/src/composable/twap.rs::tests::timing_flags_the_last_part_then_expires`, `crates/contracts/src/composable/twap.rs::tests::timing_span_closes_the_part_early_without_shortening_the_schedule`, `crates/contracts/src/composable/multiplexer.rs::tests::every_leaf_proof_verifies_across_sizes`, `fuzz/fuzz_targets/fuzz_composable_merkle_roundtrip.rs` | 2026-06-26 |

## COW Shed hooks

| Id | Crate | Property | Type | Covered | Evidence | Last reviewed |
| --- | --- | --- | --- | --- | --- | --- |
| `PROP-SHED-001` | `cow-sdk-contracts` | COW Shed `executeHooks` calldata encoding is owner-agnostic: `encode_execute_hooks_calldata_with_signature` passes the owner signature through as the factory's `bytes` argument, so both a 65-byte EOA ECDSA signature and a variable-length EIP-1271 contract-signature blob encode and round-trip through `COWShedFactory::executeHooks`, while the typed EOA entry point `encode_execute_hooks_calldata_signed` is byte-identical to the general encoder fed the same 65-byte signature and reproduces the reference calldata vectors byte-for-byte. This keeps the proxy's length-based on-chain dispatch — ECDSA recovery for a 65-byte signature, `isValidSignature` otherwise — reachable from the SDK for both externally-owned and smart-contract owners. The ERC-2098 compact representation lives solely on `RecoverableSignature` (`to_erc2098` normalizes to low-s per BIP-62, so a high-s input round-trips to its canonical twin; `parse_erc2098` is the inverse), locked by the cow_shed eoa-signature-byte-order fixture including an explicit high-s normalization row. Governed by [ADR 0049](../adr/0049-cow-shed-account-abstraction-proxy.md). | Contract | Yes | `crates/contracts/tests/signed_calldata_parity_contract.rs::signed_calldata_matches_reference_vectors_and_round_trips`, `crates/contracts/tests/signed_calldata_parity_contract.rs::with_signature_covers_eoa_and_eip1271`, `crates/contracts/tests/eoa_signature_byte_order_contract.rs::compact_round_trips_through_the_canonical_twin` | 2026-06-12 |
| `PROP-SHED-002` | `cow-sdk-contracts` | `CowShedHooks::sign`, the crate's one signer-mediated async method, emits exactly one `sign` span carrying `chain`, `version`, and `endpoint = "cow_shed.sign"` when the `tracing` feature is enabled. The owner is resolved from the supplied signer and is not surfaced, and the span records no signer, signature, owner, nonce, deadline, or typed-data payload material; the deterministic proxy, EIP-712, and calldata helpers carry no spans. Governed by [ADR 0049](../adr/0049-cow-shed-account-abstraction-proxy.md). | Contract | Yes | `crates/contracts/tests/sign_telemetry_contract.rs::sign_emits_one_span_with_chain_version_and_endpoint`, `crates/contracts/src/cow_shed/hooks.rs`, `docs/guides/observability.md` | 2026-06-08 |
