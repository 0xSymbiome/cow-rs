---
type: Property
id: js
title: "JS/WASM boundary invariants"
description: "The `cow-sdk-js` wasm-bindgen FFI boundary: facade surface and coverage, error normalization, cancellation, callback lifetimes, redaction, and the raw/facade snapshot contracts for the browser bindings."
resource: https://github.com/0xSymbiome/cow-rs/blob/main/docs/properties/js.md
families: [PROP-WB]
tags: [property, invariants]
timestamp: 2026-06-29T00:00:00Z
---

# JS/WASM boundary invariants

The `cow-sdk-js` wasm-bindgen FFI boundary: facade surface and coverage, error normalization, cancellation, callback lifetimes, redaction, and the raw/facade snapshot contracts for the browser bindings. Part of the [Properties Registry](index.md): 30 invariant(s), 30 covered.

## Host-pure boundary, exports & DTOs

| Id | Crate | Property | Type | Covered | Evidence | Last reviewed |
| --- | --- | --- | --- | --- | --- | --- |
| `PROP-WB-001` | `cow-sdk-js` | Host-pure helpers stay free of JavaScript ABI types and wrap shared Rust protocol helpers without host API dependencies. Governed by [ADR 0039](../adr/0039-typescript-callable-wasm-sdk-surface.md). | Contract | Yes | `crates/js/tests/host_pure_helpers.rs::typed_data_payload_matches_signing_module_output`, `crates/js/tests/host_pure_helpers.rs::generated_order_uid_uses_canonical_strings`, `crates/js/tests/host_pure_helpers.rs::eip1271_payload_matches_signing_module_output_and_vector` | 2026-05-09 |
| `PROP-WB-002` | `cow-sdk-js` | The exports module owns every wasm-bindgen and TypeScript declaration boundary, while host builds continue to compile the pure helper tree. Governed by [ADR 0039](../adr/0039-typescript-callable-wasm-sdk-surface.md). | Contract | Yes | `crates/js/tests/host_pure_helpers.rs::wasm_version_matches_package_version`, `crates/js/tests/wasm_surface_contract.rs::wasm_version_matches_crate_version`, `tests/wasm_dependency_invariant.rs::cow_sdk_js_wasm32_tree_omits_forbidden_dependencies` | 2026-05-09 |
| `PROP-WB-004` | `cow-sdk-js` | Order UID and digest DTO fields reuse the canonical `to_hex_string()` string surfaces instead of re-encoding byte arrays. Governed by [ADR 0039](../adr/0039-typescript-callable-wasm-sdk-surface.md). | Contract | Yes | `crates/js/tests/host_pure_helpers.rs::generated_order_uid_uses_canonical_strings`, `crates/js/tests/wasm_surface_contract.rs::compute_order_uid_returns_uid_and_digest_strings`, `crates/js/tests/wasm_eip1271_contract.rs::sign_order_with_eip1271_uid_equals_generated_order_id_as_str` | 2026-06-20 |
| `PROP-WB-015` | `cow-sdk-js` | Deterministic WASM helper logic lives in the host-safe `cow-sdk-js::helpers` module without JavaScript FFI imports, while wasm exports adapt those helpers into JavaScript-visible envelopes. Governed by [ADR 0039](../adr/0039-typescript-callable-wasm-sdk-surface.md). | Contract | Yes | `crates/js/tests/no_ffi_helpers.rs::helpers_module_does_not_import_ffi_bindings`, `crates/js/tests/host_pure_helpers.rs::typed_data_payload_matches_signing_module_output`, `crates/js/tests/host_pure_helpers.rs::eip1271_payload_matches_signing_module_output_and_vector` | 2026-06-09 |
| `PROP-WB-018` | `cow-sdk-js` | The JavaScript-visible success **envelope** carries `schemaVersion` and preserves an `__unknown` schema sentinel so a persisted envelope stays parseable across SDK versions. Thrown errors are not version-tagged (see `PROP-WB-029`). Governed by [ADR 0039](../adr/0039-typescript-callable-wasm-sdk-surface.md). | Contract | Yes | `crates/js/tests/wasm_envelope_contract.rs::envelope_serializes_schema_version_and_payload`, `crates/js/tests/wasm_envelope_contract.rs::envelope_preserves_unknown_schema_sentinel`, `crates/js/src/exports/envelope.rs` | 2026-06-20 |
| `PROP-WB-028` | `cow-sdk-js` | Every DTO emitted into a flavour's raw wasm-bindgen bindings is reachable from a public export in that flavour (transitively through fields), or is an explicit standalone type — an opaque newtype the surface renders as `string`, a thrown error type, or a callback request shape. A reachability contract test asserts no `#[derive(Tsify)]` DTO is emitted-but-unreachable, so a feature-specific DTO whose `#[cfg]` is broader than the union of its consumers' gates leaks into a leaner flavour — invisible to the facade-coverage gate when both layers carry it symmetrically — and fails the build instead. Governed by [ADR 0039](../adr/0039-typescript-callable-wasm-sdk-surface.md). | Contract | Yes | `crates/js/tests/wasm_flavour_reachability_contract.rs::every_raw_dto_is_reachable_or_allowlisted`, `crates/js/src/dto/mod.rs`, `crates/js/snapshots/raw` | 2026-06-20 |

## EIP-1271 & callback signing

| Id | Crate | Property | Type | Covered | Evidence | Last reviewed |
| --- | --- | --- | --- | --- | --- | --- |
| `PROP-WB-003` | `cow-sdk-js` | EIP-1271 smart-account signing resolves JavaScript callbacks at the facade boundary and stores only a pure resolved provider in Rust. Governed by [ADR 0040](../adr/0040-wallet-provider-callback-boundary-for-js-consumers.md). | Contract | Yes | `crates/js/src/exports/eip1271.rs` (compile-time `Send + Sync` assertion on `ResolvedEip1271Provider`), `crates/js/tests/wasm_eip1271_contract.rs::custom_eip1271_callback_signature_is_used_verbatim`, `crates/js/tests/wasm_eip1271_contract.rs::sign_order_with_eip1271_returns_eip1271_scheme` | 2026-06-01 |
| `PROP-WB-007` | `cow-sdk-js` | JavaScript callback returns are normalized through `Promise::resolve`, preserving identical handling for plain values, native Promises, and thenables. Governed by [ADR 0040](../adr/0040-wallet-provider-callback-boundary-for-js-consumers.md). | Contract | Yes | `crates/js/tests/wasm_callback_transport_contract.rs::callback_transport_receives_request_dto_with_signal`, `crates/js/tests/wasm_callback_transport_contract.rs::callback_transport_reject_maps_to_typed_error`, `crates/js/tests/wasm_callback_contract.rs::typed_data_signer_receives_order_envelope` | 2026-05-11 |
| `PROP-WB-016` | `cow-sdk-js` | Callback registry state stays internal to client construction and never appears as public JavaScript handle classes, registry ids, or manual registration functions. Governed by [ADR 0039](../adr/0039-typescript-callable-wasm-sdk-surface.md). | Contract | Yes | `crates/js/tests/wasm_snapshot_surface_contract.rs::generated_type_declarations_hide_callback_registry`, `crates/js/tests/wasm_callback_lifetime_contract.rs::client_owned_callback_survives_until_request_resolves`, `crates/js/tests/wasm_facade_snapshot_contract.rs::facade_declarations_hide_raw_wasm_bindgen_surface` | 2026-05-11 |
| `PROP-WB-017` | `cow-sdk-js` | Abort bridging removes JavaScript abort listeners on success, callback throw, callback rejection, parse failure, and timeout-overflow paths. Governed by [ADR 0040](../adr/0040-wallet-provider-callback-boundary-for-js-consumers.md). | Contract | Yes | `crates/js/tests/wasm_cancellation_contract.rs::abort_bridge_removes_listener_after_success`, `crates/js/tests/wasm_cancellation_contract.rs::abort_bridge_removes_listener_after_callback_throw`, `crates/js/tests/wasm_cancellation_contract.rs::abort_bridge_removes_listener_after_callback_reject`, `crates/js/tests/wasm_cancellation_contract.rs::abort_bridge_removes_listener_after_parse_error`, `crates/js/tests/wasm_cancellation_contract.rs::abort_bridge_removes_listener_after_timeout_overflow` | 2026-05-11 |

## Callback HTTP transport

| Id | Crate | Property | Type | Covered | Evidence | Last reviewed |
| --- | --- | --- | --- | --- | --- | --- |
| `PROP-WB-005` | `cow-sdk-js` | Callback-transport timeout configuration fails closed before dispatch when the timeout cannot be represented safely at the JavaScript boundary. Governed by [ADR 0040](../adr/0040-wallet-provider-callback-boundary-for-js-consumers.md). | Contract | Yes | `crates/js/tests/wasm_callback_transport_contract.rs::timeout_overflow_fails_before_dispatch`, `crates/js/tests/wasm_fail_closed_contract.rs::abort_controller_is_available_for_callback_transport` | 2026-05-11 |
| `PROP-WB-006` | `cow-sdk-js` | Callback HTTP transport maps success, non-2xx responses, callback rejection, and malformed timeout configuration into typed error or response envelopes without exposing raw JavaScript failures. Governed by [ADR 0040](../adr/0040-wallet-provider-callback-boundary-for-js-consumers.md). | Contract | Yes | `crates/js/tests/wasm_callback_transport_contract.rs::callback_transport_receives_request_dto_with_signal`, `crates/js/tests/wasm_callback_transport_contract.rs::callback_transport_maps_non_2xx_to_typed_error`, `crates/js/tests/wasm_callback_transport_contract.rs::callback_transport_reject_maps_to_typed_error`, `crates/js/tests/wasm_callback_transport_contract.rs::timeout_overflow_fails_before_dispatch` | 2026-05-11 |
| `PROP-WB-008` | `cow-sdk-js` | Callback HTTP request DTOs attach a live `AbortSignal` with `Reflect::set` rather than serializing it through serde. Governed by [ADR 0040](../adr/0040-wallet-provider-callback-boundary-for-js-consumers.md). | Contract | Yes | `crates/js/tests/wasm_callback_transport_contract.rs::callback_transport_receives_request_dto_with_signal`, `crates/js/tests/wasm_fail_closed_contract.rs::abort_controller_is_available_for_callback_transport` | 2026-05-11 |
| `PROP-WB-014` | `cow-sdk-js` | The runtime-neutral JavaScript callback transport lets consumers drive the SDK from runtimes outside the CI-tested set, such as a self-built Deno target, on a best-effort basis that is not part of the default release evidence claim. Governed by [ADR 0039](../adr/0039-typescript-callable-wasm-sdk-surface.md). | Contract | Yes | `crates/js/tests/wasm_callback_transport_contract.rs::callback_transport_receives_request_dto_with_signal`, `crates/js/tests/wasm_eip1271_contract.rs::eip1271_payload_matches_recorded_typescript_sdk_vector` | 2026-06-04 |
| `PROP-WB-020` | `cow-sdk-js` | JavaScript `TransportPolicyConfig` maps into the shared Rust transport policy for every HTTP-capable WASM client and rejects invalid policy values during construction. Governed by [ADR 0039](../adr/0039-typescript-callable-wasm-sdk-surface.md). | Contract | Yes | `crates/js/tests/wasm_transport_policy_contract.rs::all_client_constructors_accept_transport_policy`, `crates/js/tests/wasm_transport_policy_contract.rs::invalid_transport_policy_user_agent_is_rejected`, `crates/js/tests/wasm_snapshot_surface_contract.rs::generated_type_declarations_expose_transport_policy_config_for_http_flavours` | 2026-05-11 |

## Error normalization

| Id | Crate | Property | Type | Covered | Evidence | Last reviewed |
| --- | --- | --- | --- | --- | --- | --- |
| `PROP-WB-009` | `cow-sdk-js` | JavaScript-visible `WasmError` values preserve the workspace redaction posture for transport, app-data, signing, orderbook, subgraph, trading, and cancellation failures. Governed by [ADR 0039](../adr/0039-typescript-callable-wasm-sdk-surface.md). | Contract | Yes | `crates/js/tests/wasm_redaction_contract.rs::transport_connect_error_uses_redacted_message`, `crates/js/tests/wasm_redaction_contract.rs::http_status_error_redacts_headers_and_body`, `crates/js/tests/wasm_redaction_contract.rs::errors_module_does_not_unwrap_redacted_values` | 2026-05-09 |
| `PROP-WB-024` | `cow-sdk-js` | A wallet timeout raised while signing surfaces as the typed `walletTimeout` error carrying `timeoutMs` on every signing path — typed-data and the `eth_sign` digest path for both order and cancellation signing — rather than collapsing into `walletRequest` at the `DigestSigner` adapter boundary, whose unconstrained associated error type would otherwise flatten the typed error to a message string and lose the discriminant. Governed by [ADR 0040](../adr/0040-wallet-provider-callback-boundary-for-js-consumers.md) and [ADR 0045](../adr/0045-async-signer-trait-narrowing.md). | Contract | Yes | `crates/js/tests/wasm_callback_contract.rs::wallet_config_timeout_rejects_pending_signer_callback`, `crates/js/tests/wasm_callback_contract.rs::eth_sign_digest_timeout_preserves_wallet_timeout_kind`, `crates/js/tests/wasm_callback_contract.rs::eth_sign_cancellation_timeout_preserves_wallet_timeout_kind` | 2026-06-03 |
| `PROP-WB-025` | `cow-sdk-js` | The JavaScript-visible `WasmError` `orderbook` variant carries a `retryable` boolean (always serialised, defaulting to `false` through `#[serde(default)]`) and an optional `retryAfterMs`, projecting the native `OrderbookError::is_retryable` / `backoff_hint` verdict to JavaScript so a consumer driving its own retry loop does not re-derive the retryable-status set. Governed by [ADR 0060](../adr/0060-uniform-error-classification.md). | Contract | Yes | `crates/js/src/exports/errors.rs`, `crates/js/tests/wasm_error_abi_contract.rs::orderbook_variant_carries_retry_hints`, `crates/js/tests/wasm_error_abi_contract.rs::orderbook_variant_defaults_retryable_and_omits_absent_backoff`, `crates/js/snapshots/raw/orderbook.d.ts` | 2026-06-06 |
| `PROP-WB-029` | `cow-sdk-js` | Every error a wasm export throws is a single `CowError` class extending `Error`: it is `instanceof CowError` and `instanceof Error`, is keyed by `kind`, exposes its per-kind fields (and the `__unknown` sentinel's `raw`) as enumerable own properties, and carries no `schemaVersion`. `JSON.stringify` keeps the redacted `message` and fields while omitting `name`/`stack`; `toJSON()` and the static `CowError.fromJSON` round-trip the full field set across a `structuredClone` boundary; and the exported `isCowError` guard is exactly `instanceof CowError`. Governed by [ADR 0039](../adr/0039-typescript-callable-wasm-sdk-surface.md). | Contract | Yes | `crates/js/npm/tests/facade-error-normalization.test.ts`, `crates/js/npm/src/errors.ts`, `crates/js/tests/wasm_error_abi_contract.rs::unknown_sentinel_round_trips_raw_payload` | 2026-06-20 |
| `PROP-WB-030` | `cow-sdk-js` | The facade `CowError` discriminant set is compile-checked against the type (`KNOWN_KINDS satisfies Record<CowErrorKind, true>`), so the runtime kind check cannot drift from the union, and the hand-written `orderbook` member's retry fields (`retryable`, `retryAfterMs`) match the generated wasm-bindgen shape in presence and optionality. Governed by [ADR 0039](../adr/0039-typescript-callable-wasm-sdk-surface.md) and [ADR 0060](../adr/0060-uniform-error-classification.md). | Contract | Yes | `crates/js/npm/tests/facade-error-shape.test.ts`, `crates/js/npm/src/errors.ts`, `crates/js/snapshots/raw/orderbook.d.ts` | 2026-06-20 |
| `PROP-WB-032` | `cow-sdk-js` | A JavaScript-visible `orderbook` error carries the services `errorType` wire tag (`"InsufficientAllowance"` vs `"InsufficientBalance"`, and the rest) as the fine-grained partner of the coarse `category`, so a consumer branches on the exact rejection without parsing the message. The tag is projected from the typed `OrderbookRejection` through its derived `Serialize` — the single source mirroring `parse_rejection` — and the `Unknown` fallback projects its sanitized code; only the sanitized tag crosses, never the free-form services description. Governed by [ADR 0039](../adr/0039-typescript-callable-wasm-sdk-surface.md) and [ADR 0060](../adr/0060-uniform-error-classification.md). | Contract | Yes | `crates/js/src/exports/errors.rs`, `crates/js/npm/src/errors.ts`, `crates/orderbook/tests/rejection_contract.rs::every_documented_error_type_classifies_to_a_typed_variant`, `crates/orderbook/tests/rejection_contract.rs::unknown_rejection_projects_its_sanitized_code`, `crates/js/npm/tests/facade-error-normalization.test.ts` | 2026-06-20 |
| `PROP-WB-033` | `cow-sdk-js` | The exported `isUserRejection(value)` guard returns `true` only for a user-initiated rejection — a declined wallet request (EIP-1193 code `4001`) or a `cancelled` operation — and `false` for every other error and any non-`CowError` value, so a UI can treat a declined signature as a soft state without hardcoding the `4001` magic number. Governed by [ADR 0039](../adr/0039-typescript-callable-wasm-sdk-surface.md). | Contract | Yes | `crates/js/npm/src/errors.ts`, `crates/js/npm/tests/facade-retry.test.ts` | 2026-06-20 |

## Packaging & runtime flavours

| Id | Crate | Property | Type | Covered | Evidence | Last reviewed |
| --- | --- | --- | --- | --- | --- | --- |
| `PROP-WB-010` | `cow-sdk-js` | Cloudflare Workers consume the web-target output through each flavour's `…/edge` and `…/edge/wasm` subpaths (for example `./trading/edge`), without dynamic WebAssembly compilation entry points in worker source. Governed by [ADR 0039](../adr/0039-typescript-callable-wasm-sdk-surface.md). | Contract | Yes | `crates/js/tests/wasm_fail_closed_contract.rs::flavour_descriptor_exposes_web_and_module_subpaths`, `crates/js/tests/wasm_fail_closed_contract.rs::worker_source_avoids_dynamic_wasm_compilation_entrypoints`, `e2e/wasm-typescript-cf/tests/forbidden-instantiation.spec.ts` | 2026-06-19 |
| `PROP-WB-011` | `cow-sdk-js` | Browser-bundler consumers stay default-http-supported through package exports and browser e2e coverage. Governed by [ADR 0039](../adr/0039-typescript-callable-wasm-sdk-surface.md). | Contract | Yes | `crates/js/tests/wasm_surface_contract.rs::client_constructors_accept_supported_runtime_inputs`, `crates/js/tests/wasm_callback_transport_contract.rs::callback_transport_receives_request_dto_with_signal`, `e2e/wasm-typescript/tests/browser/browser.spec.ts` | 2026-05-11 |
| `PROP-WB-012` | `cow-sdk-js` | Node.js 22 and 24 LTS consumers stay callback-http-tested through the nodejs package subpath and callback transport. Governed by [ADR 0039](../adr/0039-typescript-callable-wasm-sdk-surface.md). | Contract | Yes | `crates/js/tests/wasm_callback_transport_contract.rs::callback_transport_receives_request_dto_with_signal`, `crates/js/tests/wasm_callback_transport_contract.rs::callback_transport_maps_non_2xx_to_typed_error`, `e2e/wasm-typescript/tests/transport.spec.ts` | 2026-05-11 |
| `PROP-WB-013` | `cow-sdk-js` | Cloudflare Workers stay callback-http-tested through web-target initialization, callback transport, and source-level dynamic-instantiation rejection. Governed by [ADR 0039](../adr/0039-typescript-callable-wasm-sdk-surface.md). | Contract | Yes | `crates/js/tests/wasm_fail_closed_contract.rs::flavour_descriptor_exposes_web_and_module_subpaths`, `crates/js/tests/wasm_fail_closed_contract.rs::worker_source_avoids_dynamic_wasm_compilation_entrypoints`, `e2e/wasm-typescript-cf/tests/init-once.spec.ts`, `e2e/wasm-typescript-cf/tests/orderbook.spec.ts` | 2026-06-19 |
| `PROP-WB-019` | `cow-sdk-js` | Per-flavor package builds expose default, orderbook, signing, and `trading` public subpaths with measured release-size budgets and without raw generated import paths. Every flavour is built for the `bundler`, `nodejs`, and `web` targets plus the source-phase `module` build — the web build backs the `browser`, `import`, `default`, and edge export conditions and each flavour's `…/edge` subpath, `…/module` is the opt-in source-phase build, and the `bundler` and `nodejs` raw builds back the facade ESM and CommonJS entries. No flavour is browser-portable while another is bundler-only. Each flavour emits one wasm binary shared across its targets, and its gzip-compressed size is verified against an explicit per-flavour byte budget that tracks Cloudflare's published Workers Free compressed-size limit on every release build. Governed by [ADR 0044](../adr/0044-bundle-size-profile-and-flavor-builds.md). | Contract | Yes | `crates/js/tests/wasm_facade_snapshot_contract.rs::facade_declarations_match_flavour_matrix`, `crates/js/tests/wasm_fail_closed_contract.rs::flavour_descriptor_exposes_web_and_module_subpaths`, `crates/js/npm/scripts/measure-wasm-size.mjs`, `crates/js/npm/flavours.json` | 2026-06-19 |
| `PROP-WB-027` | `cow-sdk-js` | The hand-written TypeScript facade exposes every public symbol of each flavour's raw wasm-bindgen bindings — every client method, free function, and exported DTO type — except an explicit, reasoned allowlist (opaque newtypes surfaced as strings, init plumbing, deliberate renames, and the options-module fetch type). A coverage contract test parses the committed raw and facade `.d.ts` snapshots and fails when a raw export has no facade counterpart, so a binding can never ship reachable from the raw layer yet absent from the published facade. Governed by [ADR 0039](../adr/0039-typescript-callable-wasm-sdk-surface.md). | Contract | Yes | `crates/js/tests/wasm_facade_coverage_contract.rs::facade_covers_raw_public_surface`, `crates/js/npm/src/default.ts`, `crates/js/npm/src/trading.ts`, `crates/js/npm/src/orderbook.ts`, `crates/js/npm/src/signing.ts`, `crates/js/snapshots/facade`, `crates/js/snapshots/raw` | 2026-06-20 |

## Telemetry, retry & tradeoffs

| Id | Crate | Property | Type | Covered | Evidence | Last reviewed |
| --- | --- | --- | --- | --- | --- | --- |
| `PROP-WB-026` | `cow-sdk-js` | When the `tracing` feature is enabled, every instrumented JavaScript export emits exactly one span carrying a stable `endpoint` label of the form `wasm.<area>.<method>` (covering the trading, orderbook, signing, eip1271, subgraph, and ipfs export areas). The spans use `skip_all`, so no JavaScript callback, signer, payload, or wallet input is captured, and `cow-sdk-js`'s feature does not enable the underlying Rust crates' spans, so each export call surfaces exactly one `wasm.*` span. Synchronous transaction-building exports carry no spans. | Contract | Yes | `crates/js/tests/wasm_telemetry_contract.rs::ipfs_fetch_export_emits_one_endpoint_labelled_span`, `crates/js/src/exports/ipfs.rs`, `crates/js/src/exports/signing.rs`, `crates/js/src/exports/subgraph.rs`, `.github/workflows/wasm.yml`, `docs/guides/observability.md` | 2026-06-08 |
| `PROP-WB-031` | `cow-sdk-js` | The exported `withRetry(fn, options?)` helper retries an awaitable only when the SDK classifies the failure as retryable (`isRetryable`, a transient orderbook fault), waiting the server `retryAfterMs` when present and otherwise an exponential backoff, up to the configured budget; any non-retryable failure is rethrown immediately as a `CowError` and never retried. An optional `onRetry(attempt, error, delayMs)` hook fires before each backoff, fire-and-forget — a throw from it is swallowed, so it never aborts the retry or masks the underlying `CowError`. Governed by [ADR 0060](../adr/0060-uniform-error-classification.md). | Contract | Yes | `crates/js/npm/tests/facade-retry.test.ts`, `crates/js/npm/src/retry.ts`, `crates/js/npm/src/errors.ts` | 2026-06-20 |
