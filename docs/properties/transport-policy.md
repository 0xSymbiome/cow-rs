---
type: Property
id: transport-policy
title: "Transport policy invariants"
description: "The `TransportPolicy` subsystem in `cow-sdk-core`: retry termination, backoff, `Retry-After` cooldown, timeout, redaction, and host-allowlist contracts that govern every HTTP surface."
resource: https://github.com/0xSymbiome/cow-rs/blob/main/docs/properties/transport-policy.md
families: [PROP-TPP]
tags: [property, invariants]
timestamp: 2026-06-29T00:00:00Z
---

# Transport policy invariants

The `TransportPolicy` subsystem in `cow-sdk-core`: retry termination, backoff, `Retry-After` cooldown, timeout, redaction, and host-allowlist contracts that govern every HTTP surface. Part of the [Properties Registry](index.md): 14 invariant(s), 14 covered.

## Policy construction & defaults

| Id | Crate | Property | Type | Covered | Evidence | Last reviewed |
| --- | --- | --- | --- | --- | --- | --- |
| `PROP-TPP-001` | `cow-sdk-core` | The default orderbook transport policy keeps its reviewed user-agent, timeout, retry attempt count, limiter budget, and per-host scope stable. Governed by [ADR 0041](../adr/0041-transport-policy-l3-layering.md). | Contract | Yes | `crates/core/tests/policy_contract.rs::default_orderbook_transport_policy_is_stable` | 2026-05-08 |
| `PROP-TPP-002` | `cow-sdk-core` | The default subgraph transport policy keeps its reviewed user-agent, timeout, retry attempt count, limiter budget, and per-host scope stable. Governed by [ADR 0041](../adr/0041-transport-policy-l3-layering.md). | Contract | Yes | `crates/core/tests/policy_contract.rs::default_subgraph_transport_policy_is_stable` | 2026-05-08 |
| `PROP-TPP-010` | `cow-sdk-core` | Every documented `TransportPolicy` constructor (`new`, `default_orderbook`, `default_subgraph`, `default_trading`, `default_ipfs`), every `with_*` setter, and the `TransportPolicyBuilder` round-trip preserve their reviewed user-agent, retry, rate-limit, and timeout fields. The builder refines a caller-set client policy in place: its `user_agent` and `timeout` setters preserve every other client-policy field — including the response-byte cap and a deliberately disabled timeout — and `build` is infallible, so a seeded, otherwise-untouched builder is field-identical to `default_orderbook`. Governed by [ADR 0041](../adr/0041-transport-policy-l3-layering.md). | Contract | Yes | `crates/core/tests/policy_contract.rs::explicit_constructor_preserves_parts`, `crates/core/tests/policy_contract.rs::default_trading_uses_trading_user_agent_and_orderbook_limiter`, `crates/core/tests/policy_contract.rs::default_ipfs_disables_retry_and_timeout_and_uses_unlimited_limiter`, `crates/core/tests/policy_contract.rs::with_setters_replace_only_their_targeted_field`, `crates/core/tests/policy_contract.rs::user_agent_validation_error_surfaces_directly`, `crates/core/tests/policy_contract.rs::builder_round_trip_preserves_every_setter`, `crates/core/tests/policy_contract.rs::builder_defaults_to_orderbook_user_agent_when_unset` | 2026-06-12 |

## Retry & jitter

| Id | Crate | Property | Type | Covered | Evidence | Last reviewed |
| --- | --- | --- | --- | --- | --- | --- |
| `PROP-TPP-003` | `cow-sdk-core` | `RetryPolicy::no_retry` is deterministic and performs only the first attempt. Governed by [ADR 0041](../adr/0041-transport-policy-l3-layering.md). | Contract | Yes | `crates/core/tests/policy_contract.rs::no_retry_policy_is_idempotent` | 2026-05-08 |
| `PROP-TPP-004` | `cow-sdk-core` | Decorrelated retry jitter stays bounded by `RetryPolicy::max_delay`, and the `Retry-After` IMF-fixdate civil-day arithmetic stays panic-free under any attacker-controlled year value through `i64` promotion. Governed by [ADR 0041](../adr/0041-transport-policy-l3-layering.md). | Contract | Yes | `crates/core/tests/policy_contract.rs::decorrelated_jitter_is_bounded_by_max_delay`, `crates/core/src/transport/policy/retry_after.rs`, `fuzz/fuzz_targets/fuzz_parse_retry_after.rs`, `fuzz/fuzz_targets/fuzz_retry_policy_delay.rs`, `fuzz/fuzz_targets/fuzz_jitter_delay_for_attempt.rs` | 2026-05-14 |
| `PROP-TPP-011` | `cow-sdk-core` | Every `JitterStrategy` variant (`None`, `Full`, `Equal`, `Decorrelated`) yields a delay within the documented `[0, max_delay]` window; `None` returns the capped base delay unchanged; `Equal` preserves at least half the capped base delay; the zero-window case returns `Duration::ZERO`; `JitterStrategy::default()` returns the `Decorrelated` variant; the default transport policies seed their decorrelated jitter once per process via `JitterStrategy::decorrelated_process`, so every policy built in one process stays field-equal while separate client processes draw different seeds and decorrelate their retry waves. Governed by [ADR 0041](../adr/0041-transport-policy-l3-layering.md). | Contract | Yes | `crates/core/tests/policy_contract.rs::none_jitter_returns_capped_base_delay_unchanged`, `crates/core/tests/policy_contract.rs::full_jitter_time_seeded_constructor_does_not_panic`, `crates/core/tests/policy_contract.rs::equal_jitter_returns_at_least_half_capped_base_delay`, `crates/core/tests/policy_contract.rs::default_jitter_strategy_is_decorrelated_variant`, `crates/core/tests/policy_contract.rs::decorrelated_process_seeds_once_per_process`, `crates/core/tests/policy_contract.rs::zero_base_delay_returns_zero_across_every_strategy` | 2026-05-14 |
| `PROP-TPP-013` | `cow-sdk-core` | `RetryPolicy` exposes documented `new`, `with_jitter`, `should_retry_status`, `should_retry_network`, and `delay_for_status` decision points; backoff clamps to `max_delay` once the exponent saturates; the `Retry-After` helper honors the header case-insensitively for retry-after-eligible statuses (`429`, `503`) and ignores it otherwise; `RetryPolicyBuilder::max_attempts(0)` is silently promoted to `1`. Governed by [ADR 0041](../adr/0041-transport-policy-l3-layering.md). | Contract | Yes | `crates/core/tests/policy_contract.rs::retry_policy_new_sets_max_attempts_and_keeps_defaults`, `crates/core/tests/policy_contract.rs::with_jitter_replaces_only_the_jitter_field`, `crates/core/tests/policy_contract.rs::should_retry_status_matches_the_public_retryable_list`, `crates/core/tests/policy_contract.rs::should_retry_network_only_retries_documented_kinds`, `crates/core/tests/policy_contract.rs::base_backoff_clamps_to_max_delay_across_attempt_range`, `crates/core/tests/policy_contract.rs::retry_after_helper_is_case_insensitive`, `crates/core/tests/policy_contract.rs::retry_builder_round_trip_and_zero_attempts_clamps_to_one` | 2026-05-14 |

## Rate limiting

| Id | Crate | Property | Type | Covered | Evidence | Last reviewed |
| --- | --- | --- | --- | --- | --- | --- |
| `PROP-TPP-005` | `cow-sdk-core` | Per-host rate limiting keys buckets by `Url::host_str`, so requests to the same host share a bucket while different hosts do not. Governed by [ADR 0041](../adr/0041-transport-policy-l3-layering.md). | Contract | Yes | `crates/core/tests/policy_contract.rs::request_rate_limiter_uses_host_keys_for_per_host_scope` | 2026-05-08 |
| `PROP-TPP-012` | `cow-sdk-core` | `RequestRateLimiter` honors its scope contract (`PerHost` keys by host, `Global` uses a constant key), the `unlimited()` short-circuit never delays or errors, `acquire_global` shares one bucket across calls, pre-cancelled tokens short-circuit without sleeping the limiter interval, and `RequestRateLimiterBuilder` round-trips every setter. Governed by [ADR 0041](../adr/0041-transport-policy-l3-layering.md). | Contract | Yes | `crates/core/tests/policy_contract.rs::unlimited_rate_limiter_never_delays_or_errors`, `crates/core/tests/policy_contract.rs::global_scope_uses_constant_key_regardless_of_host`, `crates/core/tests/policy_contract.rs::acquire_global_shares_one_bucket_across_calls`, `crates/core/tests/policy_contract.rs::pre_cancelled_token_returns_cancelled_immediately`, `crates/core/tests/policy_contract.rs::rate_limiter_builder_round_trip_preserves_setters` | 2026-05-14 |

## Status & error classification

| Id | Crate | Property | Type | Covered | Evidence | Last reviewed |
| --- | --- | --- | --- | --- | --- | --- |
| `PROP-TPP-008` | `cow-sdk-core` | The public retryable status list is exactly `408`, `425`, `429`, `500`, `502`, `503`, and `504`, and `is_retryable_status` matches it. Governed by [ADR 0041](../adr/0041-transport-policy-l3-layering.md). | Contract | Yes | `crates/core/tests/policy_contract.rs::retryable_status_list_is_complete` | 2026-05-08 |
| `PROP-TPP-009` | `cow-sdk-core` | `NetworkErrorKind::from_transport_error_class` is total across every `TransportErrorClass` variant, including the `Redirect` and `Upgrade` variants which fall through the wildcard arm to `NetworkErrorKind::Other`. Governed by [ADR 0041](../adr/0041-transport-policy-l3-layering.md). | Contract | Yes | `crates/core/tests/classify_contract.rs::network_error_kind_mapping_round_trip_is_total` | 2026-05-14 |
| `PROP-TPP-014` | `cow-sdk-core` | `parse_retry_after` accepts delta-seconds and well-formed IMF-fixdate values (every calendar month, leap-year `Feb 29` on years divisible by 4 except non-quadracentury centuries, every consecutive day pair differing by exactly `86_400` seconds for years `1971..=2100`), clamps past or epoch-equal dates to `Duration::ZERO`, and rejects malformed inputs (empty/whitespace, garbage, weekday without comma, non-GMT timezone, trailing tokens, truncated forms, non-numeric components, invalid month names, out-of-range time components, day `0`, day `31` in 30-day months, day `32` universally). Governed by [ADR 0041](../adr/0041-transport-policy-l3-layering.md). | Contract | Yes | `crates/core/tests/retry_after_contract.rs`, `fuzz/fuzz_targets/fuzz_parse_retry_after.rs` | 2026-05-14 |

## Shared retry driver

| Id | Crate | Property | Type | Covered | Evidence | Last reviewed |
| --- | --- | --- | --- | --- | --- | --- |
| `PROP-TPP-007` | `cow-sdk-core` | Target-neutral retry sleep returns within the documented short-delay accuracy bound. Governed by [ADR 0041](../adr/0041-transport-policy-l3-layering.md). | Contract | Yes | `crates/core/tests/policy_contract.rs::cross_target_sleep_accuracy_is_bounded` | 2026-05-08 |
| `PROP-TPP-015` | `cow-sdk-core` | `run_with_retry` is the shared retry driver for the orderbook, subgraph, and IPFS clients: it returns on the first `AttemptOutcome::Success`, retries a retryable status or transport signal with the policy backoff schedule until `max_attempts`, returns the attempt closure's terminal error immediately on a non-retryable signal without re-dispatching, and surfaces the last terminal error on exhaustion. HTTP-date `Retry-After` evaluation reads the target-neutral `system_now` wall clock, so the retry path behaves identically on native and `wasm32` and never aborts a browser runtime. Governed by [ADR 0041](../adr/0041-transport-policy-l3-layering.md). | Contract | Yes | `crates/core/src/transport/policy/runner.rs (tests)`, `crates/js/tests/wasm_retry_runner_contract.rs::system_now_returns_a_wall_clock_value_without_panicking`, `crates/js/tests/wasm_retry_runner_contract.rs::retryable_status_drives_backoff_without_panicking`, `crates/orderbook/tests/request_contract.rs::tracing_contract::execute_with_emits_retry_events_with_status_and_transport_error_fields`, `crates/orderbook/tests/api_contract.rs::service_unavailable_retry_after_header_delays_retry_for_at_least_server_cooldown`, `crates/subgraph/tests/api_contract.rs` | 2026-05-28 |
