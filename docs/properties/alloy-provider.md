---
type: Property
id: alloy-provider
title: "Alloy provider invariants"
description: "The native `cow-sdk-alloy-provider` boundary: RPC provider surface, typed errors, and cancellation propagation (`ProviderError` carries a `Cancelled` variant bridged from `cow_sdk_core::Cancelled`)."
resource: https://github.com/0xSymbiome/cow-rs/blob/main/docs/properties/alloy-provider.md
families: [PROP-AP, PROP-AP-CANCEL]
tags: [property, invariants]
timestamp: 2026-06-29T00:00:00Z
---

# Alloy provider invariants

The native `cow-sdk-alloy-provider` boundary: RPC provider surface, typed errors, and cancellation propagation (`ProviderError` carries a `Cancelled` variant bridged from `cow_sdk_core::Cancelled`). Part of the [Properties Registry](index.md): 15 invariant(s), 14 covered.

## Provider surface & capability boundary

| Id | Crate | Property | Type | Covered | Evidence | Last reviewed |
| --- | --- | --- | --- | --- | --- | --- |
| `PROP-AP-001` | `cow-sdk-alloy-provider` | `RpcAlloyProvider` exposes an SDK-owned documented API; upstream Alloy provider, transport, network, RPC, consensus, and `reqwest` values stay internal except for the doc-hidden sibling-crate seam, which is not subject to semver guarantees and may change in any minor release per ADR 0035. Governed by [ADR 0035](../adr/0035-alloy-provider-adapter.md). | Public API | Partial | `crates/alloy-provider/src/lib.rs`, `crates/alloy-provider/src/provider.rs`, `docs/audit/alloy-adapters-audit.md` | 2026-06-20 |
| `PROP-AP-002` | `cow-sdk-alloy-provider` | `RpcAlloyProvider` implements every method on the `cow-sdk-core::Provider` trait. Governed by [ADR 0035](../adr/0035-alloy-provider-adapter.md). | Contract | Yes | `crates/alloy-provider/tests/provider_contract.rs`, `crates/alloy-provider/src/provider.rs`, `crates/core/src/traits/provider.rs` | 2026-05-12 |
| `PROP-AP-003` | `cow-sdk-alloy-provider` | `RpcAlloyProvider` does not implement `SigningProvider` or `Signer`; compile-fail tests enforce the capability boundary. Governed by [ADR 0035](../adr/0035-alloy-provider-adapter.md). | Contract | Yes | `crates/alloy-provider/tests/compile_fail.rs::negative_impls_and_sealed_markers_hold`, `crates/alloy-provider/tests/trybuild/no_signing_provider.rs`, `crates/alloy-provider/tests/trybuild/no_signer.rs` | 2026-05-27 |
| `PROP-AP-008` | `cow-sdk-alloy-provider` | The default SDK crate set continues to exclude `alloy-provider`, while the provider adapter declares no direct signer-family dependency and the resolved provider graph excludes `alloy-signer-local`. Governed by [ADR 0035](../adr/0035-alloy-provider-adapter.md). | Contract | Yes | `xtask/src/policy/dependency_invariant.rs`, `crates/alloy-provider/tests/dependency_boundary_contract.rs` | 2026-05-06 |

## Read, logs & receipts

| Id | Crate | Property | Type | Covered | Evidence | Last reviewed |
| --- | --- | --- | --- | --- | --- | --- |
| `PROP-AP-007` | `cow-sdk-alloy-provider` | `RpcAlloyProvider::read_contract` ABI-encodes calldata with `alloy-dyn-abi`, dispatches through `eth_call`, decodes the response, returns the JSON value string required by `cow-sdk-core`, and fails malformed inputs without panicking. The canonical algorithm is also exposed through `__seam::execute_read_contract` so sibling adapter crates consume the same implementation without copying it. Governed by [ADR 0035](../adr/0035-alloy-provider-adapter.md). | Contract | Yes | `crates/alloy-provider/tests/provider_contract.rs::read_contract_balance_of_happy_path`, `crates/alloy-provider/tests/read_contract_parity.rs`, `crates/alloy-provider/tests/read_contract_no_panic.rs`, `crates/alloy-provider/tests/seam_contract.rs::read_contract_path_propagates_validation_for_malformed_args_json`, `crates/alloy-provider/src/read_contract.rs`, `crates/alloy-provider/src/lib.rs` | 2026-06-20 |
| `PROP-AP-013` | `cow-sdk-alloy-provider` | `cow_log_query_to_alloy_filter` maps a core `LogQuery` onto the standard `eth_getLogs` filter — the address set, all four topic slots (topic-0 event signature plus indexed-argument slots 1-3, each an any-of set), and the `LogBlockSelector` number-range or block-hash selection — and `alloy_log_to_cow_raw_log` maps an Alloy log onto the full `RawLog` (address, topics-plus-data, reorg `removed` flag, and `LogMeta` block number/hash, optional block timestamp, transaction hash/index, log index). An indexed-address topic is built by `Hash32::from_indexed_address` (left-padded), so an "events for my address" query filters server-side. Governed by [ADR 0057](../adr/0057-log-provider-capability-trait.md). | Contract | Yes | `crates/alloy-provider/src/conversion.rs::tests::cow_log_query_to_alloy_filter_maps_topics_addresses_and_block_hash`, `crates/alloy-provider/src/conversion.rs::tests::alloy_log_to_cow_raw_log_maps_address_meta_and_payload`, `crates/core/src/types/logs.rs::tests::builders_populate_addresses_and_topic_slots`, `crates/core/src/types/logs.rs::tests::from_indexed_address_left_pads_to_a_topic`, `docs/audit/alloy-adapters-audit.md` | 2026-06-20 |
| `PROP-AP-009` | `cow-sdk-alloy-provider` | Alloy provider receipt conversion populates `TransactionReceipt` status, block number, block hash, gas used, sender, and recipient from Alloy receipts, maps EIP-658 success and reverted states without coercing post-state roots, and preserves contract-creation receipts with no recipient. Governed by [ADR 0038](../adr/0038-transaction-lifecycle-types.md). | Contract | Yes | `crates/alloy-provider/tests/provider_contract.rs::get_transaction_receipt_populates_status_block_gas_from_to`, `crates/alloy-provider/src/conversion.rs::tests::alloy_to_cow_receipt_populates_status_success`, `crates/alloy-provider/src/conversion.rs::tests::alloy_to_cow_receipt_populates_status_reverted`, `crates/alloy-provider/src/conversion.rs::tests::alloy_to_cow_receipt_returns_none_status_for_post_state_receipt`, `crates/alloy-provider/src/conversion.rs::tests::alloy_to_cow_receipt_handles_contract_creation_no_to` | 2026-05-07 |

## Construction & builder

| Id | Crate | Property | Type | Covered | Evidence | Last reviewed |
| --- | --- | --- | --- | --- | --- | --- |
| `PROP-AP-004` | `cow-sdk-alloy-provider` | `RpcAlloyProviderBuilder::build` is callable only after HTTP transport has been selected, and externally constructed typestate markers cannot bypass the builder. Governed by [ADR 0035](../adr/0035-alloy-provider-adapter.md). | Contract | Yes | `crates/alloy-provider/src/builder.rs`, `crates/alloy-provider/tests/compile_fail.rs::negative_impls_and_sealed_markers_hold`, `crates/alloy-provider/tests/trybuild/external_marker_construction_fails.rs` | 2026-05-06 |
| `PROP-AP-010` | `cow-sdk-alloy-provider` | `RpcAlloyProviderBuilder` rejects malformed URLs without echoing the input, accepts the timeout setter on both transport-unset and HTTP-selected typestate orderings, and the typed-builder `Debug` impl redacts the configured URL through `Redacted<_>` while surfacing the configured timeout. The `TransportInit` error variant's display routes the underlying detail through `Redacted<String>`. Governed by [ADR 0035](../adr/0035-alloy-provider-adapter.md). | Contract | Yes | `crates/alloy-provider/tests/builder_contract.rs` | 2026-05-14 |
| `PROP-AP-012` | `cow-sdk-alloy-provider` | The inter-adapter `__seam` contract is pinned through behaviour tests over `cow_block_tag_to_alloy` (named tags, hex numbers, decimal numbers, block-hash form, rejection of malformed inputs), `cow_request_to_alloy` (validated minimal request preserves `to`), `rpc_error_to_class_and_detail` (JSON-RPC error envelope classifies as `Remote`), and `execute_read_contract` (canonical read-contract algorithm shared with sibling adapter crates; validation variant propagates end-to-end), plus the `cow_log_query_to_alloy_filter` and `alloy_log_to_cow_raw_log` log conversions the umbrella's `LogProvider` implementation reuses (seam reachability pinned by a compile-time test). The seam also pins the storage-hex format equivalence between `alloy_primitives::B256::from(value).to_string()` and the legacy `format!("0x{:064x}")` width-64 layout. The seam stays `#[doc(hidden)]` and is exercised exclusively by sibling adapter crates per [ADR 0035](../adr/0035-alloy-provider-adapter.md). | Contract | Yes | `crates/alloy-provider/tests/seam_contract.rs`, `crates/alloy-provider/tests/read_contract_parity.rs` | 2026-05-26 |

## Errors, redaction & retry

| Id | Crate | Property | Type | Covered | Evidence | Last reviewed |
| --- | --- | --- | --- | --- | --- | --- |
| `PROP-AP-005` | `cow-sdk-alloy-provider` | Provider URL and transport details are redacted before public formatting; invalid URL errors carry no input detail, provider debug output hides the transport, and transport errors store detail through `Redacted<String>`. Governed by [ADR 0035](../adr/0035-alloy-provider-adapter.md). | Contract | Yes | `crates/alloy-provider/tests/redaction_contract.rs`, `crates/alloy-provider/src/error.rs`, `crates/alloy-provider/src/builder.rs` | 2026-06-20 |
| `PROP-AP-006` | `cow-sdk-alloy-provider` | `ProviderError::class()` returns one of the five `ProviderErrorClass` values for every variant: validation, transport, remote, cancelled, and internal. Governed by [ADR 0035](../adr/0035-alloy-provider-adapter.md). | Contract | Yes | `crates/alloy-provider/tests/redaction_contract.rs::error_class_covers_every_variant`, `crates/alloy-provider/src/error.rs` | 2026-05-06 |
| `PROP-AP-011` | `cow-sdk-alloy-provider` | `ProviderErrorClass::as_str()` returns the stable lowercase label for every variant (`validation`, `transport`, `remote`, `cancelled`, `internal`) with `Display` forwarding through `as_str`; the documented `From<cow_sdk_core::Cancelled>` and `From<cow_sdk_core::CoreError>` lifts propagate cancellation and validation into the provider error without leaking detail; wiremock-driven RPC errors classify per the documented ladder (JSON-RPC error envelopes map to `Remote` with the explicit code, HTTP 5xx and malformed JSON map to non-validation non-cancellation transport classes). Governed by [ADR 0035](../adr/0035-alloy-provider-adapter.md). | Contract | Yes | `crates/alloy-provider/tests/error_class_contract.rs`, `crates/alloy-provider/tests/redaction_contract.rs` | 2026-05-14 |
| `PROP-AP-014` | `cow-sdk-alloy-provider`, `cow-sdk-alloy` | RPC retry is opt-in. By default `RpcAlloyProviderBuilder` (leaf) and `AlloyClientBuilder` (umbrella) issue each request once and surface a transient transport failure to the caller; passing a `RetryConfig` through `retry` wraps the JSON-RPC client in a bounded exponential backoff layer that transparently retries a rate-limited request up to the configured attempt count. The umbrella reuses the leaf's layer constructor through the doc-hidden seam so the policy and its internal compute-units budget stay defined once. Governed by [ADR 0035](../adr/0035-alloy-provider-adapter.md) and [ADR 0010](../adr/0010-runtime-neutral-async-and-transport-posture.md). | Contract | Yes | `crates/alloy-provider/tests/retry_contract.rs::default_provider_does_not_retry_a_rate_limited_read`, `crates/alloy-provider/tests/retry_contract.rs::retry_recovers_from_a_transient_rate_limit`, `crates/alloy-provider/src/retry.rs`, `crates/alloy/src/client.rs`, `docs/audit/alloy-adapters-audit.md` | 2026-06-08 |

## Cancellation propagation

| Id | Crate | Property | Type | Covered | Evidence | Last reviewed |
| --- | --- | --- | --- | --- | --- | --- |
| `PROP-AP-CANCEL-001` | `cow-sdk-alloy-provider` | `ProviderError` carries a `Cancelled` variant and an `impl From<cow_sdk_core::Cancelled>` bridge so `Cancellable::cancel_with(...).await?` propagates cancellation through the provider error type. Governed by [ADR 0035](../adr/0035-alloy-provider-adapter.md). | Contract | Yes | `crates/alloy-provider/src/error.rs`, `crates/alloy-provider/tests/cancellation_contract.rs::cancel_with_propagates_cancelled_through_question_mark` | 2026-05-06 |
