---
type: Property
id: trading
title: "Trading lifecycle invariants"
description: "The `cow-sdk-trading` order lifecycle: quote and order-bounds validation, the fluent swap typestate, `submit_and_wait_for_receipt` composition, and cancellation propagation (`WaitError` carries a `Cancelled` variant bridged from `cow_sdk_core::Cancelled`)."
resource: https://github.com/0xSymbiome/cow-rs/blob/main/docs/properties/trading.md
families: [PROP-TRD, PROP-TRD-CANCEL-WAIT, PROP-TRD-WAIT]
tags: [property, invariants]
timestamp: 2026-06-29T00:00:00Z
---

# Trading lifecycle invariants

The `cow-sdk-trading` order lifecycle: quote and order-bounds validation, the fluent swap typestate, `submit_and_wait_for_receipt` composition, and cancellation propagation (`WaitError` carries a `Cancelled` variant bridged from `cow_sdk_core::Cancelled`). Part of the [Properties Registry](index.md): 23 invariant(s), 23 covered.

## Context & precedence

| Id | Crate | Property | Type | Covered | Evidence | Last reviewed |
| --- | --- | --- | --- | --- | --- | --- |
| `PROP-TRD-001` | `cow-sdk-trading` | Quote and post context precedence remains explicit and deterministic across builder defaults, quote-request overrides, derived quote-to-order parameters, receiver fallback, and collision-driven order-id retries. A quote-request override is applied once, at the trade-parameter level, for every field it shares with the signed order (the token pair, receiver, owner, validity, and balance sources), so the quote preview, the assembled quote request, and the signed order stay mutually consistent and the EthFlow wrapped-native sell token derived from the effective parameters remains authoritative; only the request-shaped fields without a signed-order counterpart (price quality, signing scheme, timeout) are applied to the assembled request directly. | Invariant | Yes | `crates/trading/tests/invariant_contract.rs`, `crates/trading/tests/sdk_contract.rs`, `crates/trading/tests/quote_contract.rs::order_id_collision_retries_with_new_salt_until_success_or_cap`, `crates/trading/tests/quote_contract.rs::eth_flow_request_shape_follows_the_overridden_sell_token`, `crates/trading/tests/quote_contract.rs`, `crates/trading/tests/post_contract.rs`, `crates/trading/tests/order_contract.rs::order_to_sign_receiver_falls_back_to_from_when_zero_or_unset`, `crates/trading/tests/order_contract.rs` | 2026-06-14 |
| `PROP-TRD-019` | `cow-sdk-trading` | A signer that statically knows its chain through [`cow_sdk_core::Signer::chain_id`](../../crates/core/src/traits/signer.rs) must sign for the same chain the trading client posts to: a mismatch fails closed locally with `TradingError::ChainMismatch` before any app-data upload, signing, or submission, the client-side complement to the post-sign owner-recovery gate (a wrong-chain signature would carry the wrong EIP-712 domain separator). The hint is optional — a signer that learns its chain at runtime returns `None` from `chain_id` and the gate is a no-op, so the order posts as before. Governed by [ADR 0015](../adr/0015-client-side-order-bounds-validator.md). | Contract | Yes | `crates/core/src/traits/signer.rs`, `crates/trading/src/post.rs`, `crates/trading/tests/post_contract.rs::chain_mismatch_fast_fails_before_any_upload_signing_or_submission`, `crates/trading/tests/post_contract.rs::matching_signer_chain_passes_through_the_coherence_gate`, `crates/trading/tests/post_contract.rs::signer_without_static_chain_opts_out_of_the_coherence_gate` | 2026-06-11 |

## Fees & slippage

| Id | Crate | Property | Type | Covered | Evidence | Last reviewed |
| --- | --- | --- | --- | --- | --- | --- |
| `PROP-TRD-002` | `cow-sdk-trading` | Slippage outputs, protocol-fee sanitization, and partner-fee extraction remain explicit, monotonic, and clamped across valid inputs. | Invariant | Yes | `crates/trading/tests/invariant_contract.rs`, `crates/trading/tests/slippage_contract.rs::slippage_bps_clamps_to_expected_bounds`, `crates/trading/tests/slippage_contract.rs::slippage_clamps_at_eth_flow_default_for_eth_flow_orders`, `crates/trading/tests/slippage_contract.rs`, `fuzz/fuzz_targets/fuzz_slippage_policy_helpers.rs` | 2026-06-03 |
| `PROP-TRD-005` | `cow-sdk-trading` | `PartnerFee` policy round-trips for every services-supported shape (`Volume`, `Surplus`, `PriceImprovement`, plus `Single` and `Multiple` wrappers) with `u16` bps bounds and `#[non_exhaustive]` at both enum heads; quote-to-post app-data merging preserves partner-fee metadata supplied through advanced settings. | Contract | Yes | `crates/app-data/tests/partner_fee_contract.rs`, `crates/app-data/src/types/partner_fee.rs (tests)`, `crates/trading/tests/app_data_merge_contract.rs::partner_fee_in_advanced_settings_appdata_merges_through_to_post` | 2026-06-20 |
| `PROP-TRD-020` | `cow-sdk-trading` | The posting lanes sign the protocol-fee-composed amounts the quote lane previews. `post_swap_order_from_quote` defaults `protocolFeeBps` from the quote response (sanitized through `sanitize_protocol_fee_bps`) so the posted order signs the amounts the quote projected; an explicit `PostTradeAdditionalParams::protocol_fee_bps` override wins and `Some(0.0)` disables the adjustment, while the direct posting entries apply no default. The partner-fee base is the reconstructed before-protocol-fee amount, so a protocol fee combined with a partner fee strictly lowers the signed buy amount of a sell order (goldens transcribed from the pinned upstream posting test); the `EthFlow` transaction lane threads the same value, and the order `post_swap_order_from_quote` submits matches `QuoteResults.order_to_sign` field-for-field. Governed by [ADR 0058](../adr/0058-typed-quote-request-response-surface.md). | Contract | Yes | `crates/trading/src/post.rs`, `crates/trading/src/onchain.rs`, `crates/trading/src/types/params.rs`, `crates/trading/src/slippage.rs`, `parity/fixtures/trading/protocol_fee_partner_fee_composition.json`, `crates/trading/tests/quote_projection_parity.rs::protocol_fee_partner_fee_composition_matches_upstream_goldens`, `crates/trading/tests/post_contract.rs::swap_posting_defaults_protocol_fee_from_the_quote_response`, `crates/trading/tests/post_contract.rs::swap_posting_protocol_fee_override_supersedes_the_quote_default_and_zero_disables`, `crates/trading/tests/post_contract.rs::post_from_quote_signs_the_order_the_quote_previewed_under_a_protocol_fee`, `crates/trading/tests/onchain_contract.rs::eth_flow_transaction_threads_protocol_fee_into_the_signed_amounts`, `crates/trading/tests/invariant_contract.rs` | 2026-06-12 |

## On-chain builders & gas

| Id | Crate | Property | Type | Covered | Evidence | Last reviewed |
| --- | --- | --- | --- | --- | --- | --- |
| `PROP-TRD-003` | `cow-sdk-trading` | On-chain helper builders preserve unsigned `uint256` calldata boundary semantics, the eth-flow on-chain cancellation projects the order's own receiver (falling back to the owner) and a zero fee amount into the `invalidateOrder` payload matching the upstream `getEthFlowCancellation`, and the quote-amount slippage breakdown rejects protocol-fee values at or above 100% with a typed `InvalidInput` error rather than panicking on the sell path through a `BigInt` divide-by-zero. | Invariant | Yes | `crates/trading/tests/invariant_contract.rs`, `crates/trading/tests/onchain_contract.rs::ethflow_cancellation_projects_order_receiver_and_zero_fee_into_invalidate_calldata`, `crates/trading/tests/onchain_contract.rs`, `crates/trading/src/slippage.rs`, `fuzz/fuzz_targets/fuzz_slippage_amounts.rs` | 2026-06-22 |
| `PROP-TRD-007` | `cow-sdk-trading` | EthFlow and pre-sign transaction gas estimates apply a documented 20% overhead with floor integer rounding (`gas + (gas * 20) / 100`). | Contract | Yes | `crates/trading/src/onchain.rs`, `crates/trading/src/slippage.rs`, `crates/trading/tests/onchain_contract.rs::eth_flow_gas_estimate_applies_documented_floor_overhead`, `crates/trading/tests/onchain_contract.rs::pre_sign_gas_estimate_applies_documented_floor_overhead`, `docs/audit/trading-order-integrity-audit.md` | 2026-05-26 |
| `PROP-TRD-012` | `cow-sdk-trading` | EthFlow native-sell posting and the EthFlow transaction helper accept only `LimitTradeParamsFromQuote` on their public entries. The newtype guarantees a non-`None` `quote_id` by construction, lifting the prior `MissingQuoteId` runtime check on the EthFlow path to the type system at the public boundary. The `TradingError::MissingQuoteId("EthFlow order posting")` diagnostic is preserved for callers that construct from a `LimitTradeParams` value missing a quote id, and the `swap_params_to_limit_order_params` bridge produces the newtype from the pre-quote shape plus an orderbook quote response. Governed by [ADR 0011](../adr/0011-typed-amount-boundary-and-typestate-ready-state-construction.md). | Contract | Yes | `crates/trading/tests/limit_from_quote_contract.rs`, `crates/trading/tests/post_contract.rs::native_sell_posting_requires_quote_id_before_signing_or_submission`, `crates/trading/src/types/params.rs`, `crates/trading/src/order.rs`, `crates/trading/src/post.rs`, `crates/trading/src/onchain.rs`, `docs/guides/getting-started.md` | 2026-05-27 |

## Validation & bounds

| Id | Crate | Property | Type | Covered | Evidence | Last reviewed |
| --- | --- | --- | --- | --- | --- | --- |
| `PROP-TRD-004` | `cow-sdk-trading` | Client-side submission validator rejects the stable services-authority preconditions it can check without the operator's deployment configuration (expired validTo, zero amount, buy-side same-token, non-EthFlow native sentinel, zero-address from, metadata.signer mismatch, owner mismatch) before wire dispatch, while its public documentation frames the check as defence-in-depth rather than a services-acceptance guarantee and leaves the exact, operator-tunable order-validity window to services. The owner-mismatch precondition is enforced both as a pre-sign fast-fail against the signer's self-reported address and as an authoritative post-sign recovery of the signer from the produced ECDSA signature (`Eip712`/`EthSign`), the client-side mirror of the services `WrongOwner` submission check; a signer that reports one address but signs with another key passes the former and fails the latter with `ClientRejection::OwnerMismatch`. Governed by [ADR 0015](../adr/0015-client-side-order-bounds-validator.md). | Contract | Yes | `crates/trading/tests/validation_contract.rs`, `crates/trading/tests/property_contract.rs`, `crates/trading/src/validation.rs`, `crates/trading/src/post.rs`, `crates/trading/tests/post_contract.rs::post_swap_order_appdata_from_mismatch_does_not_upload_or_sign`, `crates/trading/tests/post_contract.rs::post_swap_order_same_buy_sell_token_does_not_upload_or_sign`, `crates/trading/tests/post_contract.rs::post_swap_order_zero_amount_does_not_upload_or_sign`, `crates/trading/tests/post_contract.rs::post_sign_recovery_rejects_a_signer_that_signs_with_a_different_key`, `crates/trading/tests/post_contract.rs::recoverable_limit_posting_rejects_owner_signer_mismatch_before_upload_or_submission`, `docs/audit/trading-order-integrity-audit.md` | 2026-06-11 |
| `PROP-TRD-008` | `cow-sdk-trading` | `fuzz_order_bounds_validator` carries a documented seed-corpus contract anchored to `validation_contract.rs` happy path plus boundary-shaped inputs covering every `ClientRejection` variant and the documented `now`/`valid_to` extremes. | Property | Yes | `fuzz/fuzz_targets/fuzz_order_bounds_validator.rs`, `crates/trading/tests/property_contract.rs`, `docs/audit/trading-order-integrity-audit.md` | 2026-06-07 |
| `PROP-TRD-009` | `cow-sdk-trading` | The order-bounds validator and the public `TradeParams` and `LimitTradeParams` validators mirror services' `AllowSell` same-token policy (the reviewed production configuration): sell-side same-token orders are accepted, buy-side same-token orders reject with `ClientRejection::SameBuyAndSellToken`, and the order-level WETH-paired-with-native-sentinel guard applies that same buy-only split. The SDK does not track the operator-configurable `Disallow` and `Allow` policies. | Contract | Yes | `crates/trading/src/validation.rs`, `crates/trading/src/params.rs`, `crates/trading/tests/validation_contract.rs::validate_same_token_matches_services_allow_sell_policy`, `crates/trading/tests/parameters_contract.rs::tradeparameters_validate_mirrors_services_allow_sell`, `crates/trading/tests/parameters_contract.rs::limittradeparameters_validate_mirrors_services_allow_sell`, `crates/trading/tests/post_contract.rs::post_swap_order_same_buy_sell_token_does_not_upload_or_sign`, `crates/trading/tests/post_contract.rs::post_swap_order_sell_side_same_buy_sell_token_uploads_signs_and_submits` | 2026-06-07 |
| `PROP-TRD-010` | `cow-sdk-core` | `AppCode` validates application identifiers at the typed boundary by accepting source-backed examples and rejecting only empty strings, NUL bytes, and ASCII control characters. The type lives in `cow-sdk-core` alongside the other identity primitives (`Address`, `AppDataHash`, `OrderUid`) and is the sole source of truth for application-identifier construction across the workspace; `TradingBuilder` terminals surface validation failures as `TradingError::AppCode`. | Contract | Yes | `crates/trading/tests/app_code_contract.rs`, `crates/core/src/types/app_code.rs`, `crates/trading/src/client/builder.rs` | 2026-06-07 |
| `PROP-TRD-015` | `cow-sdk-trading` | `LimitTradeParamsFromQuote` is a public newtype around `LimitTradeParams` that guarantees a non-`None` `quote_id` by construction. `try_from_limit` returns `TradingError::MissingQuoteId("EthFlow order posting")` on `quote_id = None`; the canonical bridge `swap_params_to_limit_order_params` produces the newtype only when an orderbook quote response is supplied. The EthFlow native-currency submission entry `post_sell_native_currency_order` and the EthFlow transaction helper `eth_flow_transaction` accept only `LimitTradeParamsFromQuote` on their public entries. `quote_id()` returns `i64` without an `Option`. `Serialize`/`Deserialize` flatten the inner shape so the wire form is byte-equal to the underlying limit-trade payload while the type-level guarantee is preserved on the Rust API. Governed by [ADR 0011](../adr/0011-typed-amount-boundary-and-typestate-ready-state-construction.md) and [ADR 0059](../adr/0059-hash-concrete-orderdata-directly.md). | Contract | Yes | `crates/trading/tests/limit_from_quote_contract.rs`, `crates/trading/src/types/params.rs`, `crates/trading/src/order.rs`, `crates/trading/src/post.rs`, `crates/trading/src/onchain.rs` | 2026-05-27 |

## Swap & limit lifecycle

| Id | Crate | Property | Type | Covered | Evidence | Last reviewed |
| --- | --- | --- | --- | --- | --- | --- |
| `PROP-TRD-006` | `cow-sdk-trading` | `TradingBuilder::build()` is target-neutral: it succeeds without an injected orderbook client on native and `wasm32` alike (the lazy default orderbook factory rides the orderbook builder's per-target default transport), and an injected client keeps working on both targets. Governed by [ADR 0011](../adr/0011-typed-amount-boundary-and-typestate-ready-state-construction.md) and [ADR 0013](../adr/0013-http-transport-injection-and-typestate-builders.md). | Contract | Yes | `crates/trading/tests/sdk_contract.rs::build_succeeds_on_wasm32_without_injected_orderbook_client`, `crates/trading/tests/sdk_contract.rs::build_succeeds_on_wasm32_with_injected_orderbook_client`, `crates/trading/tests/sdk_contract.rs::build_succeeds_on_native_without_injected_orderbook_client`, `.github/workflows/wasm.yml` | 2026-06-12 |
| `PROP-TRD-013` | `cow-sdk-trading` | Every documented constructor, `with_*` builder, accessor, trait impl, and serde round-trip on the trading-crate typed-parameter bundles is pinned at the public boundary: `SlippageToleranceRequest` and `SlippageToleranceResponse` builders preserve every optional field; `TraderParams`, `QuoterParams`, and `OrderTraderParams` `new` and `with_*` setters preserve chain, env, app-code, and contract-override inputs and surface `AppCodeError` on invalid app codes; the trading-crate builder does not expose `with_owner`; `TradeParams` and `LimitTradeParams` `new` seed documented defaults (`SellTokenSource::Erc20`, `BuyTokenDestination::Erc20`, no partial fills, no slippage, no validity overrides) and the ten `with_*` setters shared between the two structs are factored through one internal definition that emits identical inherent methods on each public type; `LimitTradeParams::with_quote_id` remains an inherent method on the limit type; `PostTradeAdditionalParams` and `TradeAdvancedSettings` builders round-trip and their `Debug` impls reflect presence flags without leaking trait-object internals. | Contract | Yes | `crates/trading/tests/types_contract.rs` | 2026-06-14 |
| `PROP-TRD-017` | `cow-sdk-trading` | The fluent swap lifecycle builder (`Trading::swap`) is an additive façade over the flat post path: its `quote`-then-`submit` and one-call `execute` terminals submit the same order `post_swap_order` does, the named sell-token/buy-token/amount setters make token transposition unrepresentable, and the `execute`/`quote` terminals are reachable only once all three required markers are set. Governed by [ADR 0011](../adr/0011-typed-amount-boundary-and-typestate-ready-state-construction.md). | Contract | Yes | `crates/trading/src/client/swap.rs`, `crates/trading/tests/swap_lifecycle_contract.rs::swap_builder_quote_then_submit_matches_flat_post_swap_order`, `crates/trading/tests/swap_lifecycle_contract.rs::swap_builder_execute_is_one_call_and_transposition_safe` | 2026-06-09 |
| `PROP-TRD-018` | `cow-sdk-trading` | The swap lifecycle drives any asynchronous [`Signer`](../../crates/core/src/traits/signer.rs) implementation — local key, remote signer, browser EIP-1193 wallet, or smart account — through one uniform chain, and injects the orderbook client on the builder either by value (`orderbook`) or as a shared `Arc<dyn OrderbookClient>` (`orderbook_shared`) with equivalent effect. Governed by [ADR 0011](../adr/0011-typed-amount-boundary-and-typestate-ready-state-construction.md) and [ADR 0010](../adr/0010-runtime-neutral-async-and-transport-posture.md). | Contract | Yes | `crates/trading/src/client/swap.rs`, `crates/trading/src/client/builder.rs`, `crates/trading/tests/swap_lifecycle_contract.rs::swap_builder_injects_orderbook_without_arc_and_drives_async_signer` | 2026-06-14 |
| `PROP-TRD-021` | `cow-sdk-trading` | The fluent limit-order lifecycle builder (`Trading::limit`) is an additive façade over the flat post path: its `post` terminal submits the same order `post_limit_order` does, its `post_presign` terminal posts under the pre-sign scheme without a signer, the named sell-token/buy-token/sell-amount/buy-amount setters make token and amount transposition unrepresentable, and the terminals are reachable only once all four required markers are set. `SwapBuilder` and `LimitBuilder` are distinct typestate builders that share their optional setters through one internal definition (`impl_common_order_builder_setters!`), mirroring the parameter-struct setter sharing. Governed by [ADR 0069](../adr/0069-layered-trading-operation-surface-and-signing-free-transport.md) and [ADR 0011](../adr/0011-typed-amount-boundary-and-typestate-ready-state-construction.md). | Contract | Yes | `crates/trading/src/client/limit.rs`, `crates/trading/src/client/mod.rs`, `crates/trading/tests/limit_lifecycle_contract.rs::limit_builder_post_matches_flat_post_limit_order`, `crates/trading/tests/limit_lifecycle_contract.rs::limit_builder_post_presign_posts_without_a_signer_through_the_builder` | 2026-06-14 |
| `PROP-TRD-022` | `cow-sdk-trading` | `build_limit_order_to_sign` exposes the unsigned limit order at a boundary so a smart-account EIP-1271 contract signature can be resolved against it before the order enters the native placement path, and the order it builds is byte-identical to the order `post_limit_order` signs for the same inputs: same limit defaults, `apply_costs_slippage_and_fees` projection, app-data class, and `order_to_sign` parameters, with `owner` injected as `from` and the receiver defaulting to the owner exactly as the posting path resolves it. The returned app-data hash equals the posted order's `app_data`. The two paths share their build steps, so they agree by construction; the parity guard fails closed if a future change forks one path from the other, which would leave the wallet signing a digest the posted order does not carry. The EIP-1271 placement arm posts that boundary order field-for-field under the EIP-1271 scheme. Governed by [ADR 0073](../adr/0073-order-authorization-and-bundled-presign-activation.md) and [ADR 0050](../adr/0050-eip1271-signature-blob-encoding.md). | Contract | Yes | `crates/trading/src/placement.rs`, `crates/trading/src/post.rs`, `crates/trading/tests/limit_eip1271_parity.rs::boundary_builder_digest_equals_native_post_digest_sell`, `crates/trading/tests/limit_eip1271_parity.rs::boundary_builder_digest_equals_native_post_digest_buy`, `crates/trading/tests/limit_eip1271_parity.rs::boundary_builder_digest_equals_native_post_digest_with_advanced_settings`, `crates/trading/tests/limit_eip1271_parity.rs::boundary_builder_injects_owner_as_from_and_receiver_default`, `crates/trading/tests/safe_placement_contract.rs::place_limit_eip1271_posts_the_boundary_builder_order` | 2026-06-30 |
| `PROP-TRD-023` | `cow-sdk-trading` | `place_swap` and `place_limit` carry the authorization mode as one `Authorization` value and return the typed `OrderPlacement` sum: the `Ecdsa` and `Eip1271` arms resolve to `Live { order_uid }`, while `PreSign` resolves to `PendingActivation { order_uid, activation }`. The scheme statically selects the arm, so a posted pre-sign order's UID is reachable only by matching the arm that also yields its `SafeActivation`, and the on-chain step cannot be silently dropped. The `PreSign` `activation` built by `build_presign_activation` is the ordered `[approve, setPreSignature]` pair: the first call grants the vault relayer the sell-token allowance and the second flips the settlement `setPreSignature(uid, true)` flag, each a zero-value `UnsignedTransaction` for one smart-account batch. The builder is pure — it reads no on-chain allowance and always emits both legs — and the bundled activation equals the standalone builder for the same UID, sell token, and signed sell amount. The placement path enforces no new order invariants (`from == owner`, the zero fee amount, the empty pre-sign signature, app-data hashing, and order-UID derivation are owned by the dispatch it reuses; see PROP-TRD-004 and PROP-CON-019). Governed by [ADR 0073](../adr/0073-order-authorization-and-bundled-presign-activation.md) and [ADR 0070](../adr/0070-onchain-transaction-helper-boundary.md). | Contract | Yes | `crates/trading/src/placement.rs`, `crates/trading/tests/safe_placement_contract.rs::place_limit_presign_returns_pending_activation_with_ordered_calls`, `crates/trading/tests/safe_placement_contract.rs::place_limit_eip1271_is_live_with_provider_blob_and_scheme`, `crates/trading/tests/safe_placement_contract.rs::place_swap_presign_matches_standalone_activation`, `crates/trading/tests/safe_placement_contract.rs::build_presign_activation_is_pure_and_targets_relayer_then_settlement`, `crates/trading/tests/safe_placement_contract.rs::free_function_presign_entries_stay_available` | 2026-06-30 |

## Receipt & wait-for-receipt

| Id | Crate | Property | Type | Covered | Evidence | Last reviewed |
| --- | --- | --- | --- | --- | --- | --- |
| `PROP-TRD-016` | `cow-sdk-trading` | `submit_and_wait_for_receipt` and `poll_for_receipt` emit a separated transaction-lifecycle telemetry pair when the `tracing` feature is enabled: the `transaction.submit` span records only the broadcast `tx_hash` and never a mined field, so a submission span never implies inclusion or execution success; the `transaction.receipt` span records `tx_status`, `block_number`, and `gas_used` only after a provider receipt is observed, and records no signature material, calldata, sender, or recipient. `poll_for_receipt`, which never broadcasts, carries only the receipt span. Governed by [ADR 0038](../adr/0038-transaction-lifecycle-types.md). | Contract | Yes | `crates/trading/tests/wait_telemetry_contract.rs::submit_and_wait_emits_separated_submit_and_receipt_spans`, `crates/trading/tests/wait_telemetry_contract.rs::poll_for_receipt_emits_only_the_receipt_span`, `crates/trading/src/wait.rs`, `docs/guides/observability.md` | 2026-06-08 |
| `PROP-TRD-WAIT-001` | `cow-sdk-trading` | `submit_and_wait_for_receipt` composes signer broadcast with provider receipt polling and returns the rich `TransactionReceipt` once observed, while `poll_for_receipt` supports hash-first workflows. `WaitOptions` controls poll interval, timeout, and revert handling; `WaitError` covers broadcast, lookup, timeout, reverted, and cancelled outcomes, and `WaitError::reverted()` returns the reverted receipt for the on-chain submission verdict (and `None` for the transient and environmental variants). Governed by [ADR 0038](../adr/0038-transaction-lifecycle-types.md) and [ADR 0010](../adr/0010-runtime-neutral-async-and-transport-posture.md). | Contract | Yes | `crates/trading/tests/wait_helper_contract.rs::submit_and_wait_returns_receipt_when_provider_yields_success`, `crates/trading/tests/wait_helper_contract.rs::submit_and_wait_returns_reverted_when_require_success_and_status_reverted`, `crates/trading/tests/wait_helper_contract.rs::submit_and_wait_returns_receipt_when_require_success_false_and_status_reverted`, `crates/trading/tests/wait_helper_contract.rs::submit_and_wait_returns_timeout_when_provider_never_yields_receipt`, `crates/trading/tests/wait_helper_contract.rs::submit_and_wait_propagates_signer_error_as_broadcast_variant`, `crates/trading/tests/wait_helper_contract.rs::submit_and_wait_propagates_provider_error_as_lookup_variant`, `crates/trading/tests/wait_helper_contract.rs::poll_for_receipt_returns_receipt_when_already_available`, `crates/trading/tests/wait_helper_contract.rs::cancellation_through_cancellable_propagates_through_helper`, `crates/trading/tests/wait_helper_contract.rs::reverted_accessor_reports_the_on_chain_revert_verdict` | 2026-06-08 |
| `PROP-TRD-CANCEL-WAIT-001` | `cow-sdk-trading` | `WaitError` carries `Cancelled(Cancelled)` and implements `From<cow_sdk_core::Cancelled>` so `Cancellable::cancel_with(submit_and_wait_for_receipt(...)).await?` propagates cooperative cancellation through the helper without erasing typed signer or provider failures. Governed by [ADR 0038](../adr/0038-transaction-lifecycle-types.md) and [ADR 0010](../adr/0010-runtime-neutral-async-and-transport-posture.md). | Contract | Yes | `crates/trading/tests/wait_helper_contract.rs::cancellation_through_cancellable_propagates_through_helper` | 2026-05-07 |
