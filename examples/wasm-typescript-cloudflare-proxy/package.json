{
  "name": "cow-sdk-wasm-example-cloudflare-proxy",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@9.15.0",
  "engines": {
    "node": ">=22.0.0"
  },
  "scripts": {
    "build": "node scripts/build.mjs && wrangler deploy --dry-run --outdir dist-worker-upload",
    "test": "tsc --noEmit && pnpm run build && vitest run --config vitest.config.ts"
  },
  "dependencies": {
    "cow-sdk-wasm-local": "file:../../crates/wasm/npm"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "0.16.3",
    "@cloudflare/workers-types": "4.20260507.1",
    "@types/node": "25.6.2",
    "@types/ws": "8.18.1",
    "esbuild": "0.27.2",
    "typescript": "6.0.3",
    "vitest": "4.1.5",
    "wrangler": "4.90.0"
  }
}
