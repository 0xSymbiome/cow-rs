# Properties Registry

This registry is the canonical public index of invariants and state contracts
for `cow-rs`.

Use it with:

- [Verification Guide](docs/verification-guide.md) for how the evidence is
  interpreted
- [Verification Matrix](docs/verification-matrix.md) for the crate and
  workflow lanes that exercise each surface

Executable coverage stays with the crate or browser surface that owns the
behavior. This registry records what must remain true, who owns it, and where
the current evidence lives.

`Covered` uses these values:

- `Yes`: dedicated executable coverage exists
- `Partial`: deterministic coverage exists, but not through a dedicated
  property or state-machine suite
- `No`: the property is registered, but no executable coverage is attached yet

`Last reviewed` records the most recent date the row was confirmed against the
shipped code. The registry follows a 90-day re-review rhythm that mirrors the
dependency-exception policy in `.github/config/deny.toml`: every row is
re-confirmed at least once per 90-day window, and the date here is refreshed
in the same change that touches the owning surface.

## Methodology

The `Type` column uses five labels. Two of them carry distinct evidence
methodologies that reviewers should read with care:

- `Property` rows are backed by a real `proptest!` macro harness with
  committed `tests/proptest-regressions/` seed files. Coverage comes from
  randomized input generation with shrinking to minimal counterexamples
  on failure, and the committed regression seeds preserve failure
  reproducibility across contributors. The codec crates
  (`cow-sdk-core`, `cow-sdk-contracts`, `cow-sdk-signing`,
  `cow-sdk-app-data`) carry their property rows through this lane.
- `Invariant` rows are backed by a curated deterministic boundary sweep:
  a reproducible 128-case iterator over hand-picked boundary cases plus
  named narrow-search profiles that extend the curated set without
  introducing randomness. Coverage is exhaustive over the curated case
  set and is reproducible by design, but it does not discover unexpected
  inputs or shrink to minimal counterexamples. The orchestration crates
  (`cow-sdk-orderbook`, `cow-sdk-trading`, `cow-sdk-subgraph`) carry
  their invariant rows through this lane because the reviewed
  invariants are transport and builder shape rather than roundtrip
  properties.

The remaining three labels — `Contract`, `State machine`, and `Public
API` — cover typed API and parser contracts exercised through targeted
regression tests, browser-wallet session and chain state-machine
transitions covered through deterministic transports and committed
browser automation, and the curated `cow-sdk` facade exported-symbol
snapshot respectively.

| Id | Crate | Property | Type | Covered | Evidence | Last reviewed |
| --- | --- | --- | --- | --- | --- | --- |
| `PROP-CORE-001` | `cow-sdk-core` | Unsupported chain and environment resolution stays explicit and typed instead of falling back silently. | Contract | Yes | `crates/core/tests/config_contract.rs`, `crates/core/tests/types_contract.rs` | 2026-04-17 |
| `PROP-CORE-002` | `cow-sdk-core` | Runtime traits preserve typed transaction, hash, and provider boundaries across sync and async implementations. | Contract | Yes | `crates/core/tests/traits_contract.rs`, `crates/core/tests/types_contract.rs` | 2026-04-17 |
| `PROP-CORE-003` | `cow-sdk-core` | Validated hex wrappers (`Address`, `Hash32`, `AppDataHex`, `OrderUid`, `HexData`) preserve input case and roundtrip through their string forms while failing closed on length, prefix, and character errors. | Property | Yes | `crates/core/tests/property_contract.rs`, `crates/core/tests/types_contract.rs` | 2026-04-21 |
| `PROP-CORE-004` | `cow-sdk-core` | `Amount` canonicalizes decimal and hexadecimal inputs into equal values, rejects negative and out-of-range inputs, and normalizes zero forms deterministically. | Property | Yes | `crates/core/tests/property_contract.rs` | 2026-04-21 |
| `PROP-CORE-005` | `cow-sdk-core` | `Address` case normalization keeps `addresses_equal` case-insensitive while `token_id` remains deterministic and sensitive to both chain id and address changes. | Property | Yes | `crates/core/tests/property_contract.rs` | 2026-04-21 |
| `PROP-CORE-006` | `cow-sdk-core` | `SupportedChainId` roundtrips through its numeric `ChainId` form for every supported variant and fails closed on unsupported chain ids. | Property | Yes | `crates/core/tests/property_contract.rs`, `crates/core/tests/config_contract.rs` | 2026-04-17 |
| `PROP-CON-001` | `cow-sdk-contracts` | Order hashing remains deterministic across semantically equivalent normalized inputs. | Property | Yes | `crates/contracts/tests/property_contract.rs`, `crates/contracts/tests/order_contract.rs` | 2026-04-21 |
| `PROP-CON-002` | `cow-sdk-contracts` | ABI helper builders, compact flag codecs, and signature payload codecs preserve explicit boundary semantics for settlement, swap, vault, reader, interaction, signature, and deployment payloads. | Property | Yes | `crates/contracts/tests/property_contract.rs` (includes `abi_layout_narrow_search_profile_preserves_eip1271_payload_boundaries`), `crates/contracts/tests/property_contract.rs::decode_trade_flags_accepts_0b00_and_0b01_as_erc20`, `crates/contracts/tests/property_contract.rs::decode_order_rejects_out_of_bounds_token_indices`, `crates/contracts/tests/settlement_contract.rs`, `crates/contracts/tests/swap_contract.rs`, `crates/contracts/tests/signature_contract.rs`, `crates/contracts/tests/vault_contract.rs`, `crates/contracts/tests/reader_contract.rs`, `crates/contracts/tests/interaction_contract.rs`, `crates/contracts/src/primitives.rs (tests)`, `crates/contracts/src/order.rs (tests)`, `crates/contracts/src/settlement.rs (tests)`, `crates/contracts/src/deploy.rs (tests)` | 2026-05-01 |
| `PROP-SIG-001` | `cow-sdk-signing` | Domain separation changes only when the typed-data domain changes. | Property | Yes | `crates/signing/tests/property_contract.rs`, `crates/signing/tests/domain_contract.rs` | 2026-04-17 |
| `PROP-SIG-002` | `cow-sdk-signing` | Order and cancellation typed-data payloads, generated ids, and EIP-1271 helper payloads stay deterministic for equivalent inputs and explicit signing-scheme boundaries. | Property | Yes | `crates/signing/tests/property_contract.rs`, `crates/signing/tests/order_signing_contract.rs`, `crates/signing/tests/order_signing_contract.rs::eth_sign_routes_raw_32_byte_digest_to_sign_message`, `crates/signing/tests/cancellation_contract.rs`, `crates/signing/tests/eip1271_contract.rs` | 2026-05-01 |
| `PROP-APP-001` | `cow-sdk-app-data` | CID conversion round-trips between digest and CID forms without silent mutation. | Property | Yes | `crates/app-data/tests/property_contract.rs`, `crates/app-data/tests/cid_contract.rs`, `crates/app-data/src/cid.rs (tests)` | 2026-04-17 |
| `PROP-APP-002` | `cow-sdk-app-data` | Invalid app-data, schema, fetch, info, and pinning inputs fail closed. | Property | Yes | `crates/app-data/tests/property_contract.rs` (includes `schema_parsing_narrow_search_profile_roundtrips_valid_triplets_and_rejects_invalid_forms`), `crates/app-data/tests/schema_contract.rs`, `crates/app-data/tests/fetch_contract.rs`, `crates/app-data/tests/pinning_contract.rs`, `crates/app-data/src/info.rs (tests)`, `crates/app-data/src/types.rs (tests)` | 2026-04-21 |
| `PROP-APP-003` | `cow-sdk-app-data` | Deterministic document sources render canonical JSON and stable latest-path digests for equivalent document shapes. | Property | Yes | `crates/app-data/tests/property_contract.rs` (includes `canonicalization_narrow_search_profile_preserves_equivalent_nested_documents`), `crates/app-data/tests/app_data_info_contract.rs`, `crates/app-data/src/info.rs (tests)` | 2026-04-21 |
| `PROP-ORD-001` | `cow-sdk-orderbook` | Request builders preserve explicit field shape, pagination defaults, and `appData` transport without silently coercing unsupported inputs. | Invariant | Yes | `crates/orderbook/tests/invariant_contract.rs`, `crates/orderbook/tests/request_contract.rs`, `crates/orderbook/tests/api_contract.rs`, `crates/orderbook/tests/types_contract.rs` | 2026-04-21 |
| `PROP-ORD-002` | `cow-sdk-orderbook` | Response decoding, retry termination, `Retry-After` cooldown honor on `429` and `503`, and transform layers fail closed on malformed upstream payloads. | Invariant | Yes | `crates/orderbook/tests/invariant_contract.rs`, `crates/orderbook/tests/transform_contract.rs`, `crates/orderbook/tests/types_contract.rs`, `crates/orderbook/tests/request_contract.rs`, `crates/orderbook/tests/api_contract.rs::service_unavailable_retry_after_header_delays_retry_for_at_least_server_cooldown`, `crates/orderbook/src/request.rs` (`tests::parse_retry_after_covers_documented_boundary_matrix`) | 2026-04-24 |
| `PROP-TRD-001` | `cow-sdk-trading` | Quote and post context precedence remains explicit and deterministic across builder defaults, quote-request overrides, derived quote-to-order parameters, and collision-driven order-id retries. | Invariant | Yes | `crates/trading/tests/invariant_contract.rs`, `crates/trading/tests/sdk_contract.rs`, `crates/trading/tests/quote_contract.rs`, `crates/trading/tests/post_contract.rs`, `crates/trading/tests/order_contract.rs` | 2026-04-21 |
| `PROP-TRD-002` | `cow-sdk-trading` | Slippage outputs, protocol-fee sanitization, and partner-fee extraction remain explicit, monotonic, and clamped across valid inputs. | Invariant | Yes | `crates/trading/tests/invariant_contract.rs`, `crates/trading/tests/slippage_contract.rs` | 2026-04-17 |
| `PROP-TRD-003` | `cow-sdk-trading` | On-chain helper builders preserve unsigned `uint256` calldata boundary semantics. | Invariant | Yes | `crates/trading/tests/invariant_contract.rs`, `crates/trading/tests/onchain_contract.rs` | 2026-04-17 |
| `PROP-SBG-001` | `cow-sdk-subgraph` | Query requests preserve explicit operation-name handling plus nested variable object and array shape. | Invariant | Yes | `crates/subgraph/tests/invariant_contract.rs` (includes `raw_request_narrow_search_profile_preserves_explicit_documents_and_nested_variables`), `crates/subgraph/tests/query_contract.rs`, `crates/subgraph/tests/api_contract.rs` | 2026-04-17 |
| `PROP-SBG-002` | `cow-sdk-subgraph` | Typed response decoding accepts equivalent string-or-number scalar forms and fails closed on malformed or missing data. | Invariant | Yes | `crates/subgraph/tests/invariant_contract.rs` (includes `scalar_decode_narrow_search_profile_covers_boundary_numeric_forms`), `crates/subgraph/tests/types_contract.rs`, `crates/subgraph/tests/api_contract.rs` | 2026-04-17 |
| `PROP-BWL-001` | `cow-sdk-browser-wallet` | Ambiguous discovery never silently auto-selects a provider. | State machine | Yes | `crates/browser-wallet/src/wallet.rs`, `crates/browser-wallet/tests/wallet_contract.rs` | 2026-04-17 |
| `PROP-BWL-002` | `cow-sdk-browser-wallet` | Session, chain, typed-data, and typed RPC classification boundaries stay explicit under deterministic transports and committed browser automation. | State machine | Yes | `crates/browser-wallet/tests/state_machine_contract.rs`, `crates/browser-wallet/tests/provider_contract.rs`, `crates/browser-wallet/tests/wallet_contract.rs`, `crates/browser-wallet/src/provider.rs (tests)`, `crates/browser-wallet/src/error.rs (tests)`, `e2e/browser-wallet/tests/browser-wallet-console.spec.ts` | 2026-04-17 |
| `PROP-SDK-001` | `cow-sdk` | The facade remains curated and feature-gated, without widening the default surface beyond leaf-crate ownership. | Public API | Yes | `crates/sdk/tests/public_api.rs` | 2026-04-24 |
| `PROP-CORE-007` | `cow-sdk-core` | `HttpTransport` is dyn-compatible, typed failures flow through `TransportError` with a `TransportErrorClass` partition, every default adapter strips the URL before wrapping so credential-bearing query strings never surface through `Debug` or `Display`, and `sanitize_public_base_url` strips path, query, fragment, and credentials before URL identity crosses a diagnostic or tracing boundary. Governed by [ADR 0013](docs/adr/0013-http-transport-injection-and-typestate-builders.md). | Contract | Yes | `crates/core/tests/transport_contract.rs::sanitize_public_base_url_strips_private_url_parts`, `crates/core/tests/transport_contract.rs::sanitize_public_base_url_strips_userinfo_credentials`, `crates/core/tests/transport_contract.rs`, `crates/transport-wasm/tests/parity_contract.rs` | 2026-04-25 |
| `PROP-CON-003` | `cow-sdk-contracts` | Every ABI binding is generated through `alloy::sol!` from committed upstream Solidity excerpts and preserves byte-identity on the encoded call-data and hashed payloads of `GPv2Settlement`, `GPv2VaultRelayer`, `CoWSwapEthFlow`, the EIP-1967 proxy, and `IERC20` / `IERC20Permit`. Governed by [ADR 0012](docs/adr/0012-alloy-sol-bindings-and-registry-authority.md). | Contract | Yes | `crates/contracts/tests/parity_contract.rs` | 2026-04-22 |
| `PROP-CON-004` | `cow-sdk-contracts` | `Registry` resolves every deployed contract address from the typed `(ContractId, SupportedChainId, CowEnv)` triple; the embedded TOML manifest is validated at compile time and the runtime parser surfaces every failure class as a typed `RegistryError`. Governed by [ADR 0012](docs/adr/0012-alloy-sol-bindings-and-registry-authority.md). | Contract | Yes | `crates/contracts/tests/registry.rs`, `crates/contracts/tests/build_rs_compile_fail.rs` | 2026-04-21 |
| `PROP-CON-005` | `cow-sdk-contracts` | Every ECDSA signature returned by `cow_sdk_contracts::normalized_ecdsa_signature` carries a canonical recovery byte in the legacy `{27, 28}` range. Inputs with `v ∈ {0, 1}` are canonicalized; any other byte is rejected with `ContractsError::InvalidSignatureRecoveryByte`. Governed by [ADR 0022](docs/adr/0022-ecdsa-signature-v-normalization.md). | Contract | Yes | `crates/contracts/tests/signature_contract.rs`, `crates/contracts/tests/property_contract.rs::ecdsa_v_normalization_rejects_every_excluded_byte_value`, `parity/fixtures/signing.json`, `fuzz/fuzz_targets/fuzz_ecdsa_v_normalization.rs`, `fuzz/corpus/fuzz_ecdsa_v_normalization/` | 2026-05-01 |
| `PROP-CON-006` | `cow-sdk-contracts` | Contracts-crate digest computation flows only through the canonical `UnsignedOrder` to `Order` path; the legacy compatibility shims that zeroed digest inputs are removed from the public surface. Governed by [ADR 0023](docs/adr/0023-legacy-compatibility-shim-removal.md). | Contract | Yes | `crates/contracts/tests/order_contract.rs`, `crates/contracts/src/order.rs` (`compile_fail` doctest) | 2026-04-24 |
| `PROP-CON-007` | `cow-sdk-contracts` | The order UID pack/unpack fuzz target ships with a non-empty corpus seeded from parity fixture data plus edge-shaped 56-byte triples, so scheduled fuzzing starts from known codec boundaries instead of random-only inputs. | Contract | Yes | `fuzz/fuzz_targets/fuzz_order_uid_pack_unpack.rs`, `fuzz/corpus/fuzz_order_uid_pack_unpack/`, `parity/fixtures/contracts.json` | 2026-04-24 |
| `PROP-SIG-003` | `cow-sdk-signing` | `verify_eip1271_signature_async` takes a mandatory `Eip1271VerificationCache` parameter and caches only `Ok(())` and `Eip1271MagicValueMismatch` outcomes; every other error class re-hits the chain. Governed by [ADR 0014](docs/adr/0014-eip1271-verification-cache.md). | Contract | Yes | `crates/signing/tests/eip1271_cache_contract.rs::cache_skips_every_non_cacheable_error_class`, `crates/signing/tests/ui.rs::eip1271_error_match_requires_wildcard`, `crates/signing/tests/ui/eip1271_error_match_requires_wildcard.rs` | 2026-05-01 |
| `PROP-SIG-004` | `cow-sdk-signing` | `InMemoryEip1271VerificationCache` uses an injectable browser-safe time source on `wasm32-unknown-unknown`, so cache misses, writes, and TTL boundaries remain deterministic while preserving the native `std::time::Instant` contract elsewhere. Governed by [ADR 0014](docs/adr/0014-eip1271-verification-cache.md). | Contract | Yes | `crates/signing/tests/eip1271_cache_contract.rs::cache_ttl_boundary_holds_at_minus_one_and_misses_at_plus_one`, `crates/signing/tests/wasm_cache_contract.rs::cache_ttl_boundary_holds_at_minus_one_and_misses_at_plus_one_on_wasm32`, `.github/workflows/wasm.yml` | 2026-05-01 |
| `PROP-SIG-005` | `cow-sdk-signing` | The typed-data digest fuzz target ships with a non-empty 200-byte corpus seeded from signing parity fixture data plus byte-pattern boundaries, so scheduled EIP-712 digest fuzzing starts from deterministic signed-order shapes. | Contract | Yes | `fuzz/fuzz_targets/fuzz_typed_data_digest.rs`, `fuzz/corpus/fuzz_typed_data_digest/`, `parity/fixtures/signing.json` | 2026-04-24 |
| `PROP-ORD-003` | `cow-sdk-orderbook` | `OrderBookApi::builder()` is the sole production construction path; the typestate encodes the required inputs (chain, environment, transport) at compile time and no free-function constructor remains. Governed by [ADR 0013](docs/adr/0013-http-transport-injection-and-typestate-builders.md). | Contract | Yes | `crates/orderbook/tests/builder_contract.rs` | 2026-04-21 |
| `PROP-SBG-003` | `cow-sdk-subgraph` | `SubgraphApi::builder()` is the sole production construction path; the typestate encodes the required inputs (chain, API key, transport) at compile time and a `trybuild` UI witness asserts `.build()` without `.transport(...)` does not compile on `wasm32` targets. Governed by [ADR 0013](docs/adr/0013-http-transport-injection-and-typestate-builders.md). | Contract | Yes | `crates/subgraph/tests/builder_contract.rs`, `crates/subgraph/tests/ui/builder_wasm32_missing_transport.rs` | 2026-04-21 |
| `PROP-SBG-004` | `cow-sdk-subgraph` | `SubgraphError::Transport` carries a typed `TransportErrorClass` alongside sanitized transport details, so subgraph callers can classify failures without parsing rendered error text and credential-bearing URLs stay stripped before display. Governed by [ADR 0013](docs/adr/0013-http-transport-injection-and-typestate-builders.md). | Contract | Yes | `crates/subgraph/tests/error_contract.rs::transport_variant_carries_typed_class_and_sanitized_detail`, `crates/subgraph/tests/api_contract.rs::transport_failures_surface_typed_context`, `crates/subgraph/src/error.rs` | 2026-04-25 |
| `PROP-WS-001` | whole workspace | The published `cow-sdk` crate family (`cow-sdk`, `cow-sdk-core`, `cow-sdk-contracts`, `cow-sdk-signing`, `cow-sdk-app-data`, `cow-sdk-orderbook`, `cow-sdk-trading`, `cow-sdk-subgraph`, `cow-sdk-browser-wallet`) excludes `alloy-provider` from its transitive dependency graph. The `cargo tree --invert alloy-provider -p ...` invariant succeeds when no shipped crate transitively depends on `alloy-provider`; Cargo's success-case `did not match any packages` output is normalised by `cargo check-alloy-provider-invariant`. | Contract | Yes | `.github/workflows/_quality-gate.yml` (alloy-provider-invariant job), `cargo tree --invert alloy-provider -p cow-sdk-core -p cow-sdk-contracts -p cow-sdk-signing -p cow-sdk-orderbook -p cow-sdk-subgraph -p cow-sdk-app-data -p cow-sdk-trading -p cow-sdk-browser-wallet -p cow-sdk` | 2026-04-21 |
| `PROP-WS-002` | whole workspace | Public-field compatibility surfaces in `cow-sdk-app-data` (`AppDataValidated`, `AppDataParams`, `AppDataInfo`, `ValidationResult`, `IpfsUploadResult`, `IpfsFetchPolicy`, `TransportResponse`), `cow-sdk-subgraph` (`SubgraphGraphQlError`, `SubgraphGraphQlErrorLocation`, `SubgraphRequestErrorContext`, `SubgraphConfig`, `SubgraphConfigOverride`), `cow-sdk-signing` (`SigningResult`, `GeneratedOrderId`), and `cow-sdk-trading` (`Eip1271VerificationParameters`, `QuoteCacheKey`) carry `#[non_exhaustive]` at the struct head so additive fields remain compatible for downstream consumers. | Public API | Yes | `crates/app-data/src/info.rs`, `crates/app-data/src/types.rs`, `crates/app-data/src/fetch.rs`, `crates/subgraph/src/api.rs`, `crates/subgraph/src/error.rs`, `crates/signing/src/order_signing.rs`, `crates/trading/src/cache.rs`, `crates/trading/src/types.rs` | 2026-04-24 |
| `PROP-WS-003` | whole workspace | Typestate marker structs across `cow-sdk-orderbook`, `cow-sdk-subgraph`, and `cow-sdk-trading` are sealed with private tuple fields, so external callers cannot construct marker values directly and must use the documented builder transition methods. | Public API | Yes | `crates/orderbook/src/builder.rs`, `crates/subgraph/src/builder.rs`, `crates/trading/src/sdk.rs` | 2026-04-25 |
| `PROP-WS-004` | whole workspace | CI enforces dependency-pin lockstep for the cross-crate protocol stack: every root `alloy-*` workspace pin must remain on one major.minor release line, and the nested WASM workspace pins for `wasm-bindgen`, `web-sys`, and `wasm-bindgen-futures` must match the root workspace pins. | Contract | Yes | `.github/workflows/_quality-gate.yml`, `.github/workflows/wasm.yml`, `Cargo.toml`, `examples/wasm/Cargo.toml`, `examples/wasm/sdk-verification-console/Cargo.toml`, `examples/wasm/browser-wallet-console/Cargo.toml` | 2026-04-25 |
| `PROP-WS-005` | whole workspace | Both shipped WASM consoles ship a `Content-Security-Policy` meta tag declaring the canonical `script-src` and `connect-src` allowlists. | Contract | Yes | `examples/wasm/browser-wallet-console/index.html`, `examples/wasm/sdk-verification-console/index.html`, `examples/wasm/README.md` | 2026-04-25 |
| `PROP-DOCS-001` | whole workspace | Every published crate with a per-crate `README.md` declares it in package metadata, and every per-crate README that carries Rust examples is compile-gated through crate rustdoc via `cargo test --workspace --doc`. The current README set is `cow-sdk-core`, `cow-sdk-contracts`, `cow-sdk-signing`, `cow-sdk-app-data`, `cow-sdk-orderbook`, `cow-sdk-trading`, `cow-sdk-subgraph`, `cow-sdk-browser-wallet`, `cow-sdk-transport-wasm`, and `cow-sdk`. | Contract | Yes | `crates/core/README.md`, `crates/contracts/README.md`, `crates/signing/README.md`, `crates/app-data/README.md`, `crates/orderbook/README.md`, `crates/trading/README.md`, `crates/subgraph/README.md`, `crates/browser-wallet/README.md`, `crates/transport-wasm/README.md`, `crates/sdk/README.md`, `crates/core/src/lib.rs`, `crates/contracts/src/lib.rs`, `crates/signing/src/lib.rs`, `crates/app-data/src/lib.rs`, `crates/orderbook/src/lib.rs`, `crates/trading/src/lib.rs`, `crates/subgraph/src/lib.rs`, `crates/sdk/src/lib.rs`, `crates/browser-wallet/src/lib.rs`, `.github/workflows/docs-quality.yml` | 2026-04-25 |
| `PROP-DOCS-002` | whole workspace | Workspace MSRV bump policy is documented at `docs/msrv-policy.md` with explicit cadence, notice window, and trigger criteria. | Contract | Yes | `docs/msrv-policy.md`, `README.md`, `CONTRIBUTING.md` | 2026-04-24 |
| `PROP-DOCS-003` | `cow-sdk` | The `cow-sdk` prelude remains curated to first-touch quote, sign, post, app-data validation, transport/provider wiring, and primary error-handling workflows; specialized APIs remain reachable through named-module re-exports. | Public API | Yes | `crates/sdk/src/prelude.rs`, `crates/sdk/tests/public_api.rs` | 2026-04-24 |
| `PROP-DOCS-004` | whole workspace | `SECURITY.md` documents the base-URL override and browser-wallet trust threat surfaces with operator-side mitigations. | Contract | Yes | `SECURITY.md` | 2026-04-25 |
| `PROP-CORE-008` | `cow-sdk-core` | `SellTokenSource` and `BuyTokenDestination` are distinct typed enums; no silent normalize-for-buy rewrite between sell-side and buy-side balance destinations. Governed by [ADR 0016](docs/adr/0016-split-sell-and-buy-token-balance-enums.md). | Contract | Yes | `crates/core/tests/token_balance_parity.rs`, `crates/core/tests/token_balance_ui.rs` | 2026-04-21 |
| `PROP-CORE-009` | `cow-sdk-core` | `SignedAmount` keeps authoritative typed storage as `BigInt`, preserves the decimal-string wire serde shape, and delegates signed arithmetic through typed accessors and checked operations instead of reparsing strings. | Property | Yes | `crates/core/tests/property_contract.rs`, `crates/core/tests/types_contract.rs` | 2026-04-24 |
| `PROP-CORE-010` | `cow-sdk-core` | `AsyncProvider` is the read-only async chain-RPC trait, and `AsyncSigningProvider` is the signing-capable extension that owns signer creation. Wallet-capable providers implement both traits; read-only adapters implement only `AsyncProvider`, keeping signer dependencies out of read-only provider crates. Governed by [ADR 0024](docs/adr/0024-asyncprovider-asyncsigningprovider-capability-split.md). | Contract | Yes | `crates/core/tests/asyncprovider_capability_split_contract.rs`, `crates/core/src/traits.rs` doctests | 2026-04-24 |
| `PROP-CORE-011` | `cow-sdk-core` | Typed-amount arithmetic stays on the typed boundary: `Add`, `Sub`, `AddAssign`, `SubAssign`, `checked_add`, `checked_sub`, and `checked_mul` exist on `cow_sdk_core::Amount`. | Contract | Yes | `crates/core/src/types.rs` (`Amount::checked_add`, `Amount::checked_sub`, `Amount::checked_mul`, `impl Add<Self> for Amount`, `impl Sub<Self> for Amount`, `impl AddAssign<Self> for Amount`, `impl SubAssign<Self> for Amount`), `crates/core/tests/types_contract.rs::amount_addition_is_commutative_across_curated_boundaries`, `crates/core/tests/types_contract.rs::amount_addition_is_associative_for_curated_triple`, `crates/core/tests/types_contract.rs::amount_checked_arithmetic_preserves_option_shape` | 2026-04-24 |
| `PROP-CORE-012` | `cow-sdk-core`, `cow-sdk-transport-wasm` | Both native and browser HTTP adapters emit one transport-layer span per dispatch with the same method, path-only endpoint, request-byte, and response-byte field shape when the `tracing` feature is enabled; the browser adapter adds only its platform-specific `chain = "wasm32"` field. | Contract | Yes | `crates/core/tests/transport_contract.rs`, `crates/transport-wasm/tests/fetch_contract.rs`, `docs/observability.md` | 2026-04-24 |
| `PROP-CORE-013` | `cow-sdk-core` | `Cancellable::cancel_with` resolves a fired token through the crate-level `Cancelled` error path, emits a `debug` tracing event with target `cow_sdk::cancel` and `cancelled = true` when the `tracing` feature is enabled, and composes across every long-running public async method on `OrderBookApi`, `SubgraphApi`, and `TradingSdk` through call-site wrapping. | Contract | Yes | `crates/core/tests/cancellation_contract.rs`, `crates/core/tests/cancellation_contract.rs::tracing_contract::cancellation_branch_emits_debug_event_with_target_and_field`, `crates/core/tests/cancellation_coverage_validator.rs::cancellation_tables_cover_every_public_async_method`, `crates/orderbook/tests/api_contract.rs::get_version_returns_cancelled_when_combinator_token_fires_before_send`, `crates/orderbook/tests/api_contract.rs::get_version_combinator_aborts_an_in_flight_request`, `crates/orderbook/tests/cancellation_composition_contract.rs`, `crates/orderbook/tests/request_contract.rs::retry_after_backoff_wait_can_be_cancelled_before_next_attempt`, `crates/subgraph/tests/api_contract.rs::get_totals_returns_cancelled_when_combinator_token_fires_before_send`, `crates/subgraph/tests/api_contract.rs::get_totals_combinator_aborts_an_in_flight_request`, `crates/subgraph/tests/cancellation_composition_contract.rs`, `crates/trading/tests/sdk_contract.rs::get_quote_only_returns_cancelled_when_combinator_token_fires_before_call`, `crates/trading/tests/sdk_contract.rs::get_quote_only_combinator_aborts_an_in_flight_quote`, `crates/trading/tests/cancellation_composition_contract.rs` | 2026-05-01 |
| `PROP-TRD-004` | `cow-sdk-trading` | Client-side submission validator rejects every services-authority precondition (validTo range, zero amount, same-token, non-EthFlow native sentinel, zero-address from, metadata.signer mismatch, owner mismatch) before wire dispatch, while its public documentation frames the check as defence-in-depth rather than a services-acceptance guarantee. Governed by [ADR 0015](docs/adr/0015-client-side-order-bounds-validator.md). | Contract | Yes | `crates/trading/tests/validation_contract.rs`, `crates/trading/tests/property_contract.rs`, `crates/trading/src/validation.rs`, `crates/trading/tests/post_contract.rs::post_swap_order_appdata_from_mismatch_does_not_upload_or_sign`, `crates/trading/tests/post_contract.rs::post_swap_order_same_buy_sell_token_does_not_upload_or_sign`, `crates/trading/tests/post_contract.rs::post_swap_order_zero_amount_does_not_upload_or_sign`, `docs/audit/trading-order-bounds-validator-audit.md` | 2026-05-01 |
| `PROP-TRD-005` | `cow-sdk-trading` | `PartnerFee` policy round-trips for every services-supported shape (`Volume`, `Surplus`, `PriceImprovement`, plus `Single` and `Multiple` wrappers) with `u16` bps bounds and `#[non_exhaustive]` at both enum heads. | Property | Yes | `crates/app-data/tests/partner_fee_contract.rs`, `crates/app-data/src/types.rs (tests)` | 2026-04-21 |
| `PROP-TRD-006` | `cow-sdk-trading` | `TradingSdkBuilder::build_ready()` on `wasm32` targets fails at the terminal with `TradingError::MissingInjectedOrderbookClient` unless an orderbook client is injected through `TradingSdkOptions::with_orderbook_client`. Governed by [ADR 0011](docs/adr/0011-typed-amount-boundary-and-typestate-ready-state-construction.md). | Contract | Yes | `crates/trading/tests/sdk_contract.rs::build_ready_rejects_missing_injected_orderbook_client_on_wasm32`, `crates/trading/tests/sdk_contract.rs::build_ready_succeeds_on_wasm32_with_injected_orderbook_client`, `crates/trading/tests/sdk_contract.rs::build_ready_succeeds_on_native_without_injected_orderbook_client`, `.github/workflows/wasm.yml` | 2026-04-23 |
| `PROP-TRD-007` | `cow-sdk-trading` | EthFlow and pre-sign transaction gas estimates apply a documented 20% overhead with floor integer rounding (`gas + (gas * 20) / 100`). | Contract | Yes | `crates/trading/src/onchain.rs`, `crates/trading/src/slippage.rs`, `crates/trading/tests/onchain_contract.rs::eth_flow_gas_estimate_applies_documented_floor_overhead`, `crates/trading/tests/onchain_contract.rs::pre_sign_gas_estimate_applies_documented_floor_overhead`, `docs/audit/trading-order-bounds-validator-audit.md` | 2026-05-01 |
| `PROP-TRD-008` | `cow-sdk-trading` | `fuzz_order_bounds_validator` ships with a non-empty corpus seeded from `validation_contract.rs` happy path plus boundary-shaped inputs covering every `ClientRejection` variant and the documented `now`/`valid_to` extremes. | Property | Yes | `fuzz/fuzz_targets/fuzz_order_bounds_validator.rs`, `fuzz/corpus/fuzz_order_bounds_validator/`, `crates/trading/tests/property_contract.rs`, `docs/audit/trading-order-bounds-validator-audit.md` | 2026-05-01 |
| `PROP-APP-004` | `cow-sdk-app-data` | `AppDataValidation` emits `ApproachingSizeLimit` warning at ≥ 75% of `APP_DATA_MAX_BYTES` and rejects `TooLarge` at 100%. | Contract | Yes | `crates/app-data/tests/validated_shape_contract.rs`, `crates/app-data/tests/property_contract.rs`, `crates/app-data/src/info.rs (tests)` | 2026-04-21 |
| `PROP-AD-001` | `cow-sdk-app-data` | App-data metadata hooks have a typed `HookList` slot on `AppDataParams`. Consumers can read the same underlying `metadata.hooks` field through either the typed slot or the open-ended `metadata["hooks"]` map; both paths expose the same data and are equally supported. | Contract | Yes | `crates/app-data/tests/hooks_contract.rs`, `crates/app-data/src/metadata/hooks.rs`, `crates/app-data/src/types.rs` | 2026-04-25 |
| `PROP-AD-002` | `cow-sdk-app-data`, `cow-sdk-trading` | `fuzz_app_data_merge` ships with a corpus seeded from typed app-data merge fixtures with at least 5 seed files covering empty bases, nested metadata, signer conflicts, partner-fee shapes, hooks replacement, flash-loan metadata, and key collisions. | Contract | Yes | `fuzz/fuzz_targets/fuzz_app_data_merge.rs`, `fuzz/corpus/fuzz_app_data_merge/`, `crates/trading/tests/app_data_merge_contract.rs`, `docs/audit/trading-app-data-merge-audit.md` | 2026-05-01 |
| `PROP-ORD-004` | `cow-sdk-orderbook` | `OrderbookRejection` decodes every services `errorType` to a typed variant; unknown codes route to `Unknown { code, message }` with `#[non_exhaustive]` at the enum head. Governed by [ADR 0017](docs/adr/0017-typed-orderbook-rejection-parser.md). | Contract | Yes | `crates/orderbook/tests/rejection_contract.rs::every_known_services_tag_parses_to_its_typed_variant`, `crates/orderbook/tests/rejection_contract.rs::app_data_invalid_tag_preserves_typed_message_and_display`, `crates/orderbook/tests/rejection_contract.rs::app_data_mismatch_tag_preserves_typed_message_and_display`, `crates/orderbook/tests/rejection_contract.rs::not_found_tag_preserves_typed_message_and_display`, `crates/orderbook/tests/parity_contract.rs::parity_fixture_cases_hold` | 2026-04-23 |
| `PROP-ORD-005` | `cow-sdk-orderbook` | Orderbook quote-side, quote, order, trade, auction-order, total-surplus, and solver-execution wire DTO amount fields store promoted amounts as `cow_sdk_core::Amount` while preserving the decimal-string JSON wire shape through custom serde and failing malformed amount strings at deserialization. | Contract | Yes | `crates/orderbook/src/types.rs`, `crates/orderbook/tests/wire_contract.rs`, `crates/orderbook/tests/parity_contract.rs::parity_fixture_cases_hold` | 2026-04-24 |
| `PROP-ORD-006` | `cow-sdk-orderbook` | Retry observability stays aligned with the public field registry: retry events carry `attempt_index`, `backoff_ms`, and either `status` or `transport_error_class`, and quote/order spans populate `quote_id`, `attempts`, and `status` when the `tracing` feature is enabled. | Contract | Yes | `crates/orderbook/tests/request_contract.rs::tracing_contract::execute_with_emits_retry_events_with_status_and_transport_error_fields`, `crates/orderbook/tests/request_contract.rs::tracing_contract::send_order_span_records_quote_id_attempts_and_status`, `docs/observability.md` | 2026-04-24 |
| `PROP-OBK-001` | `cow-sdk-orderbook`, `cow-sdk-trading` | Order quote requests pre-validate the `(signingScheme, onchainOrder)` pair before transport: `Eip712` and `EthSign` cannot be marked on-chain, while `Eip1271` and `PreSign` retain services-compatible on-chain behavior. | Contract | Yes | `crates/orderbook/src/types.rs`, `crates/orderbook/tests/types_contract.rs::quote_request_validate_rejects_incompatible_onchain_ecdsa_signing_pairs`, `crates/trading/tests/quote_contract.rs::quote_request_validation_runs_before_orderbook_transport` | 2026-04-25 |
| `PROP-OBK-002` | `cow-sdk-orderbook`, `cow-sdk-trading` | The `OrderbookClient` trait is owned by `cow-sdk-orderbook` and reachable as `cow_sdk_orderbook::OrderbookClient` without a trading-crate dependency. `cow-sdk-trading` re-exports the same trait as an additive convenience so trading-crate consumers can compose against it without an explicit orderbook-crate import. | Public API | Yes | `crates/orderbook/src/lib.rs`, `crates/trading/src/lib.rs` | 2026-04-25 |
| `PROP-SEC-001` | whole workspace | Credential-bearing surfaces on `OrderBookApiBuilder`, `EnvBaseUrlOverrides`, `SubgraphApiBuilder`, and the `IpfsUploadTransport::post_json` Pinata header boundary debug-format as `[redacted]` and do not expose secret bytes through `Debug`; the orderbook API-key header path returns a typed validation error rather than panicking on invalid header material. | Contract | Yes | `crates/orderbook/tests/builder_contract.rs::builder_debug_redacts_partner_api_key`, `crates/orderbook/tests/builder_contract.rs::env_base_url_overrides_debug_redacts_embedded_credentials`, `crates/orderbook/src/api.rs`, `crates/subgraph/tests/builder_contract.rs::builder_debug_redacts_partner_api_key`, `crates/app-data/tests/pinning_contract.rs::pinning_headers_debug_redacts_secret_bytes` | 2026-04-25 |
| `PROP-CORE-014` | `cow-sdk-core`, `cow-sdk-app-data` | Redacted URL maps, optional URL maps, API-context base URLs, and response-body snippets redact credential-bearing values before public formatting or serialization, while raw dispatch access remains explicit. | Contract | Yes | `crates/core/tests/redaction_contract.rs::redacted_url_map_public_representations_redact_values_and_preserve_keys`, `crates/core/tests/redaction_contract.rs::redacted_optional_url_map_public_representations_redact_some_values_and_keep_none`, `crates/core/tests/redaction_contract.rs::api_context_redacts_base_urls_in_debug_and_serialize_but_resolves_raw_url`, `crates/core/tests/redaction_contract.rs::response_body_redaction_strips_credential_shapes_without_regex_dependency`, `crates/app-data/tests/error_redaction_contract.rs::pinning_error_body_is_redacted_at_storage_and_public_representations` | 2026-04-27 |
| `PROP-CORE-015` | `cow-sdk-core` | `fuzz_transport_error_classify` ships with a corpus seeded from core transport-error fixtures with at least 5 seed files covering every `TransportErrorClass` variant, malformed `Retry-After` headers, credential-bearing URL body snippets, and JSON-RPC error bodies. | Contract | Yes | `fuzz/fuzz_targets/fuzz_transport_error_classify.rs`, `fuzz/corpus/fuzz_transport_error_classify/`, `crates/core/tests/transport_contract.rs::transport_error_class_labels_cover_every_documented_variant`, `crates/core/tests/redaction_contract.rs::response_body_redaction_strips_credential_shapes_without_regex_dependency` | 2026-05-01 |
| `PROP-ORD-007` | `cow-sdk-orderbook` | `InvalidTradeFilter`, `InvalidLimit`, and the `LIMIT_OUT_OF_BOUNDS` wire tag parse to dedicated `OrderbookRejection` variants; the enum surface remains 49 variants including the forward-compatible `Unknown` fallback. | Contract | Yes | `crates/orderbook/tests/rejection_contract.rs::every_known_services_tag_parses_to_its_typed_variant`, `crates/orderbook/src/rejection.rs` | 2026-04-27 |
| `PROP-ORD-008` | `cow-sdk-orderbook` | `fuzz_orderbook_rejection_decode` ships with a corpus seeded from services rejection fixtures with at least 5 seed files covering every known `errorType` tag plus malformed-envelope and non-UTF-8 body boundaries. | Contract | Yes | `fuzz/fuzz_targets/fuzz_orderbook_rejection_decode.rs`, `fuzz/corpus/fuzz_orderbook_rejection_decode/`, `crates/orderbook/tests/rejection_contract.rs::every_known_services_tag_parses_to_its_typed_variant` | 2026-05-01 |
| `PROP-SIG-006` | `cow-sdk-contracts`, `cow-sdk-signing` | Contracts and signing domain-separator helpers stay byte-identical against the shared EIP-712 parity fixture. | Contract | Yes | `crates/contracts/src/primitives.rs::tests::domain_separator_matches_shared_parity_fixture`, `crates/signing/src/domain.rs::tests::domain_separator_matches_shared_parity_fixture` | 2026-04-27 |
| `PROP-CON-008` | `cow-sdk-contracts` | `verify_eip1271_signature_async` emits the canonical tracing span and cache telemetry events without recording verifier addresses, digests, signatures, or response bytes. | Contract | Yes | `crates/contracts/tests/verify_telemetry_contract.rs::verifier_emits_canonical_span_and_safe_miss_store_events`, `crates/contracts/tests/verify_telemetry_contract.rs::verifier_emits_hit_event_without_reaching_provider`, `crates/contracts/tests/verify_telemetry_contract.rs::verifier_emits_skip_event_for_non_cacheable_errors` | 2026-04-27 |
| `PROP-SEC-002` | `cow-sdk-orderbook`, `cow-sdk-subgraph` | Orderbook and subgraph base-URL builders reject non-canonical external hosts by default and require explicit reviewed host-policy opt-in for custom or loopback routes. | Contract | Yes | `crates/orderbook/tests/host_policy_contract.rs::orderbook_builder_blocks_custom_hosts_by_default`, `crates/orderbook/tests/host_policy_contract.rs::orderbook_builder_accepts_explicit_allow_and_loopback_policy`, `crates/subgraph/tests/host_policy_contract.rs::subgraph_builder_blocks_custom_hosts_by_default`, `crates/subgraph/tests/host_policy_contract.rs::subgraph_builder_accepts_explicit_allow_and_loopback_policy` | 2026-04-27 |
| `PROP-BWL-003` | `cow-sdk-browser-wallet` | Anonymous EIP-1193 provider transports fail closed unless they are discovered with origin metadata or constructed with an explicitly reviewed trusted origin. | State machine | Yes | `crates/browser-wallet/tests/provider_contract.rs::anonymous_provider_builder_requires_trusted_origin`, `crates/browser-wallet/tests/provider_contract.rs::provider_builder_accepts_explicit_trusted_origin`, `crates/browser-wallet/tests/wasm_bridge_contract.rs` | 2026-04-27 |
| `PROP-CON-009` | `cow-sdk-contracts` | `Signature::recover_ecdsa_address` recovers EIP-712 and EthSign ECDSA signers from the supplied digest, rejects non-ECDSA variants, and `Signature::declared_address` reports only declared-owner signature schemes. | Contract | Yes | `crates/contracts/tests/signature_contract.rs::signature_helpers_preserve_public_contract_surface`, `crates/contracts/tests/signature_contract.rs::recover_ecdsa_address_recovers_eip712_prehash_signer`, `crates/contracts/tests/signature_contract.rs::recover_ecdsa_address_eth_sign_recovers_signer_through_canonical_eip191_prehash`, `crates/contracts/tests/signature_contract.rs::recover_ecdsa_address_rejects_non_ecdsa_variants` | 2026-05-01 |
| `PROP-CON-010` | `cow-sdk-contracts` | `cow-sdk-contracts::grant_required_roles`-emitted role hashes are byte-identical to `solidityKeccak256(["uint256","bytes4"], [vaultAddress, selector])` from the pinned upstream `cowprotocol/contracts` repo, for every supported `VAULT_INTERFACE` method. | Contract | Yes | `crates/contracts/tests/vault_contract.rs::vault_role_hashes_match_the_canonical_solidity_packed_layout`, `crates/contracts/tests/parity_contract.rs::parity_fixture_cases_hold`, `parity/fixtures/contracts.json` | 2026-05-01 |
| `PROP-CON-011` | `cow-sdk-contracts` | `SettlementEncoder::encode_interaction` rejects vault-relayer-as-target only for canonical-registry settlement domains; custom or unknown settlement domains pass through neutrally. Governed by [ADR 0034](docs/adr/0034-interaction-encoder-target-policy.md). | Contract | Yes | `crates/contracts/tests/interaction_contract.rs::interaction_encoder_rejects_vault_relayer_target_for_canonical_settlement_domain`, `crates/contracts/tests/interaction_contract.rs::interaction_encoder_accepts_non_vault_target_for_canonical_settlement_domain`, `crates/contracts/tests/interaction_contract.rs::interaction_encoder_does_not_cross_match_chain_or_env`, `crates/contracts/tests/interaction_contract.rs::interaction_encoder_neutral_for_unknown_custom_settlement_domain` | 2026-05-01 |
| `PROP-CON-012` | `cow-sdk-contracts` | `fuzz_recover_ecdsa_address` ships with a corpus seeded from signing parity fixture data with at least 5 seed files covering canonical ECDSA recovery bytes plus all-zero, all-`0xff`, and rejected-`v` boundaries. | Contract | Yes | `fuzz/fuzz_targets/fuzz_recover_ecdsa_address.rs`, `fuzz/corpus/fuzz_recover_ecdsa_address/`, `parity/fixtures/signing.json`, `crates/contracts/tests/signature_contract.rs` | 2026-05-01 |
| `PROP-CON-013` | `cow-sdk-contracts` | `fuzz_settlement_settle_encode` ships with a corpus seeded from contracts parity fixture data with at least 5 seed files covering canonical settlement ABI shapes plus empty-list, single-trade, capped-list, and mixed-interaction boundaries. | Contract | Yes | `fuzz/fuzz_targets/fuzz_settlement_settle_encode.rs`, `fuzz/corpus/fuzz_settlement_settle_encode/`, `parity/fixtures/contracts.json`, `crates/contracts/tests/settlement_contract.rs` | 2026-05-01 |
| `PROP-CON-014` | `cow-sdk-contracts` | `fuzz_settlement_invalidate_order_encode` ships with a corpus seeded from contracts parity fixture data with at least 5 seed files covering canonical packed order UIDs plus empty, 55-byte, 56-byte, 57-byte, and all-`0xff` payload boundaries. | Contract | Yes | `fuzz/fuzz_targets/fuzz_settlement_invalidate_order_encode.rs`, `fuzz/corpus/fuzz_settlement_invalidate_order_encode/`, `parity/fixtures/contracts.json`, `crates/contracts/tests/settlement_contract.rs` | 2026-05-01 |
| `PROP-CON-015` | `cow-sdk-contracts` | `fuzz_vault_relayer_transfer_from_accounts_encode` ships with a corpus seeded from contracts parity fixture data with at least 5 seed files covering canonical transfer ABI shapes plus empty-list, single-transfer, capped-list, all-zero, and all-`0xff` boundaries. | Contract | Yes | `fuzz/fuzz_targets/fuzz_vault_relayer_transfer_from_accounts_encode.rs`, `fuzz/corpus/fuzz_vault_relayer_transfer_from_accounts_encode/`, `parity/fixtures/contracts.json`, `crates/contracts/tests/vault_contract.rs` | 2026-05-01 |
| `PROP-CON-016` | `cow-sdk-contracts` | `fuzz_ethflow_create_order_encode` ships with a corpus seeded from contracts parity fixture data with at least 5 seed files covering canonical EthFlow order ABI shapes plus zero-amount, maximum-timestamp, all-zero, and all-`0xff` boundaries. | Contract | Yes | `fuzz/fuzz_targets/fuzz_ethflow_create_order_encode.rs`, `fuzz/corpus/fuzz_ethflow_create_order_encode/`, `parity/fixtures/contracts.json`, `crates/trading/tests/parity_contract.rs` | 2026-05-01 |
| `PROP-CON-017` | `cow-sdk-contracts` | `fuzz_erc20_permit_typed_data_hash` ships with a corpus seeded from contracts parity fixture data with at least 5 seed files covering canonical EIP-2612 permit digests plus empty-domain, all-zero, all-`0xff`, and maximum-amount boundaries. | Contract | Yes | `fuzz/fuzz_targets/fuzz_erc20_permit_typed_data_hash.rs`, `fuzz/corpus/fuzz_erc20_permit_typed_data_hash/`, `parity/fixtures/contracts.json`, `crates/contracts/tests/erc20.rs` | 2026-05-01 |
| `PROP-AUD-001` | whole workspace | Three named audit-lane docs cover the workspace `unsafe_code = deny` lint posture, the panic-free public surface contract, and the workflow security posture. | Contract | Yes | `docs/audit/unsafe-code-policy-audit.md`, `docs/audit/panic-free-public-surface-audit.md`, `docs/audit/workflow-security-audit.md`, `.github/workflows/_quality-gate.yml` | 2026-04-23 |
