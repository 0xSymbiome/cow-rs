# Properties Registry

This registry is the canonical public index of invariants and state contracts
for `cow-rs`.

Use it with:

- [Verification](docs/verification.md) for how the evidence is interpreted and
  the crate and workflow lanes that exercise each surface

Executable coverage stays with the crate or browser surface that owns the
behavior. This registry records what must remain true, who owns it, and where
the current evidence lives.

`Covered` uses these values:

- `Yes`: dedicated executable coverage exists
- `Partial`: deterministic coverage exists, but not through a dedicated
  property or state-machine suite
- `No`: the property is registered, but no executable coverage is attached yet

`Last reviewed` records the most recent date the row was confirmed against the
shipped code. The registry follows a 90-day re-review rhythm that mirrors the
dependency-exception policy in `.github/config/deny.toml`: every row is
re-confirmed at least once per 90-day window, and the date here is refreshed
in the same change that touches the owning surface.

## Methodology

The `Type` column uses five labels. Two of them carry distinct evidence
methodologies that reviewers should read with care:

- `Property` rows are backed by a real `proptest!` macro harness with
  committed `tests/proptest-regressions/` seed files. Coverage comes from
  randomized input generation with shrinking to minimal counterexamples
  on failure, and the committed regression seeds preserve failure
  reproducibility across contributors. The codec crates
  (`cow-sdk-core`, `cow-sdk-contracts`, `cow-sdk-signing`,
  `cow-sdk-app-data`) carry their property rows through this lane.
- `Invariant` rows are backed by a curated deterministic boundary sweep:
  a reproducible 128-case iterator over hand-picked boundary cases plus
  named narrow-search profiles that extend the curated set without
  introducing randomness. Coverage is exhaustive over the curated case
  set and is reproducible by design, but it does not discover unexpected
  inputs or shrink to minimal counterexamples. The orchestration crates
  (`cow-sdk-orderbook`, `cow-sdk-trading`, `cow-sdk-subgraph`) carry
  their invariant rows through this lane because the reviewed
  invariants are transport and builder shape rather than roundtrip
  properties.

The remaining three labels — `Contract`, `State machine`, and `Public
API` — cover typed API and parser contracts exercised through targeted
regression tests, browser-wallet session and chain state-machine
transitions covered through deterministic transports and committed
browser automation, and the curated `cow-sdk` facade exported-symbol
snapshot respectively.

Release-doc guard package list: `cargo tree --invert alloy-provider -p cow-sdk-core -p cow-sdk-contracts -p cow-sdk-signing -p cow-sdk-orderbook -p cow-sdk-subgraph -p cow-sdk-app-data -p cow-sdk-trading -p cow-sdk-browser-wallet -p cow-sdk-transport-wasm -p cow-sdk-alloy-provider -p cow-sdk-alloy-signer -p cow-sdk-alloy -p cow-sdk`.

| Id | Crate | Property | Type | Covered | Evidence | Last reviewed |
| --- | --- | --- | --- | --- | --- | --- |
| `PROP-CORE-001` | `cow-sdk-core` | Unsupported chain and environment resolution stays explicit and typed instead of falling back silently. | Contract | Yes | `crates/core/tests/config_contract.rs`, `crates/core/tests/types_contract.rs` | 2026-04-17 |
| `PROP-CORE-002` | `cow-sdk-core` | Runtime traits preserve typed transaction, hash, and provider boundaries across every implementation. | Contract | Yes | `crates/core/tests/traits_contract.rs`, `crates/core/tests/types_contract.rs` | 2026-05-27 |
| `PROP-CORE-003` | `cow-sdk-core` | Validated hex wrappers (`Address`, `Hash32`, `AppDataHex`, `OrderUid`, `HexData`) preserve input case and roundtrip through their string forms while failing closed on length, prefix, and character errors. | Property | Yes | `crates/core/tests/property_contract.rs`, `crates/core/tests/types_contract.rs`, `fuzz/fuzz_targets/fuzz_core_identity_validators.rs`, `fuzz/fuzz_targets/fuzz_valid_to_relative.rs` | 2026-05-21 |
| `PROP-CORE-004` | `cow-sdk-core` | `Amount` canonicalizes decimal and hexadecimal inputs into equal values, rejects negative and out-of-range inputs, rejects arithmetic that would exceed the unsigned 256-bit boundary through its `checked_*` surface (the `checked_pow` variant surfaces ruint's silent-wrap `wrapping_pow` as an explicit `None` rather than a wrapped value), and normalizes zero forms deterministically. Governed by [ADR 0011](docs/adr/0011-typed-amount-boundary-and-typestate-ready-state-construction.md). | Property | Yes | `crates/core/tests/property_contract.rs`, `crates/core/tests/property_contract.rs::amount_checked_pow_delegates_to_inner_uint_checked_pow`, `crates/core/tests/property_contract.rs::amount_saturating_pow_delegates_to_inner_uint_saturating_pow`, `crates/core/tests/types_contract.rs::amount_checked_pow_returns_none_on_overflow`, `fuzz/fuzz_targets/fuzz_amount_parse.rs` | 2026-06-01 |
| `PROP-CORE-005` | `cow-sdk-core` | `Address` case normalization is canonical: a checksummed, lowercase, or mixed-case 20-byte hex address parses to the same value, so `PartialEq`, `Hash`, and `to_hex_string` agree across casing and `HashMap` / `HashSet` lookups match the equality rule. | Property | Yes | `crates/core/tests/property_contract.rs::address_case_normalization_holds_across_hash_and_equality` | 2026-06-02 |
| `PROP-CORE-006` | `cow-sdk-core` | `SupportedChainId` roundtrips through its numeric `ChainId` form for every supported variant and fails closed on unsupported chain ids. | Property | Yes | `crates/core/tests/property_contract.rs`, `crates/core/tests/config_contract.rs` | 2026-04-17 |
| `PROP-CORE-007` | `cow-sdk-core`, `cow-sdk-transport-policy` | Every HTTP response body the SDK buffers is bounded by a configurable per-client `max_response_bytes`: a streamed read refuses an over-limit body with a typed `TransportErrorClass::ResponseTooLarge` outcome (proven on a decompression bomb so the bound holds on decoded bytes), the success and non-2xx error paths share the bound, untrusted-gateway defaults are tighter than the trusted-orderbook default, an over-limit outcome is never retried, and signature hex fields are length-bounded before decode. Governed by [ADR 0055](docs/adr/0055-bounded-response-reads.md). | Contract | Yes | `crates/core/tests/transport_contract.rs::response_exceeding_cap_is_rejected_as_response_too_large`, `crates/core/tests/transport_contract.rs::gzip_bomb_is_rejected_on_decompressed_size`, `crates/core/tests/transport_contract.rs::response_exactly_at_cap_is_accepted_and_one_over_is_rejected`, `crates/core/tests/transport_contract.rs::oversized_error_status_body_is_rejected_as_response_too_large`, `crates/transport-policy/tests/classify_contract.rs::response_too_large_is_never_retried`, `crates/transport-policy/tests/policy_contract.rs::default_policies_carry_per_client_response_byte_caps`, `crates/contracts/src/hex_field.rs (tests)`, `crates/app-data/tests/json_recursion_contract.rs` | 2026-05-29 |
| `PROP-CORE-RX-001` | `cow-sdk-core` | Transaction lifecycle types distinguish broadcast acknowledgement from mined receipt observation: `TransactionBroadcast` carries hash-only submission acknowledgement, `TransactionStatus` is a non-exhaustive success/reverted execution state, and `TransactionReceipt` is non-exhaustive with optional `status`, `block_number`, `block_hash`, `gas_used`, `from`, and `to` fields constructed through `new`, `from_parts`, and `with_*` builders. Governed by [ADR 0038](docs/adr/0038-transaction-lifecycle-types.md). | Contract | Yes | `crates/core/tests/traits_contract.rs::signer_returns_transaction_broadcast`, `crates/core/tests/traits_contract.rs::provider_returns_rich_transaction_receipt`, `crates/core/tests/traits_contract.rs::transaction_status_serde_roundtrip_camel_case`, `crates/core/tests/traits_contract.rs::transaction_receipt_skips_serializing_none_fields`, `crates/core/tests/traits_contract.rs::transaction_receipt_serializes_populated_fields`, `crates/core/tests/traits_contract.rs::transaction_receipt_with_builders_round_trips`, `crates/core/tests/traits_contract.rs::transaction_broadcast_minimal_serde_shape`, `crates/core/tests/trait_evolution_contract.rs::signer_trait_shape_unchanged`, `crates/core/tests/trait_evolution_contract.rs::transaction_receipt_struct_carries_rich_fields` | 2026-05-27 |
| `PROP-CON-001` | `cow-sdk-contracts` | Order hashing remains deterministic across semantically equivalent normalized inputs. | Property | Yes | `crates/contracts/tests/property_contract.rs`, `crates/contracts/tests/order_contract.rs`, `fuzz/fuzz_targets/fuzz_hash_order_cancellations.rs` | 2026-06-08 |
| `PROP-CON-002` | `cow-sdk-contracts` | Signature payload codecs and interaction normalization preserve explicit boundary semantics for signature and interaction payloads. | Property | Yes | `crates/contracts/tests/signature_contract.rs::eip1271_signature_payloads_roundtrip_with_variable_lengths`, `crates/contracts/tests/signature_contract.rs`, `crates/contracts/tests/interaction_contract.rs`, `crates/contracts/src/primitives.rs (tests)`, `crates/contracts/src/order/hash.rs (tests)` | 2026-06-08 |
| `PROP-SIG-001` | `cow-sdk-signing` | Domain separation changes only when the typed-data domain changes. | Property | Yes | `crates/signing/tests/property_contract.rs`, `crates/signing/tests/domain_contract.rs`, `fuzz/fuzz_targets/fuzz_signing_domain_separator.rs` | 2026-05-13 |
| `PROP-SIG-002` | `cow-sdk-signing` | Order and cancellation typed-data payloads, generated ids, and EIP-1271 helper payloads stay deterministic for equivalent inputs and explicit signing-scheme boundaries. | Property | Yes | `crates/signing/tests/property_contract.rs`, `crates/signing/tests/order_signing_contract.rs`, `crates/signing/tests/order_signing_contract.rs::eth_sign_routes_raw_32_byte_digest_to_sign_message`, `crates/signing/tests/cancellation_contract.rs`, `crates/signing/tests/eip1271_contract.rs` | 2026-05-01 |
| `PROP-APP-001` | `cow-sdk-app-data` | CID conversion round-trips between digest and CID forms without silent mutation. Governed by [ADR 0052](docs/adr/0052-alloy-primitives-canonical-primitive-layer.md). | Property | Yes | `crates/app-data/tests/property_contract.rs`, `crates/app-data/tests/cid_contract.rs::cid_rejects_non_keccak256_multihash_codecs`, `crates/app-data/tests/cid_contract.rs`, `crates/app-data/src/cid.rs (tests)`, `fuzz/fuzz_targets/fuzz_app_data_cid_roundtrip.rs`, `fuzz/fuzz_targets/fuzz_cid_to_app_data_hex.rs` | 2026-05-26 |
| `PROP-APP-002` | `cow-sdk-app-data` | Invalid app-data, typed-metadata, fetch, and info inputs fail closed. | Property | Yes | `crates/app-data/tests/property_contract.rs` (includes `schema_versions_roundtrip_and_reject_malformed`), `crates/app-data/tests/typed_metadata_contract.rs`, `crates/app-data/tests/schema_drift_contract.rs`, `crates/app-data/tests/schema_contract.rs::unmodelled_wrappers_doc_passes_through_and_preserves_typed_fields`, `crates/app-data/tests/flashloan_contract.rs::flashloan_v1_7_0_rejects_invalid_address`, `crates/app-data/tests/flashloan_contract.rs::flashloan_v1_7_0_rejects_zero_amount`, `crates/app-data/tests/flashloan_contract.rs::flashloan_v1_7_0_rejects_missing_field`, `crates/app-data/tests/schema_contract.rs`, `crates/app-data/tests/fetch_contract.rs`, `crates/app-data/src/info.rs (tests)`, `crates/app-data/src/types/doc.rs (tests)`, `crates/app-data/tests/ipfs_config_redaction_contract.rs`, `fuzz/fuzz_targets/fuzz_partner_fee_from_value.rs`, `fuzz/fuzz_targets/fuzz_flashloan_hints.rs`, `fuzz/fuzz_targets/fuzz_hook_list_deserialize.rs`, `fuzz/fuzz_targets/fuzz_schema_version_is_semver.rs` | 2026-06-03 |
| `PROP-APP-003` | `cow-sdk-app-data` | Deterministic document sources render canonical JSON and stable latest-path digests for equivalent document shapes. | Property | Yes | `crates/app-data/tests/property_contract.rs` (includes `document_sources_canonicalize_equivalent_top_level_permutations` and `canonical_json_handles_unicode_escapes_consistently`), `crates/app-data/tests/app_data_info_contract.rs`, `crates/app-data/src/info.rs (tests)`, `fuzz/fuzz_targets/fuzz_stringify_deterministic.rs` | 2026-05-13 |
| `PROP-AP-001` | `cow-sdk-alloy-provider` | `RpcAlloyProvider` exposes an SDK-owned documented API; upstream Alloy provider, transport, network, RPC, consensus, and `reqwest` values stay internal except for the doc-hidden sibling-crate seam, which is not subject to semver guarantees and may change in any minor release per ADR 0035. Governed by [ADR 0035](docs/adr/0035-alloy-provider-adapter.md). | Public API | Yes | `crates/alloy-provider/src/lib.rs`, `crates/alloy-provider/src/provider.rs`, `docs/audit/alloy-provider-adapter-audit.md` | 2026-05-26 |
| `PROP-AP-002` | `cow-sdk-alloy-provider` | `RpcAlloyProvider` implements every method on the `cow-sdk-core::Provider` trait. Governed by [ADR 0035](docs/adr/0035-alloy-provider-adapter.md). | Contract | Yes | `crates/alloy-provider/tests/provider_contract.rs`, `crates/alloy-provider/src/provider.rs`, `crates/core/src/traits/provider.rs` | 2026-05-12 |
| `PROP-AP-003` | `cow-sdk-alloy-provider` | `RpcAlloyProvider` does not implement `SigningProvider` or `Signer`; compile-fail tests enforce the capability boundary. Governed by [ADR 0035](docs/adr/0035-alloy-provider-adapter.md). | Contract | Yes | `crates/alloy-provider/tests/compile_fail.rs::negative_impls_and_sealed_markers_hold`, `crates/alloy-provider/tests/trybuild/no_signing_provider.rs`, `crates/alloy-provider/tests/trybuild/no_signer.rs` | 2026-05-27 |
| `PROP-AP-004` | `cow-sdk-alloy-provider` | `RpcAlloyProviderBuilder::build` is callable only after HTTP transport has been selected, and externally constructed typestate markers cannot bypass the builder. Governed by [ADR 0035](docs/adr/0035-alloy-provider-adapter.md). | Contract | Yes | `crates/alloy-provider/src/builder.rs`, `crates/alloy-provider/tests/compile_fail.rs::negative_impls_and_sealed_markers_hold`, `crates/alloy-provider/tests/trybuild/external_marker_construction_fails.rs` | 2026-05-06 |
| `PROP-AP-005` | `cow-sdk-alloy-provider` | Provider URL and transport details are redacted before public formatting; invalid URL errors carry no input detail, provider debug output hides the transport, and transport errors store detail through `Redacted<String>`. Governed by [ADR 0035](docs/adr/0035-alloy-provider-adapter.md). | Property | Yes | `crates/alloy-provider/tests/redaction_contract.rs`, `crates/alloy-provider/src/error.rs`, `crates/alloy-provider/src/builder.rs` | 2026-05-06 |
| `PROP-AP-006` | `cow-sdk-alloy-provider` | `ProviderError::class()` returns one of the five `ProviderErrorClass` values for every variant: validation, transport, remote, cancelled, and internal. Governed by [ADR 0035](docs/adr/0035-alloy-provider-adapter.md). | Contract | Yes | `crates/alloy-provider/tests/redaction_contract.rs::error_class_covers_every_variant`, `crates/alloy-provider/src/error.rs` | 2026-05-06 |
| `PROP-AP-007` | `cow-sdk-alloy-provider` | `RpcAlloyProvider::read_contract` ABI-encodes calldata with `alloy-dyn-abi`, dispatches through `eth_call`, decodes the response, returns the JSON value string required by `cow-sdk-core`, and fails malformed inputs without panicking. The canonical algorithm is also exposed through `__seam::execute_read_contract` so sibling adapter crates consume the same implementation without copying it. Governed by [ADR 0035](docs/adr/0035-alloy-provider-adapter.md). | Property | Yes | `crates/alloy-provider/tests/provider_contract.rs::read_contract_balance_of_happy_path`, `crates/alloy-provider/tests/read_contract_parity.rs`, `crates/alloy-provider/tests/read_contract_no_panic.rs`, `crates/alloy-provider/tests/seam_contract.rs::read_contract_path_propagates_validation_for_malformed_args_json`, `crates/alloy-provider/src/read_contract.rs`, `crates/alloy-provider/src/lib.rs` | 2026-05-26 |
| `PROP-AP-008` | `cow-sdk-alloy-provider` | The default SDK crate set continues to exclude `alloy-provider`, while the provider adapter declares no direct signer-family dependency and the resolved provider graph excludes `alloy-signer-local`. Governed by [ADR 0035](docs/adr/0035-alloy-provider-adapter.md). | Contract | Yes | `tests/alloy_provider_invariant_covers_every_published_crate.rs`, `scripts/policy-maintainer/src/check_alloy_provider_invariant.rs`, `crates/alloy-provider/tests/dependency_boundary_contract.rs` | 2026-05-06 |
| `PROP-AP-009` | `cow-sdk-alloy-provider` | Alloy provider receipt conversion populates `TransactionReceipt` status, block number, block hash, gas used, sender, and recipient from Alloy receipts, maps EIP-658 success and reverted states without coercing post-state roots, and preserves contract-creation receipts with no recipient. Governed by [ADR 0038](docs/adr/0038-transaction-lifecycle-types.md). | Contract | Yes | `crates/alloy-provider/tests/provider_contract.rs::get_transaction_receipt_populates_status_block_gas_from_to`, `crates/alloy-provider/src/conversion.rs::tests::alloy_to_cow_receipt_populates_status_success`, `crates/alloy-provider/src/conversion.rs::tests::alloy_to_cow_receipt_populates_status_reverted`, `crates/alloy-provider/src/conversion.rs::tests::alloy_to_cow_receipt_returns_none_status_for_post_state_receipt`, `crates/alloy-provider/src/conversion.rs::tests::alloy_to_cow_receipt_handles_contract_creation_no_to` | 2026-05-07 |
| `PROP-AP-CANCEL-001` | `cow-sdk-alloy-provider` | `ProviderError` carries a `Cancelled` variant and an `impl From<cow_sdk_core::Cancelled>` bridge so `Cancellable::cancel_with(...).await?` propagates cancellation through the provider error type. Governed by [ADR 0035](docs/adr/0035-alloy-provider-adapter.md). | Contract | Yes | `crates/alloy-provider/src/error.rs`, `crates/alloy-provider/tests/cancellation_contract.rs::cancel_with_propagates_cancelled_through_question_mark`, `docs/audit/cooperative-cancellation-contract-audit.md` | 2026-05-06 |
| `PROP-AP-010` | `cow-sdk-alloy-provider` | `RpcAlloyProviderBuilder` rejects malformed URLs without echoing the input, accepts the timeout setter on both transport-unset and HTTP-selected typestate orderings, and the typed-builder `Debug` impl redacts the configured URL through `Redacted<_>` while surfacing the configured timeout. The `TransportInit` error variant's display routes the underlying detail through `Redacted<String>`. Governed by [ADR 0035](docs/adr/0035-alloy-provider-adapter.md). | Contract | Yes | `crates/alloy-provider/tests/builder_contract.rs` | 2026-05-14 |
| `PROP-AP-011` | `cow-sdk-alloy-provider` | `ProviderErrorClass::as_str()` returns the stable lowercase label for every variant (`validation`, `transport`, `remote`, `cancelled`, `internal`) with `Display` forwarding through `as_str`; the documented `From<cow_sdk_core::Cancelled>` and `From<cow_sdk_core::CoreError>` lifts propagate cancellation and validation into the provider error without leaking detail; wiremock-driven RPC errors classify per the documented ladder (JSON-RPC error envelopes map to `Remote` with the explicit code, HTTP 5xx and malformed JSON map to non-validation non-cancellation transport classes). Governed by [ADR 0035](docs/adr/0035-alloy-provider-adapter.md). | Contract | Yes | `crates/alloy-provider/tests/error_class_contract.rs`, `crates/alloy-provider/tests/redaction_contract.rs` | 2026-05-14 |
| `PROP-AP-012` | `cow-sdk-alloy-provider` | The inter-adapter `__seam` contract is pinned through behaviour tests over `cow_block_tag_to_alloy` (named tags, hex numbers, decimal numbers, block-hash form, rejection of malformed inputs), `cow_request_to_alloy` (validated minimal request preserves `to`), `rpc_error_to_class_and_detail` (JSON-RPC error envelope classifies as `Remote`), and `execute_read_contract` (canonical read-contract algorithm shared with sibling adapter crates; validation variant propagates end-to-end), plus the `cow_log_query_to_alloy_filter` and `alloy_log_to_cow_raw_log` log conversions the umbrella's `LogProvider` implementation reuses (seam reachability pinned by a compile-time test). The seam also pins the storage-hex format equivalence between `alloy_primitives::B256::from(value).to_string()` and the legacy `format!("0x{:064x}")` width-64 layout. The seam stays `#[doc(hidden)]` and is exercised exclusively by sibling adapter crates per [ADR 0035](docs/adr/0035-alloy-provider-adapter.md). | Contract | Yes | `crates/alloy-provider/tests/seam_contract.rs`, `crates/alloy-provider/tests/read_contract_parity.rs` | 2026-05-26 |
| `PROP-AP-013` | `cow-sdk-alloy-provider` | `cow_log_query_to_alloy_filter` maps a core `LogQuery` onto the standard `eth_getLogs` filter — the address set, all four topic slots (topic-0 event signature plus indexed-argument slots 1-3, each an any-of set), and the `LogBlockSelector` number-range or block-hash selection — and `alloy_log_to_cow_raw_log` maps an Alloy log onto the full `RawLog` (address, topics-plus-data, reorg `removed` flag, and `LogMeta` block number/hash, optional block timestamp, transaction hash/index, log index). An indexed-address topic is built by `Hash32::from_indexed_address` (left-padded), so an "events for my address" query filters server-side. Governed by [ADR 0057](docs/adr/0057-log-provider-capability-trait.md). | Property | Yes | `crates/alloy-provider/src/conversion.rs::tests::cow_log_query_to_alloy_filter_maps_topics_addresses_and_block_hash`, `crates/alloy-provider/src/conversion.rs::tests::alloy_log_to_cow_raw_log_maps_address_meta_and_payload`, `crates/core/src/types/logs.rs::tests::builders_populate_addresses_and_topic_slots`, `crates/core/src/types/logs.rs::tests::from_indexed_address_left_pads_to_a_topic`, `docs/audit/log-provider-capability-audit.md` | 2026-06-03 |
| `PROP-AP-014` | `cow-sdk-alloy-provider`, `cow-sdk-alloy` | RPC retry is opt-in. By default `RpcAlloyProviderBuilder` (leaf) and `AlloyClientBuilder` (umbrella) issue each request once and surface a transient transport failure to the caller; passing a `RetryConfig` through `with_retry` wraps the JSON-RPC client in a bounded exponential backoff layer that transparently retries a rate-limited request up to the configured attempt count. The umbrella reuses the leaf's layer constructor through the doc-hidden seam so the policy and its internal compute-units budget stay defined once. Governed by [ADR 0035](docs/adr/0035-alloy-provider-adapter.md) and [ADR 0010](docs/adr/0010-runtime-neutral-async-and-transport-posture.md). | Contract | Yes | `crates/alloy-provider/tests/retry_contract.rs::default_provider_does_not_retry_a_rate_limited_read`, `crates/alloy-provider/tests/retry_contract.rs::with_retry_recovers_from_a_transient_rate_limit`, `crates/alloy-provider/src/retry.rs`, `crates/alloy/src/client.rs`, `docs/audit/alloy-provider-adapter-audit.md` | 2026-06-08 |
| `PROP-AS-001` | `cow-sdk-alloy-signer` | `LocalAlloyKeystoreSigner` exposes an SDK-owned documented API while upstream Alloy private-key signer values stay internal and redacted. The crate also exposes a `#[doc(hidden)] pub mod __seam` module re-exporting the EIP-712 typed-data conversion helpers (`cow_typed_data_payload_to_alloy`, `cow_flat_to_alloy_typed_data`) and the shared signature normalizer (`alloy_signature_to_hex`) so sibling adapter crates reuse the reviewed implementation without copying it; the seam is not part of the documented consumer API. Governed by [ADR 0036](docs/adr/0036-alloy-signer-adapter.md). | Public API | Yes | `crates/alloy-signer/src/lib.rs`, `crates/alloy-signer/src/signer.rs`, `crates/alloy-signer/src/conversion.rs`, `crates/alloy-signer/README.md`, `docs/audit/alloy-signer-adapter-audit.md` | 2026-05-26 |
| `PROP-AS-002` | `cow-sdk-alloy-signer` | `LocalAlloyKeystoreSigner` implements `cow_sdk_core::Signer` for address, EIP-191 message, canonical EIP-712 payload, and legacy flat typed-data signing while provider-backed transaction methods return `ProviderRequired`. Governed by [ADR 0036](docs/adr/0036-alloy-signer-adapter.md). | Contract | Yes | `crates/alloy-signer/src/signer.rs`, `crates/alloy-signer/tests/signer_contract.rs`, `crates/alloy-signer/tests/eip191_reference_vectors.rs`, `crates/alloy-signer/tests/eip712_reference_vectors.rs` | 2026-05-06 |
| `PROP-AS-003` | `cow-sdk-alloy-signer` | `LocalAlloyKeystoreSigner` does not implement `Provider` or `SigningProvider`; compile-fail tests enforce the capability boundary. Governed by [ADR 0036](docs/adr/0036-alloy-signer-adapter.md). | Contract | Yes | `crates/alloy-signer/tests/compile_fail.rs`, `crates/alloy-signer/tests/trybuild/no_provider.rs`, `crates/alloy-signer/tests/trybuild/no_signing_provider.rs` | 2026-05-27 |
| `PROP-AS-004` | `cow-sdk-alloy-signer` | `LocalAlloyKeystoreSignerBuilder::build` is callable only after private-key source and chain id have been selected, and externally constructed typestate markers cannot bypass the builder. Governed by [ADR 0036](docs/adr/0036-alloy-signer-adapter.md). | Contract | Yes | `crates/alloy-signer/src/builder.rs`, `crates/alloy-signer/tests/compile_fail.rs`, `crates/alloy-signer/tests/trybuild/external_marker_construction_fails.rs` | 2026-05-06 |
| `PROP-AS-005` | `cow-sdk-alloy-signer` | Canonical typed-data payload signing preserves the payload primary type, while the legacy flat typed-data path uses `Message` as a placeholder primary type and remains distinguishable by signature output. Governed by [ADR 0036](docs/adr/0036-alloy-signer-adapter.md). | Contract | Yes | `crates/alloy-signer/src/conversion.rs`, `crates/alloy-signer/tests/signer_contract.rs::sign_typed_data_payload_preserves_order_primary_type`, `crates/alloy-signer/tests/signer_contract.rs::sign_typed_data_flat_uses_message_placeholder_primary_type` | 2026-05-26 |
| `PROP-AS-006` | `cow-sdk-alloy-signer` | EIP-191 and EIP-712 signatures are normalized through `cow-sdk-contracts`, recover to the local signer, and stay covered by committed reference vectors plus property-based recovery checks. Governed by [ADR 0036](docs/adr/0036-alloy-signer-adapter.md). | Property | Yes | `crates/alloy-signer/src/conversion.rs`, `crates/alloy-signer/tests/eip191_reference_vectors.rs`, `crates/alloy-signer/tests/eip712_reference_vectors.rs`, `crates/alloy-signer/tests/proptests.rs`, `docs/audit/ecdsa-signature-normalization-audit.md` | 2026-05-06 |
| `PROP-AS-007` | `cow-sdk-alloy-signer` | `SignerError::class()` returns one of the six signer classes for every variant, and validation, signing, internal, and private-key parsing details are redacted before public formatting. Governed by [ADR 0036](docs/adr/0036-alloy-signer-adapter.md). | Contract | Yes | `crates/alloy-signer/src/error.rs`, `crates/alloy-signer/tests/redaction_contract.rs`, `crates/alloy-signer/src/builder.rs` | 2026-05-06 |
| `PROP-AS-CANCEL-001` | `cow-sdk-alloy-signer` | `SignerError` carries a `Cancelled` variant and an `impl From<cow_sdk_core::Cancelled>` bridge so `Cancellable::cancel_with(...).await?` propagates cancellation through the signer error type. Governed by [ADR 0036](docs/adr/0036-alloy-signer-adapter.md). | Contract | Yes | `crates/alloy-signer/src/error.rs`, `crates/alloy-signer/tests/cancellation_contract.rs::cancel_with_propagates_cancelled_through_question_mark`, `docs/audit/cooperative-cancellation-contract-audit.md` | 2026-05-06 |
| `PROP-AS-008` | `cow-sdk-alloy-signer` | `cow_typed_data_payload_to_alloy` resolves struct-typed fields declared in the type map, directly or as an array (for example `Call[]`), so nested multi-type EIP-712 payloads convert and produce a signing digest byte-identical to the macro-emitted `SolStruct` envelope, while a field referencing a struct that is not declared in the type map stays fail-closed. Governed by [ADR 0036](docs/adr/0036-alloy-signer-adapter.md). | Contract | Yes | `crates/alloy-signer/src/conversion.rs`, `crates/alloy-signer/src/conversion.rs::tests::nested_struct_payload_matches_macro_digest`, `crates/alloy-signer/src/conversion.rs::tests::undeclared_struct_reference_is_rejected` | 2026-06-03 |
| `PROP-AU-001` | `cow-sdk-alloy` | `AlloyClient` exposes an SDK-owned composed native API while upstream Alloy provider, wallet, transport, and local signer values stay private and redacted. Governed by [ADR 0037](docs/adr/0037-alloy-umbrella-adapter.md). | Public API | Yes | `crates/alloy/src/lib.rs`, `crates/alloy/src/client.rs`, `crates/alloy/src/handle.rs`, `crates/alloy/README.md`, `docs/audit/alloy-umbrella-adapter-audit.md` | 2026-05-26 |
| `PROP-AU-002` | `cow-sdk-alloy` | `AlloyClient` implements every method on `Provider` and implements `SigningProvider` by returning an owned `AlloyClientSignerHandle`. Governed by [ADR 0037](docs/adr/0037-alloy-umbrella-adapter.md). | Contract | Yes | `crates/alloy/tests/provider_contract.rs`, `crates/alloy/tests/signing_provider_contract.rs::create_signer_returns_owned_handle`, `tests/alloy_umbrella_composition.rs::alloy_client_satisfies_trading_sdk_boundaries` | 2026-05-26 |
| `PROP-AU-003` | `cow-sdk-alloy` | `AlloyClientSignerHandle` remains usable after the parent `AlloyClient` is dropped, while the client does not implement `Signer` and the handle does not implement `Provider`. Governed by [ADR 0037](docs/adr/0037-alloy-umbrella-adapter.md). | Contract | Yes | `crates/alloy/tests/handle_survives_drop.rs::signer_handle_remains_usable_after_parent_client_drop`, `crates/alloy/tests/compile_fail.rs::public_surface_rejects_signer_on_client_and_provider_on_handle`, `crates/alloy/tests/trybuild/no_signer_on_client.rs`, `crates/alloy/tests/trybuild/no_provider_on_handle.rs` | 2026-05-27 |
| `PROP-AU-004` | `cow-sdk-alloy` | The umbrella signer handle preserves typed-data primary types, normalizes ECDSA signatures, submits transactions through the Alloy wallet-filler provider, returns `TransactionBroadcast` with the broadcast hash read through `*pending.tx_hash()` without waiting for confirmation, estimates gas through the provider, rejects raw `sign_transaction` without dispatching HTTP, and delegates rich receipt lookup to the provider adapter. Governed by [ADR 0037](docs/adr/0037-alloy-umbrella-adapter.md) and [ADR 0038](docs/adr/0038-transaction-lifecycle-types.md). | Contract | Yes | `crates/alloy/tests/eip712_reference_vectors.rs::sign_typed_data_payload_preserves_primary_type`, `crates/alloy/tests/no_broadcast_for_sign_transaction.rs::sign_transaction_returns_unsupported_without_http_request`, `crates/alloy/tests/send_transaction_does_not_wait_for_confirmation.rs::send_transaction_does_not_dispatch_get_transaction_receipt`, `crates/alloy/tests/provider_contract.rs::get_transaction_receipt_populates_rich_fields_from_alloy_receipt`, `tests/alloy_umbrella_composition.rs::alloy_client_satisfies_trading_sdk_boundaries`, `tests/transaction_lifecycle_cross_adapter_invariant.rs`, `examples/native/scenarios/alloy_trading_full_flow.rs` | 2026-05-26 |
| `PROP-AU-CANCEL-001` | `cow-sdk-alloy` | `AlloyClientError` carries a `Cancelled` variant and an `impl From<cow_sdk_core::Cancelled>` bridge so `Cancellable::cancel_with(...).await?` propagates cancellation through the composed adapter error type. Governed by [ADR 0037](docs/adr/0037-alloy-umbrella-adapter.md). | Contract | Yes | `crates/alloy/src/error.rs`, `crates/alloy/tests/cancellation_contract.rs::cancel_with_propagates_cancelled_through_question_mark`, `docs/audit/cooperative-cancellation-contract-audit.md` | 2026-05-06 |
| `PROP-AU-005` | `cow-sdk-alloy` | `AlloyClientBuilder` rejects every documented malformed input (invalid URL, invalid prefixed and bare-hex private keys, all-zero key bytes, curve-order key) without echoing the offending value, the chain-mismatch detection in `build_checked` carries both configured and remote chain ids in its display, the `From<AlloyClientError>` lift propagates transparently into the `Client` variant, and the fully-typed builder `Debug` impl redacts key bytes while surfacing the chain id. Governed by [ADR 0037](docs/adr/0037-alloy-umbrella-adapter.md). | Contract | Yes | `crates/alloy/tests/builder_contract.rs` | 2026-05-14 |
| `PROP-AU-006` | `cow-sdk-alloy` | `AlloyClientError::class()` returns one of the eight `AlloyClientErrorClass` values for every variant with stable lowercase label forwarding through `Display`, and the documented `From<ProviderError>` lift maps every provider-error variant (validation, transport, remote, cancelled, internal) into the composed adapter's typed surface without leaking detail. Governed by [ADR 0037](docs/adr/0037-alloy-umbrella-adapter.md). | Contract | Yes | `crates/alloy/tests/error_contract.rs`, `crates/alloy/tests/redaction_contract.rs` | 2026-05-14 |
| `PROP-AU-007` | `cow-sdk-alloy` | `AlloyClient::read_contract` consumes the provider leaf's `execute_read_contract` through the doc-hidden inter-crate seam, lifts `ProviderError` into `AlloyClientError` through the existing `From` impl, and surfaces `AlloyClientError::Validation` end-to-end for malformed inputs (invalid ABI type, wrong argument count, type mismatches, length mismatches, null arguments, object-for-address, overloaded functions) without panicking. Every documented scalar (`uint256`, `int256`, `bool`, `string`, `bytes`, `bytes32`, `address`, `uint8`, `uint64`), every compound shape (dynamic arrays, fixed arrays, multi-output tuples, address arrays), and every documented argument-shape variant (JSON array, JSON object with named params, single scalar form) flows through this single path. Governed by [ADR 0037](docs/adr/0037-alloy-umbrella-adapter.md). | Contract | Yes | `crates/alloy/tests/read_contract_contract.rs`, `crates/alloy/src/client.rs`, `crates/alloy-provider/src/read_contract.rs`, `crates/alloy-provider/src/lib.rs`, `tests/alloy_read_contract_parity_invariant.rs` | 2026-05-26 |
| `PROP-AU-008` | `cow-sdk-alloy` | `AlloyClient` implements `LogProvider`, issuing a single bounded `eth_getLogs` over the composed Alloy provider and reusing the provider leaf's `LogQuery` → filter and Alloy-log → `RawLog` conversions through the doc-hidden inter-crate seam, so an event-log fetch needs no second provider for the same RPC endpoint. Governed by [ADR 0037](docs/adr/0037-alloy-umbrella-adapter.md) and [ADR 0057](docs/adr/0057-log-provider-capability-trait.md). | Contract | Yes | `crates/alloy/tests/log_provider_contract.rs::alloy_client_implements_log_provider_and_returns_typed_error_on_unreachable_rpc`, `crates/alloy/src/client.rs`, `crates/alloy-provider/tests/seam_contract.rs::seam_exposes_log_conversions_for_the_umbrella`, `docs/audit/log-provider-capability-audit.md` | 2026-06-03 |
| `PROP-ORD-001` | `cow-sdk-orderbook` | Request builders preserve explicit field shape, pagination defaults, and per-form `appData` transport (hash-only, document-only, and document-plus-hash) without silently coercing unsupported inputs. | Invariant | Yes | `crates/orderbook/tests/invariant_contract.rs`, `crates/orderbook/tests/request_contract.rs`, `crates/orderbook/tests/api_contract.rs`, `crates/orderbook/tests/types_contract.rs` | 2026-05-30 |
| `PROP-ORD-002` | `cow-sdk-orderbook` | Response decoding, retry termination, `Retry-After` cooldown honor on `429` and `503`, and transform layers fail closed on malformed upstream payloads. | Invariant | Yes | `crates/orderbook/tests/invariant_contract.rs`, `crates/orderbook/tests/transform_contract.rs`, `crates/orderbook/tests/types_contract.rs`, `crates/orderbook/tests/request_contract.rs`, `crates/orderbook/tests/api_contract.rs::service_unavailable_retry_after_header_delays_retry_for_at_least_server_cooldown`, `crates/transport-policy/tests/policy_contract.rs::retry_after_only_affects_rate_limit_and_unavailable_statuses` | 2026-05-28 |
| `PROP-TRD-001` | `cow-sdk-trading` | Quote and post context precedence remains explicit and deterministic across builder defaults, quote-request overrides, derived quote-to-order parameters, receiver fallback, and collision-driven order-id retries. | Invariant | Yes | `crates/trading/tests/invariant_contract.rs`, `crates/trading/tests/sdk_contract.rs`, `crates/trading/tests/quote_contract.rs::order_id_collision_retries_with_new_salt_until_success_or_cap`, `crates/trading/tests/quote_contract.rs`, `crates/trading/tests/post_contract.rs`, `crates/trading/tests/order_contract.rs::order_to_sign_receiver_falls_back_to_from_when_zero_or_unset`, `crates/trading/tests/order_contract.rs` | 2026-05-26 |
| `PROP-TRD-002` | `cow-sdk-trading` | Slippage outputs, protocol-fee sanitization, and partner-fee extraction remain explicit, monotonic, and clamped across valid inputs. | Invariant | Yes | `crates/trading/tests/invariant_contract.rs`, `crates/trading/tests/slippage_contract.rs::slippage_bps_clamps_to_expected_bounds`, `crates/trading/tests/slippage_contract.rs::slippage_clamps_at_eth_flow_default_for_eth_flow_orders`, `crates/trading/tests/slippage_contract.rs`, `fuzz/fuzz_targets/fuzz_slippage_policy_helpers.rs` | 2026-06-03 |
| `PROP-TRD-003` | `cow-sdk-trading` | On-chain helper builders preserve unsigned `uint256` calldata boundary semantics, and the quote-amount slippage breakdown rejects protocol-fee values at or above 100% with a typed `InvalidInput` error rather than panicking on the sell path through a `BigInt` divide-by-zero. | Invariant | Yes | `crates/trading/tests/invariant_contract.rs`, `crates/trading/tests/onchain_contract.rs`, `crates/trading/src/slippage/breakdown.rs`, `fuzz/fuzz_targets/fuzz_slippage_amounts.rs` | 2026-05-26 |
| `PROP-SBG-001` | `cow-sdk-subgraph` | Query requests preserve explicit operation-name handling plus nested variable object and array shape. | Invariant | Yes | `crates/subgraph/tests/query_contract.rs::query_request_preserves_nested_variable_objects_and_arrays`, `crates/subgraph/tests/query_contract.rs::multi_operation_document_requires_explicit_operation_name`, `crates/subgraph/tests/query_contract.rs::query_documents_open_response_dto_tolerates_unknown_extra_fields`, `crates/subgraph/tests/query_contract.rs`, `crates/subgraph/tests/api_contract.rs` | 2026-05-01 |
| `PROP-SBG-002` | `cow-sdk-subgraph` | Typed response decoding accepts equivalent string-or-number scalar forms and fails closed on malformed, non-finite, overflowed, missing, or GraphQL-error-response data. | Invariant | Yes | `crates/subgraph/tests/types_contract.rs::subgraph_graphql_error_decodes_the_graph_wire_shape`, `crates/subgraph/tests/types_contract.rs::scalar_decode_rejects_non_finite_floats_and_overflows`, `crates/subgraph/tests/types_contract.rs`, `crates/subgraph/tests/api_contract.rs` | 2026-05-31 |
| `PROP-BWL-001` | `cow-sdk-browser-wallet` | Ambiguous discovery never silently auto-selects a provider, and multi-provider iteration remains deterministic for explicit user selection. | State machine | Yes | `crates/browser-wallet/src/wallet/discovery.rs`, `crates/browser-wallet/tests/wallet_contract.rs::eip6963_provider_iteration_order_matches_documented_rule`, `crates/browser-wallet/tests/wallet_contract.rs` | 2026-05-13 |
| `PROP-BWL-002` | `cow-sdk-browser-wallet` | Session, chain, typed-data, and typed RPC classification boundaries stay explicit under deterministic transports. | State machine | Yes | `crates/browser-wallet/tests/state_machine_contract.rs::malformed_json_rpc_error_classifies_as_typed_provider_error`, `crates/browser-wallet/tests/state_machine_contract.rs`, `crates/browser-wallet/tests/provider_contract.rs`, `crates/browser-wallet/tests/wallet_contract.rs`, `crates/browser-wallet/tests/wasm_bridge_contract.rs::mock_wallet_console_state_machine_is_deterministic`, `crates/browser-wallet/tests/wasm_bridge_contract.rs::eip6963_discovery_event_serde_roundtrip`, `crates/browser-wallet/src/provider/provider_impl.rs (tests)`, `crates/browser-wallet/src/error.rs (tests)`, `fuzz/fuzz_targets/fuzz_rpc_error_payload_serde.rs`, `fuzz/fuzz_targets/fuzz_contract_call_serde.rs`, `fuzz/fuzz_targets/fuzz_transaction_request_serde.rs` | 2026-05-13 |
| `PROP-SDK-001` | `cow-sdk` | The facade remains curated and feature-gated, without widening the default surface beyond leaf-crate ownership. | Public API | Yes | `crates/sdk/tests/public_api.rs`, `crates/sdk/tests/public_api_default_features_only.rs`, `crates/sdk/tests/public_api_with_all_features.rs`, `crates/sdk/tests/fixtures/public_api_default_features_only.snap`, `crates/sdk/tests/fixtures/public_api_with_all_features.snap`, `crates/sdk/tests/ui/orderbook_client_reachable_through_trading_re_export.rs` | 2026-05-31 |
| `PROP-SDK-002` | `cow-sdk` | `CowError::class()` maps an orderbook response that signalled HTTP 429 after the transport retry budget was exhausted to `ErrorClass::RateLimited` on both the recognised-rejection (`Rejected`) and unparsed-envelope (`Api`) paths, distinct from the generic `ErrorClass::Remote` used for other structured responses; the transport layer already retries 429s with `Retry-After` honoring, so the class is an exhausted-retry telemetry signal rather than a control-flow hook. | Contract | Yes | `crates/sdk/tests/error_class_contract.rs::exhausted_retry_429_classifies_as_rate_limited`, `crates/sdk/tests/error_class_contract.rs::non_429_remote_responses_stay_remote`, `crates/sdk/src/lib.rs` | 2026-05-31 |
| `PROP-SDK-003` | `cow-sdk` | Every public error type the facade aggregates exposes a `class() -> ErrorClass` accessor (`CoreError`, `AppDataError`, `SigningError`, `ContractsError`, `OrderbookError`, `TradingError`, `BrowserWalletError`, and `SubgraphError` behind the off-by-default `subgraph` feature); `CowError::class()` delegates to them and composite errors delegate to the wrapped error so wrapped granularity (a wrapped 429 staying `ErrorClass::RateLimited`) is preserved. `ErrorClass` is defined in `cow-sdk-core` and re-exported from `cow-sdk`. Governed by [ADR 0060](docs/adr/0060-uniform-error-classification.md). | Contract | Yes | `crates/sdk/tests/error_class_contract.rs::error_class_partitions_every_bucket`, `crates/sdk/tests/error_class_contract.rs::error_class_delegates_through_trading_and_facade`, `crates/sdk/tests/error_class_contract.rs::subgraph::subgraph_error_class_partitions_every_bucket` (with `--features subgraph`), `crates/contracts/tests/error_contract.rs::class_partitions_validation_internal_and_signing`, `crates/core/src/errors.rs`, `crates/contracts/src/errors.rs`, `crates/subgraph/src/error.rs`, `crates/sdk/src/lib.rs` | 2026-06-07 |
| `PROP-SDK-004` | `cow-sdk` | `OrderbookError` exposes `is_retryable() -> bool` and `backoff_hint() -> Option<Duration>`; `is_retryable` keys off the retained HTTP status (the `PROP-TPP-008` retryable set) for structured responses and the transient `TransportErrorClass` mapping for transport failures, while `backoff_hint` surfaces the `Retry-After` parsed from the failing response (delta-seconds or HTTP-date) when present. `TradingError` and `CowError` delegate both accessors to the wrapped orderbook error and return `false` / `None` for every non-orderbook variant. Governed by [ADR 0060](docs/adr/0060-uniform-error-classification.md). | Contract | Yes | `crates/orderbook/src/error.rs` (retry-classification unit tests), `crates/sdk/tests/error_class_contract.rs::is_retryable_delegates_through_trading_and_facade`, `crates/sdk/tests/error_class_contract.rs::backoff_hint_delegates_through_trading_and_facade`, `crates/transport-policy/src/retry_after.rs` | 2026-06-06 |
| `PROP-CORE-007` | `cow-sdk-core` | `HttpTransport` is dyn-compatible, typed failures flow through `TransportError` with a `TransportErrorClass` partition, every default adapter strips the URL before wrapping so credential-bearing query strings never surface through `Debug` or `Display`, `sanitize_public_base_url` strips path, query, fragment, and credentials before URL identity crosses a diagnostic or tracing boundary, and retry/backoff policy remains documented at the orderbook layer rather than on the raw transport trait. Governed by [ADR 0013](docs/adr/0013-http-transport-injection-and-typestate-builders.md). | Contract | Yes | `crates/core/tests/transport_contract.rs::sanitize_public_base_url_strips_private_url_parts`, `crates/core/tests/transport_contract.rs::sanitize_public_base_url_strips_userinfo_credentials`, `crates/core/tests/transport_contract.rs::sanitize_public_base_url_strips_userinfo_without_password`, `crates/core/tests/transport_contract.rs`, `crates/transport-wasm/tests/parity_contract.rs`, `crates/core/src/transport/http.rs`, `crates/core/src/transport/reqwest.rs`, `docs/transport.md` | 2026-05-04 |
| `PROP-CON-003` | `cow-sdk-contracts` | Every ABI binding is authored as an inline `alloy::sol!` interface in `cow-sdk-contracts` and preserves byte-identity on the encoded call-data and hashed payloads of `GPv2Settlement`, `CoWSwapEthFlow`, `CoWSwapOnchainOrders` events, `IWrappedNativeToken`, and `IERC20` / `IERC20Permit`, proven against TypeScript-SDK-derived call-data fixtures pinned by upstream commit in `parity/source-lock.yaml`. Governed by [ADR 0012](docs/adr/0012-alloy-sol-bindings-and-registry-authority.md). | Contract | Yes | `crates/contracts/tests/parity_contract.rs::parity_fixture_cases_hold`, `crates/contracts/tests/parity_contract.rs` | 2026-06-08 |
| `PROP-CON-004` | `cow-sdk-contracts` | `Registry` resolves every deployed contract address from the typed `(ContractId, SupportedChainId, CowEnv)` triple; the embedded TOML manifest is validated at compile time, and the runtime parser surfaces every failure class as a typed `RegistryError`. Governed by [ADR 0012](docs/adr/0012-alloy-sol-bindings-and-registry-authority.md). | Contract | Yes | `crates/contracts/tests/registry.rs::registry_address_lookup_matrix_is_exhaustive`, `crates/contracts/tests/registry.rs`, `crates/contracts/tests/schema_v2_rejection.rs` | 2026-06-08 |
| `PROP-CON-005` | `cow-sdk-contracts` | Every ECDSA signature returned by `cow_sdk_contracts::RecoverableSignature` carries a canonical recovery byte in the legacy `{27, 28}` range. Inputs with `v ∈ {0, 1}` are canonicalized; any other byte (including the EIP-155 chain-encoded range `35..=255`) is rejected with `ContractsError::InvalidSignatureRecoveryByte`. Governed by [ADR 0022](docs/adr/0022-ecdsa-signature-v-normalization.md). | Contract | Yes | `crates/contracts/tests/signature_contract.rs`, `crates/contracts/tests/recoverable_signature_contract.rs`, `crates/contracts/tests/v_normalization_contract.rs`, `crates/contracts/tests/property_contract.rs::ecdsa_v_normalization_rejects_every_excluded_byte_value`, `parity/fixtures/ecdsa/v_normalization.json`, `fuzz/fuzz_targets/fuzz_ecdsa_v_normalization.rs`, `fuzz/fuzz_targets/fuzz_recoverable_signature_differential.rs`, `fuzz/fuzz_targets/fuzz_recoverable_signature_parse_hex.rs`, `fuzz/fuzz_targets/fuzz_eip1271_signature_data_codec.rs` | 2026-05-28 |
| `PROP-CON-006` | `cow-sdk-contracts` | Contracts-crate digest computation flows directly through the concrete `cow_sdk_core::OrderData`; the legacy compatibility shims that zeroed digest inputs are removed from the public surface, and the contracts crate exposes no order type of its own. Governed by [ADR 0023](docs/adr/0023-legacy-compatibility-shim-removal.md) and [ADR 0059](docs/adr/0059-hash-concrete-orderdata-directly.md). | Contract | Yes | `crates/contracts/tests/order_contract.rs`, `crates/contracts/tests/order_digest_parity_contract.rs` | 2026-05-30 |
| `PROP-CON-007` | `cow-sdk-contracts` | The order UID pack/unpack fuzz target carries a documented seed-corpus contract anchored to parity fixture data plus edge-shaped 56-byte triples, so fuzzing starts from known codec boundaries instead of random-only inputs. | Contract | Yes | `fuzz/fuzz_targets/fuzz_order_uid_pack_unpack.rs`, `parity/fixtures/contracts.json` | 2026-04-24 |
| `PROP-SIG-003` | `cow-sdk-signing` | `verify_eip1271_signature_cached` takes a mandatory `Eip1271VerificationCache` parameter, keys the cache on the full `(verifier, digest, signature_hash)` probe identity, and records only `Ok(())` outcomes (positive-only); a magic-value mismatch and every other error class re-hit the chain. Governed by [ADR 0014](docs/adr/0014-eip1271-verification-cache.md). | Contract | Yes | `crates/contracts/tests/signature_contract.rs::async_eip1271_verification_records_only_valid_outcomes_keyed_by_signature`, `crates/signing/tests/eip1271_cache_contract.rs::cache_skips_every_non_cacheable_error_class`, `crates/signing/tests/eip1271_cache_contract.rs::verify_cached_does_not_serve_a_cached_valid_for_a_different_signature`, `crates/signing/tests/eip1271_cache_contract.rs::verify_cached_never_records_a_mismatch`, `crates/signing/tests/ui.rs::eip1271_error_match_requires_wildcard`, `crates/signing/tests/ui/eip1271_error_match_requires_wildcard.rs` | 2026-05-28 |
| `PROP-SIG-004` | `cow-sdk-signing` | `InMemoryEip1271VerificationCache` uses an injectable browser-safe time source on `wasm32-unknown-unknown`, so cache misses, writes, and TTL boundaries remain deterministic while preserving the native `std::time::Instant` contract elsewhere. Governed by [ADR 0014](docs/adr/0014-eip1271-verification-cache.md). | Contract | Yes | `crates/signing/tests/eip1271_cache_contract.rs::cache_ttl_boundary_holds_at_minus_one_and_misses_at_plus_one`, `crates/signing/tests/wasm_cache_contract.rs::cache_ttl_boundary_holds_at_minus_one_and_misses_at_plus_one_on_wasm32`, `.github/workflows/wasm.yml` | 2026-05-01 |
| `PROP-SIG-008` | `cow-sdk-signing` | The `Eip1271VerificationCache` trait and the dependency-free `NoopEip1271VerificationCache` are always available, while `InMemoryEip1271VerificationCache` (and the `parking_lot` and `web-time` dependencies it needs) is gated behind the opt-in, default-off `in-memory-cache` feature, which the `cow-sdk` facade forwards under its own matching feature. Governed by [ADR 0014](docs/adr/0014-eip1271-verification-cache.md). | Public API | Yes | `crates/sdk/tests/public_api_default_features_only.rs::public_api_default_features_only_snapshot_matches`, `crates/signing/Cargo.toml`, `crates/sdk/Cargo.toml` | 2026-05-28 |
| `PROP-SIG-005` | `cow-sdk-signing` | The typed-data digest fuzz target carries a documented seed-corpus contract anchored to the EIP-712 order-digest parity vectors plus byte-pattern boundaries, so EIP-712 digest fuzzing starts from deterministic signed-order shapes. | Contract | Yes | `fuzz/fuzz_targets/fuzz_typed_data_digest.rs`, `parity/fixtures/eip712/order_digests.json` | 2026-05-01 |
| `PROP-ORD-003` | `cow-sdk-orderbook` | `OrderbookApi::builder()` is the sole production construction path; the typestate encodes the required inputs (chain, environment, transport) at compile time and no free-function constructor remains; `trybuild` compile-fail witnesses assert `.build()` is unreachable until the chain id and environment are supplied. Governed by [ADR 0013](docs/adr/0013-http-transport-injection-and-typestate-builders.md). | Contract | Yes | `crates/orderbook/tests/builder_contract.rs`, `crates/orderbook/tests/ui/build_without_chain.rs`, `crates/orderbook/tests/ui/build_without_environment.rs`, `crates/orderbook/tests/ui/build_on_empty_builder.rs` | 2026-06-02 |
| `PROP-ORD-004` | `cow-sdk-orderbook` | The `cow_sdk_contracts::SigningScheme` and `cow_sdk_orderbook::SigningScheme` enums maintain a variant-by-variant typed bridge: every contracts variant maps to exactly one orderbook variant and back through bidirectional `From` impls; the cancellation-only `EcdsaSigningScheme` narrowing is `From`-embedded into both parent enums; and the fallible inverse `TryFrom<SigningScheme> for EcdsaSigningScheme` rejects `Eip1271` and `PreSign` with a typed `SigningSchemeNotEcdsa` error rather than silently dropping data. The two parent enums are kept distinct because their wire serde shapes diverge: `cow_sdk_contracts::SigningScheme` uses the protocol `#[repr(u8)]` discriminant and default PascalCase string serde, while `cow_sdk_orderbook::SigningScheme` uses `#[serde(rename_all = "lowercase")]` to match the orderbook HTTP wire JSON. | Contract | Yes | `crates/orderbook/tests/signing_scheme_bridge_contract.rs`, `crates/orderbook/src/types/enums.rs`, `crates/contracts/src/signature.rs` | 2026-05-16 |
| `PROP-SBG-003` | `cow-sdk-subgraph` | `SubgraphApi::builder()` is the sole production construction path; the typestate encodes the required inputs (chain, API key, transport) at compile time; `trybuild` compile-fail witnesses assert `.build()` is unreachable until the chain id and API key are supplied, and on `wasm32` the default-transport `build()` is `cfg`-gated off so a browser caller must inject a `FetchTransport` before `.build()` is reachable. Governed by [ADR 0013](docs/adr/0013-http-transport-injection-and-typestate-builders.md). | Contract | Yes | `crates/subgraph/tests/builder_contract.rs`, `crates/subgraph/tests/ui/build_without_chain.rs`, `crates/subgraph/tests/ui/build_without_api_key.rs`, `crates/subgraph/tests/ui/build_on_empty_builder.rs`, `crates/subgraph/src/builder.rs`, `.github/workflows/wasm.yml` | 2026-06-02 |
| `PROP-SBG-004` | `cow-sdk-subgraph` | `SubgraphError::Transport` and the build-time `SubgraphError::TransportConfiguration` each carry a typed `TransportErrorClass` alongside redacted transport details, so subgraph callers can classify failures without parsing rendered error text and credential-bearing content stays stripped before display. Governed by [ADR 0013](docs/adr/0013-http-transport-injection-and-typestate-builders.md). | Contract | Yes | `crates/subgraph/tests/error_contract.rs::transport_variant_carries_typed_class_and_sanitized_detail`, `crates/subgraph/tests/api_contract.rs::transport_failures_surface_typed_context`, `crates/sdk/tests/error_redaction_contract.rs::subgraph_errors_and_contexts_redact_serialized_request_payloads`, `crates/subgraph/src/error.rs` | 2026-05-31 |
| `PROP-TPP-001` | `cow-sdk-transport-policy` | The default orderbook transport policy keeps its reviewed user-agent, timeout, retry attempt count, limiter budget, and per-host scope stable. Governed by [ADR 0041](docs/adr/0041-transport-policy-l3-layering.md). | Contract | Yes | `crates/transport-policy/tests/policy_contract.rs::prop_tpp_001_default_orderbook_transport_policy_is_stable` | 2026-05-08 |
| `PROP-TPP-002` | `cow-sdk-transport-policy` | The default subgraph transport policy keeps its reviewed user-agent, timeout, retry attempt count, limiter budget, and per-host scope stable. Governed by [ADR 0041](docs/adr/0041-transport-policy-l3-layering.md). | Contract | Yes | `crates/transport-policy/tests/policy_contract.rs::prop_tpp_002_default_subgraph_transport_policy_is_stable` | 2026-05-08 |
| `PROP-TPP-003` | `cow-sdk-transport-policy` | `RetryPolicy::no_retry` is deterministic and performs only the first attempt. Governed by [ADR 0041](docs/adr/0041-transport-policy-l3-layering.md). | Contract | Yes | `crates/transport-policy/tests/policy_contract.rs::prop_tpp_003_no_retry_policy_is_idempotent` | 2026-05-08 |
| `PROP-TPP-004` | `cow-sdk-transport-policy` | Decorrelated retry jitter stays bounded by `RetryPolicy::max_delay`, and the `Retry-After` IMF-fixdate civil-day arithmetic stays panic-free under any attacker-controlled year value through `i64` promotion. Governed by [ADR 0041](docs/adr/0041-transport-policy-l3-layering.md). | Contract | Yes | `crates/transport-policy/tests/policy_contract.rs::prop_tpp_004_decorrelated_jitter_is_bounded_by_max_delay`, `crates/transport-policy/src/retry_after.rs`, `fuzz/fuzz_targets/fuzz_parse_retry_after.rs`, `fuzz/fuzz_targets/fuzz_retry_policy_delay.rs`, `fuzz/fuzz_targets/fuzz_jitter_delay_for_attempt.rs` | 2026-05-14 |
| `PROP-TPP-005` | `cow-sdk-transport-policy` | Per-host rate limiting keys buckets by `Url::host_str`, so requests to the same host share a bucket while different hosts do not. Governed by [ADR 0041](docs/adr/0041-transport-policy-l3-layering.md). | Contract | Yes | `crates/transport-policy/tests/policy_contract.rs::prop_tpp_005_request_rate_limiter_uses_host_keys_for_per_host_scope` | 2026-05-08 |
| `PROP-TPP-006` | `cow-sdk-transport-policy` | The optional reqwest classifier maps every observed `reqwest::Error` shape into a bounded `NetworkErrorKind` without panicking. Governed by [ADR 0041](docs/adr/0041-transport-policy-l3-layering.md). | Contract | Yes | `crates/transport-policy/tests/policy_contract.rs::prop_tpp_006_reqwest_error_classifier_is_total` | 2026-05-08 |
| `PROP-TPP-007` | `cow-sdk-transport-policy` | Target-neutral retry sleep returns within the documented short-delay accuracy bound. Governed by [ADR 0041](docs/adr/0041-transport-policy-l3-layering.md). | Contract | Yes | `crates/transport-policy/tests/policy_contract.rs::prop_tpp_007_cross_target_sleep_accuracy_is_bounded` | 2026-05-08 |
| `PROP-TPP-008` | `cow-sdk-transport-policy` | The public retryable status list is exactly `408`, `425`, `429`, `500`, `502`, `503`, and `504`, and `is_retryable_status` matches it. Governed by [ADR 0041](docs/adr/0041-transport-policy-l3-layering.md). | Contract | Yes | `crates/transport-policy/tests/policy_contract.rs::prop_tpp_008_retryable_status_list_is_complete` | 2026-05-08 |
| `PROP-TPP-009` | `cow-sdk-transport-policy` | `NetworkErrorKind::from_transport_error_class` is total across every `TransportErrorClass` variant, including the `Redirect` and `Upgrade` variants which fall through the wildcard arm to `NetworkErrorKind::Other`. The optional `reqwest-classifier` feature maps real `reqwest::Error` shapes (builder, request, connect, timeout, status) into the same partition. Governed by [ADR 0041](docs/adr/0041-transport-policy-l3-layering.md). | Contract | Yes | `crates/transport-policy/tests/classify_contract.rs::network_error_kind_mapping_round_trip_is_total`, `crates/transport-policy/tests/classify_contract.rs::reqwest_classifier::reqwest_classifier_maps_invalid_url_to_builder_or_request`, `crates/transport-policy/tests/classify_contract.rs::reqwest_classifier::reqwest_classifier_maps_unreachable_host_to_connect_or_timeout`, `crates/transport-policy/tests/classify_contract.rs::reqwest_classifier::reqwest_classifier_maps_status_500_to_http_status` | 2026-05-14 |
| `PROP-TPP-010` | `cow-sdk-transport-policy` | Every documented `TransportPolicy` constructor (`new`, `default_orderbook`, `default_subgraph`, `default_trading`, `default_ipfs`), every `with_*` setter, and the `TransportPolicyBuilder` round-trip preserve their reviewed user-agent, retry, rate-limit, timeout, and tracing fields. Governed by [ADR 0041](docs/adr/0041-transport-policy-l3-layering.md). | Contract | Yes | `crates/transport-policy/tests/policy_contract.rs::prop_tpp_009_explicit_constructor_disables_tracing_and_preserves_parts`, `crates/transport-policy/tests/policy_contract.rs::prop_tpp_010_default_trading_uses_trading_user_agent_and_orderbook_limiter`, `crates/transport-policy/tests/policy_contract.rs::prop_tpp_011_default_ipfs_disables_retry_and_timeout_and_uses_unlimited_limiter`, `crates/transport-policy/tests/policy_contract.rs::prop_tpp_012_with_setters_replace_only_their_targeted_field`, `crates/transport-policy/tests/policy_contract.rs::prop_tpp_013_user_agent_validation_error_is_transparent_through_build_error`, `crates/transport-policy/tests/policy_contract.rs::prop_tpp_014_builder_round_trip_preserves_every_setter`, `crates/transport-policy/tests/policy_contract.rs::prop_tpp_015_builder_defaults_to_orderbook_user_agent_when_unset` | 2026-05-14 |
| `PROP-TPP-011` | `cow-sdk-transport-policy` | Every `JitterStrategy` variant (`None`, `Full`, `Equal`, `Decorrelated`) yields a delay within the documented `[0, max_delay]` window; `None` returns the capped base delay unchanged; `Equal` preserves at least half the capped base delay; the zero-window case returns `Duration::ZERO`; `JitterStrategy::default()` returns the `Decorrelated` variant. Governed by [ADR 0041](docs/adr/0041-transport-policy-l3-layering.md). | Contract | Yes | `crates/transport-policy/tests/policy_contract.rs::prop_tpp_016_none_jitter_returns_capped_base_delay_unchanged`, `crates/transport-policy/tests/policy_contract.rs::prop_tpp_017_full_jitter_time_seeded_constructor_does_not_panic`, `crates/transport-policy/tests/policy_contract.rs::prop_tpp_018_equal_jitter_returns_at_least_half_capped_base_delay`, `crates/transport-policy/tests/policy_contract.rs::prop_tpp_019_default_jitter_strategy_is_decorrelated_variant`, `crates/transport-policy/tests/policy_contract.rs::prop_tpp_020_zero_base_delay_returns_zero_across_every_strategy` | 2026-05-14 |
| `PROP-TPP-012` | `cow-sdk-transport-policy` | `RequestRateLimiter` honors its scope contract (`PerHost` keys by host, `Global` uses a constant key), the `unlimited()` short-circuit never delays or errors, `acquire_global` shares one bucket across calls, pre-cancelled tokens short-circuit without sleeping the limiter interval, and `RequestRateLimiterBuilder` round-trips every setter. Governed by [ADR 0041](docs/adr/0041-transport-policy-l3-layering.md). | Contract | Yes | `crates/transport-policy/tests/policy_contract.rs::prop_tpp_021_unlimited_rate_limiter_never_delays_or_errors`, `crates/transport-policy/tests/policy_contract.rs::prop_tpp_022_global_scope_uses_constant_key_regardless_of_host`, `crates/transport-policy/tests/policy_contract.rs::prop_tpp_023_acquire_global_shares_one_bucket_across_calls`, `crates/transport-policy/tests/policy_contract.rs::prop_tpp_024_pre_cancelled_token_returns_cancelled_immediately`, `crates/transport-policy/tests/policy_contract.rs::prop_tpp_025_rate_limiter_builder_round_trip_preserves_setters` | 2026-05-14 |
| `PROP-TPP-013` | `cow-sdk-transport-policy` | `RetryPolicy` exposes documented `new`, `with_jitter`, `should_retry_status`, `should_retry_network`, and `delay_for_status` decision points; backoff clamps to `max_delay` once the exponent saturates; the `Retry-After` helper honors the header case-insensitively for retry-after-eligible statuses (`429`, `503`) and ignores it otherwise; `RetryPolicyBuilder::max_attempts(0)` is silently promoted to `1`. Governed by [ADR 0041](docs/adr/0041-transport-policy-l3-layering.md). | Contract | Yes | `crates/transport-policy/tests/policy_contract.rs::prop_tpp_026_retry_policy_new_sets_max_attempts_and_keeps_defaults`, `crates/transport-policy/tests/policy_contract.rs::prop_tpp_027_with_jitter_replaces_only_the_jitter_field`, `crates/transport-policy/tests/policy_contract.rs::prop_tpp_028_should_retry_status_matches_the_public_retryable_list`, `crates/transport-policy/tests/policy_contract.rs::prop_tpp_029_should_retry_network_only_retries_documented_kinds`, `crates/transport-policy/tests/policy_contract.rs::prop_tpp_030_base_backoff_clamps_to_max_delay_across_attempt_range`, `crates/transport-policy/tests/policy_contract.rs::prop_tpp_031_retry_after_helper_is_case_insensitive`, `crates/transport-policy/tests/policy_contract.rs::prop_tpp_032_retry_builder_round_trip_and_zero_attempts_clamps_to_one` | 2026-05-14 |
| `PROP-TPP-014` | `cow-sdk-transport-policy` | `parse_retry_after` accepts delta-seconds and well-formed IMF-fixdate values (every calendar month, leap-year `Feb 29` on years divisible by 4 except non-quadracentury centuries, every consecutive day pair differing by exactly `86_400` seconds for years `1971..=2100`), clamps past or epoch-equal dates to `Duration::ZERO`, and rejects malformed inputs (empty/whitespace, garbage, weekday without comma, non-GMT timezone, trailing tokens, truncated forms, non-numeric components, invalid month names, out-of-range time components, day `0`, day `31` in 30-day months, day `32` universally). Governed by [ADR 0041](docs/adr/0041-transport-policy-l3-layering.md). | Contract | Yes | `crates/transport-policy/tests/retry_after_contract.rs`, `fuzz/fuzz_targets/fuzz_parse_retry_after.rs` | 2026-05-14 |
| `PROP-TPP-015` | `cow-sdk-transport-policy` | `run_with_retry` is the shared retry driver for the orderbook, subgraph, and IPFS clients: it returns on the first `AttemptOutcome::Success`, retries a retryable status or transport signal with the policy backoff schedule until `max_attempts`, returns the attempt closure's terminal error immediately on a non-retryable signal without re-dispatching, and surfaces the last terminal error on exhaustion. HTTP-date `Retry-After` evaluation reads the target-neutral `system_now` wall clock, so the retry path behaves identically on native and `wasm32` and never aborts a browser runtime. Governed by [ADR 0041](docs/adr/0041-transport-policy-l3-layering.md). | Contract | Yes | `crates/transport-policy/src/runner.rs (tests)`, `crates/wasm/tests/wasm_retry_runner_contract.rs::system_now_returns_a_wall_clock_value_without_panicking`, `crates/wasm/tests/wasm_retry_runner_contract.rs::retryable_status_drives_backoff_without_panicking`, `crates/orderbook/tests/request_contract.rs::tracing_contract::execute_with_emits_retry_events_with_status_and_transport_error_fields`, `crates/orderbook/tests/api_contract.rs::service_unavailable_retry_after_header_delays_retry_for_at_least_server_cooldown`, `crates/subgraph/tests/api_contract.rs` | 2026-05-28 |
| `PROP-WS-001` | whole workspace | Native Alloy dependency boundaries are explicit: `alloy-provider` is allowed only in `cow-sdk-alloy-provider` and `cow-sdk-alloy`, while `alloy-signer-local` is allowed only in `cow-sdk-alloy-signer` and `cow-sdk-alloy`; the default facade still excludes these native adapters unless their opt-in features are enabled. | Contract | Yes | `.github/workflows/_quality-gate.yml` (alloy-provider-invariant and alloy-signer-invariant jobs), `tests/alloy_provider_invariant_covers_every_published_crate.rs`, `tests/alloy_signer_invariant_covers_every_published_crate.rs`, `scripts/policy-maintainer/src/check_alloy_provider_invariant.rs`, `scripts/policy-maintainer/src/check_alloy_signer_invariant.rs` | 2026-05-06 |
| `PROP-WS-RX-001` | whole workspace | Native Alloy and browser-wallet receipt adapters expose the same `TransactionReceipt` lifecycle contract: absent optional fields remain empty, present malformed browser-wallet receipt fields fail closed, EIP-658 status maps to success or reverted, and contract creation keeps `to` empty. Governed by [ADR 0038](docs/adr/0038-transaction-lifecycle-types.md). | Contract | Yes | `crates/alloy-provider/tests/provider_contract.rs::get_transaction_receipt_populates_status_block_gas_from_to`, `crates/alloy/tests/provider_contract.rs::get_transaction_receipt_populates_rich_fields_from_alloy_receipt`, `crates/browser-wallet/tests/transaction_receipt_parsing.rs::parse_receipt_populates_every_field_from_post_byzantium_response`, `crates/browser-wallet/tests/transaction_receipt_parsing.rs::parse_receipt_handles_pre_byzantium_absent_status`, `crates/browser-wallet/tests/transaction_receipt_parsing.rs::parse_receipt_handles_contract_creation_no_to`, `crates/browser-wallet/tests/transaction_receipt_parsing.rs::parse_receipt_returns_none_when_chain_has_not_observed_tx`, `crates/browser-wallet/tests/transaction_receipt_parsing.rs::parse_receipt_rejects_response_without_transaction_hash`, `crates/browser-wallet/tests/transaction_receipt_parsing.rs::parse_receipt_returns_reverted_for_status_0x0`, `crates/browser-wallet/tests/transaction_receipt_parsing.rs::parse_receipt_rejects_invalid_status_value`, `crates/browser-wallet/tests/transaction_receipt_parsing.rs::parse_receipt_rejects_malformed_block_number`, `crates/browser-wallet/tests/transaction_receipt_parsing.rs::parse_receipt_rejects_malformed_block_hash`, `crates/browser-wallet/tests/transaction_receipt_parsing.rs::parse_receipt_rejects_malformed_gas_used`, `crates/browser-wallet/tests/transaction_receipt_parsing.rs::parse_receipt_rejects_malformed_from`, `crates/browser-wallet/tests/transaction_receipt_parsing.rs::parse_receipt_rejects_malformed_to`, `tests/transaction_lifecycle_cross_adapter_invariant.rs` | 2026-05-07 |
| `PROP-WS-TX-001` | whole workspace | Signer-backed transaction submission returns broadcast acknowledgement without implicit receipt polling across native Alloy and browser-wallet adapters. Governed by [ADR 0038](docs/adr/0038-transaction-lifecycle-types.md). | Contract | Yes | `crates/alloy/tests/send_transaction_does_not_wait_for_confirmation.rs::send_transaction_does_not_dispatch_get_transaction_receipt`, `tests/transaction_lifecycle_cross_adapter_invariant.rs::alloy_send_transaction_does_not_poll_for_receipt`, `tests/transaction_lifecycle_cross_adapter_invariant.rs::browser_wallet_send_transaction_does_not_poll_for_receipt`, `examples/native/scenarios/transaction_lifecycle.rs` | 2026-05-07 |
| `PROP-WS-002` | whole workspace | Public-field compatibility surfaces in `cow-sdk-app-data` (`AppDataValidated`, `AppDataParams`, `AppDataInfo`, `ValidationResult`, `IpfsFetchPolicy`), `cow-sdk-subgraph` (`SubgraphGraphQlError`, `SubgraphGraphQlErrorLocation`, `SubgraphRequestErrorContext`, `SubgraphConfig`, `SubgraphConfigOverride`), `cow-sdk-signing` (`SigningResult`, `GeneratedOrderId`), and `cow-sdk-trading` (`Eip1271VerificationParameters`) carry `#[non_exhaustive]` at the struct head so additive fields remain compatible for downstream consumers. | Public API | Yes | `crates/app-data/src/info.rs`, `crates/app-data/src/types/params.rs`, `crates/app-data/src/types/validation.rs`, `crates/app-data/src/types/partner_fee.rs`, `crates/app-data/src/fetch.rs`, `crates/subgraph/src/api.rs`, `crates/subgraph/src/error.rs`, `crates/signing/src/order_signing.rs`, `crates/trading/src/types/eip1271.rs`, `.github/config/enum-policy.yaml`, `crates/contracts/tests/non_exhaustive_dto_contract.rs::enum_policy_manifest_entries_match_expected_markers`, `crates/trading/tests/ui/client_rejection_external_match_requires_wildcard.rs` | 2026-06-01 |
| `PROP-WS-003` | whole workspace | Typestate marker structs across `cow-sdk-orderbook`, `cow-sdk-subgraph`, and `cow-sdk-trading` are sealed with private tuple fields, so external callers cannot construct marker values directly and must use the documented builder transition methods. | Public API | Yes | `crates/orderbook/src/builder.rs`, `crates/subgraph/src/builder.rs`, `crates/trading/src/sdk/mod.rs`, `crates/trading/src/sdk/builder.rs`, `crates/contracts/tests/ui/typestate_marker_sealing.rs` | 2026-05-31 |
| `PROP-WS-004` | whole workspace | CI enforces dependency-pin lockstep for the cross-crate protocol stack: Alloy runtime crates remain on the `2.0` release family, Alloy ABI/core crates remain on the `1.5` release family, direct nested Alloy pins stay in the same two-family policy. | Contract | Yes | `.github/workflows/_quality-gate.yml`, `.github/workflows/alloy-release-candidate.yml`, `tests/workspace_alloy_pin_lockstep.rs`, `tests/alloy_two_family_pin_lockstep.rs`, `tests/alloy_two_family_lockfile_invariant.rs`, `Cargo.toml`, `Cargo.lock`, `parity/source-lock.yaml` | 2026-05-06 |
| `PROP-WS-007` | whole workspace | `scripts/parity-maintainer` rejects `source-lock.yaml` files whose `schema_version` is not 3 with the stable diagnostic substring `expected source-lock schema_version 3`; v2, v3, and v4 fixture tests pin the rejection and acceptance contract. | Contract | Yes | `scripts/parity-maintainer/tests/source_lock_schema_version.rs::source_lock_with_schema_v2_is_rejected_with_stable_diagnostic`, `scripts/parity-maintainer/tests/source_lock_schema_version.rs::source_lock_with_schema_v3_is_accepted`, `scripts/parity-maintainer/tests/source_lock_schema_version.rs::source_lock_with_schema_v4_is_rejected_with_stable_diagnostic`, `scripts/parity-maintainer/tests/fixtures/source-lock-v2.yaml`, `scripts/parity-maintainer/tests/fixtures/source-lock-v3.yaml`, `scripts/parity-maintainer/tests/fixtures/source-lock-v4.yaml`, `.github/workflows/_quality-gate.yml` | 2026-05-01 |
| `PROP-WS-008` | whole workspace | Representative atomic-unit `Amount` strings (zero through the full uint256 ceiling) parse through `cow_sdk_core::Amount::new` and render back byte-identically, and the parse is deterministic (the same literal always decodes to the same typed `Amount`). | Contract | Yes | `crates/sdk/tests/amount_roundtrip.rs::amount_strings_round_trip_byte_identically` | 2026-05-01 |
| `PROP-WS-009` | whole workspace | Workspace release-gate policy inputs remain executable: the MSRV declared in Cargo matches CI, dependency default-feature policy is enforced from the root workspace table, enum-policy classifications match source attributes, and every RustSec audit ignore token has documented dependency-gate rationale. | Contract | Yes | `tests/msrv_consistency.rs`, `tests/dependency_default_features_audit.rs`, `crates/contracts/tests/non_exhaustive_dto_contract.rs::enum_policy_manifest_entries_match_expected_markers`, `scripts/check-release-docs-agree.sh`, `.github/config/enum-policy.yaml`, `.github/config/deny.toml`, `docs/audit/dependency-gate-audit.md` | 2026-05-01 |
| `PROP-DOCS-001` | whole workspace | Every published crate with a per-crate `README.md` declares it in package metadata, every per-crate README that carries Rust examples is compile-gated through crate rustdoc via `cargo test --workspace --doc`, and docs-quality smoke coverage confirms the README heading appears in rendered rustdoc HTML. The current README set is `cow-sdk-core`, `cow-sdk-contracts`, `cow-sdk-signing`, `cow-sdk-app-data`, `cow-sdk-orderbook`, `cow-sdk-trading`, `cow-sdk-subgraph`, `cow-sdk-browser-wallet`, `cow-sdk-transport-wasm`, `cow-sdk-wasm`, `cow-sdk-alloy-provider`, `cow-sdk-alloy-signer`, `cow-sdk-alloy`, and `cow-sdk`. | Contract | Yes | `crates/core/README.md`, `crates/contracts/README.md`, `crates/signing/README.md`, `crates/app-data/README.md`, `crates/orderbook/README.md`, `crates/trading/README.md`, `crates/subgraph/README.md`, `crates/browser-wallet/README.md`, `crates/transport-wasm/README.md`, `crates/wasm/README.md`, `crates/alloy-provider/README.md`, `crates/alloy-signer/README.md`, `crates/alloy/README.md`, `crates/sdk/README.md`, `crates/core/src/lib.rs`, `crates/contracts/src/lib.rs`, `crates/signing/src/lib.rs`, `crates/app-data/src/lib.rs`, `crates/orderbook/src/lib.rs`, `crates/trading/src/lib.rs`, `crates/subgraph/src/lib.rs`, `crates/sdk/src/lib.rs`, `crates/browser-wallet/src/lib.rs`, `crates/transport-wasm/src/lib.rs`, `crates/wasm/src/lib.rs`, `crates/alloy-provider/src/lib.rs`, `crates/alloy-signer/src/lib.rs`, `crates/alloy/src/lib.rs`, `.github/workflows/docs-quality.yml` | 2026-05-09 |
| `PROP-WB-001` | `cow-sdk-wasm` | Host-pure helpers stay free of JavaScript ABI types and wrap shared Rust protocol helpers without host API dependencies. Governed by [ADR 0039](docs/adr/0039-typescript-callable-wasm-sdk-surface.md). | Contract | Yes | `crates/wasm/tests/host_pure_helpers.rs::typed_data_payload_matches_signing_module_output`, `crates/wasm/tests/host_pure_helpers.rs::generated_order_uid_uses_canonical_strings`, `crates/wasm/tests/host_pure_helpers.rs::eip1271_payload_matches_signing_module_output_and_vector` | 2026-05-09 |
| `PROP-WB-002` | `cow-sdk-wasm` | The exports module owns every wasm-bindgen and TypeScript declaration boundary, while host builds continue to compile the pure helper tree. Governed by [ADR 0039](docs/adr/0039-typescript-callable-wasm-sdk-surface.md). | Contract | Yes | `crates/wasm/tests/host_pure_helpers.rs::wasm_version_matches_package_version`, `crates/wasm/tests/wasm_surface_contract.rs::wasm_version_matches_crate_version`, `tests/wasm_dependency_invariant.rs::cow_sdk_wasm_wasm32_tree_omits_forbidden_dependencies` | 2026-05-09 |
| `PROP-WB-003` | `cow-sdk-wasm` | EIP-1271 smart-account signing resolves JavaScript callbacks at the facade boundary and stores only a pure resolved provider in Rust. Governed by [ADR 0040](docs/adr/0040-wallet-provider-callback-boundary-for-js-consumers.md). | Contract | Yes | `crates/wasm/src/exports/eip1271.rs` (compile-time `Send + Sync` assertion on `ResolvedEip1271Provider`), `crates/wasm/tests/wasm_eip1271_contract.rs::custom_eip1271_callback_signature_is_used_verbatim`, `crates/wasm/tests/wasm_eip1271_contract.rs::sign_order_with_eip1271_returns_eip1271_scheme` | 2026-06-01 |
| `PROP-WB-004` | `cow-sdk-wasm` | Order UID and digest DTO fields reuse the canonical `as_str()` string surfaces instead of re-encoding byte arrays. Governed by [ADR 0039](docs/adr/0039-typescript-callable-wasm-sdk-surface.md). | Contract | Yes | `crates/wasm/tests/host_pure_helpers.rs::generated_order_uid_uses_canonical_strings`, `crates/wasm/tests/wasm_surface_contract.rs::compute_order_uid_returns_uid_and_digest_strings`, `crates/wasm/tests/wasm_eip1271_contract.rs::sign_order_with_eip1271_uid_equals_generated_order_id_as_str` | 2026-05-09 |
| `PROP-WB-005` | `cow-sdk-wasm` | Callback-transport timeout configuration fails closed before dispatch when the timeout cannot be represented safely at the JavaScript boundary. Governed by [ADR 0040](docs/adr/0040-wallet-provider-callback-boundary-for-js-consumers.md). | Contract | Yes | `crates/wasm/tests/wasm_callback_transport_contract.rs::timeout_overflow_fails_before_dispatch`, `crates/wasm/tests/wasm_fail_closed_contract.rs::abort_controller_is_available_for_callback_transport` | 2026-05-11 |
| `PROP-WB-006` | `cow-sdk-wasm` | Callback HTTP transport maps success, non-2xx responses, callback rejection, and malformed timeout configuration into typed error or response envelopes without exposing raw JavaScript failures. Governed by [ADR 0040](docs/adr/0040-wallet-provider-callback-boundary-for-js-consumers.md). | Contract | Yes | `crates/wasm/tests/wasm_callback_transport_contract.rs::callback_transport_receives_request_dto_with_signal`, `crates/wasm/tests/wasm_callback_transport_contract.rs::callback_transport_maps_non_2xx_to_typed_error`, `crates/wasm/tests/wasm_callback_transport_contract.rs::callback_transport_reject_maps_to_typed_error`, `crates/wasm/tests/wasm_callback_transport_contract.rs::timeout_overflow_fails_before_dispatch` | 2026-05-11 |
| `PROP-WB-007` | `cow-sdk-wasm` | JavaScript callback returns are normalized through `Promise::resolve`, preserving identical handling for plain values, native Promises, and thenables. Governed by [ADR 0040](docs/adr/0040-wallet-provider-callback-boundary-for-js-consumers.md). | Contract | Yes | `crates/wasm/tests/wasm_callback_transport_contract.rs::callback_transport_receives_request_dto_with_signal`, `crates/wasm/tests/wasm_callback_transport_contract.rs::callback_transport_reject_maps_to_typed_error`, `crates/wasm/tests/wasm_callback_contract.rs::typed_data_signer_receives_order_envelope` | 2026-05-11 |
| `PROP-WB-008` | `cow-sdk-wasm` | Callback HTTP request DTOs attach a live `AbortSignal` with `Reflect::set` rather than serializing it through serde. Governed by [ADR 0040](docs/adr/0040-wallet-provider-callback-boundary-for-js-consumers.md). | Contract | Yes | `crates/wasm/tests/wasm_callback_transport_contract.rs::callback_transport_receives_request_dto_with_signal`, `crates/wasm/tests/wasm_fail_closed_contract.rs::abort_controller_is_available_for_callback_transport` | 2026-05-11 |
| `PROP-WB-009` | `cow-sdk-wasm` | JavaScript-visible `WasmError` values preserve the workspace redaction posture for transport, app-data, signing, orderbook, subgraph, trading, and cancellation failures. Governed by [ADR 0039](docs/adr/0039-typescript-callable-wasm-sdk-surface.md). | Contract | Yes | `crates/wasm/tests/wasm_redaction_contract.rs::transport_connect_error_uses_redacted_message`, `crates/wasm/tests/wasm_redaction_contract.rs::http_status_error_redacts_headers_and_body`, `crates/wasm/tests/wasm_redaction_contract.rs::errors_module_does_not_unwrap_redacted_values` | 2026-05-09 |
| `PROP-WB-010` | `cow-sdk-wasm` | Cloudflare Workers consume the web-target output through `./cloudflare` and `./cloudflare/wasm`, without dynamic WebAssembly compilation entry points in worker source. Governed by [ADR 0039](docs/adr/0039-typescript-callable-wasm-sdk-surface.md). | Contract | Yes | `crates/wasm/tests/wasm_fail_closed_contract.rs::flavour_descriptor_exposes_cloudflare_wasm_subpath`, `crates/wasm/tests/wasm_fail_closed_contract.rs::worker_source_avoids_dynamic_wasm_compilation_entrypoints`, `e2e/wasm-typescript-cf/tests/forbidden-instantiation.spec.ts` | 2026-05-11 |
| `PROP-WB-011` | `cow-sdk-wasm` | Browser-bundler consumers stay default-http-supported through package exports and browser e2e coverage. Governed by [ADR 0039](docs/adr/0039-typescript-callable-wasm-sdk-surface.md). | Contract | Yes | `crates/wasm/tests/wasm_surface_contract.rs::client_constructors_accept_supported_runtime_inputs`, `crates/wasm/tests/wasm_callback_transport_contract.rs::callback_transport_receives_request_dto_with_signal`, `e2e/wasm-typescript/tests/browser/browser.spec.ts` | 2026-05-11 |
| `PROP-WB-012` | `cow-sdk-wasm` | Node.js 22 and 24 LTS consumers stay callback-http-tested through the nodejs package subpath and callback transport. Governed by [ADR 0039](docs/adr/0039-typescript-callable-wasm-sdk-surface.md). | Contract | Yes | `crates/wasm/tests/wasm_callback_transport_contract.rs::callback_transport_receives_request_dto_with_signal`, `crates/wasm/tests/wasm_callback_transport_contract.rs::callback_transport_maps_non_2xx_to_typed_error`, `e2e/wasm-typescript/tests/transport.spec.ts` | 2026-05-11 |
| `PROP-WB-013` | `cow-sdk-wasm` | Cloudflare Workers stay callback-http-tested through web-target initialization, callback transport, and source-level dynamic-instantiation rejection. Governed by [ADR 0039](docs/adr/0039-typescript-callable-wasm-sdk-surface.md). | Contract | Yes | `crates/wasm/tests/wasm_fail_closed_contract.rs::flavour_descriptor_exposes_cloudflare_wasm_subpath`, `crates/wasm/tests/wasm_fail_closed_contract.rs::worker_source_avoids_dynamic_wasm_compilation_entrypoints`, `e2e/wasm-typescript-cf/tests/init-once.spec.ts`, `e2e/wasm-typescript-cf/tests/orderbook.spec.ts` | 2026-05-11 |
| `PROP-WB-014` | `cow-sdk-wasm` | The runtime-neutral JavaScript callback transport lets consumers drive the SDK from runtimes outside the CI-tested set, such as a self-built Deno target, on a best-effort basis that is not part of the default release evidence claim. Governed by [ADR 0039](docs/adr/0039-typescript-callable-wasm-sdk-surface.md). | Contract | Yes | `crates/wasm/tests/wasm_callback_transport_contract.rs::callback_transport_receives_request_dto_with_signal`, `crates/wasm/tests/wasm_eip1271_contract.rs::eip1271_payload_matches_recorded_typescript_sdk_vector` | 2026-06-04 |
| `PROP-WB-015` | `cow-sdk-wasm`, `cow-sdk-pure-helpers` | Deterministic WASM helper logic lives in `cow-sdk-pure-helpers` without JavaScript FFI imports, while wasm exports adapt those helpers into JavaScript-visible envelopes. Governed by [ADR 0042](docs/adr/0042-pure-helpers-extraction.md). | Contract | Yes | `crates/pure-helpers/tests/no_ffi_imports.rs::pure_helpers_do_not_import_ffi_bindings`, `crates/wasm/tests/host_pure_helpers.rs::typed_data_payload_matches_signing_module_output`, `crates/wasm/tests/host_pure_helpers.rs::eip1271_payload_matches_signing_module_output_and_vector` | 2026-05-11 |
| `PROP-WB-016` | `cow-sdk-wasm` | Callback registry state stays internal to client construction and never appears as public JavaScript handle classes, registry ids, or manual registration functions. Governed by [ADR 0043](docs/adr/0043-callback-registry-internalization.md). | Contract | Yes | `crates/wasm/tests/wasm_snapshot_surface_contract.rs::generated_type_declarations_hide_callback_registry`, `crates/wasm/tests/wasm_callback_lifetime_contract.rs::client_owned_callback_survives_until_request_resolves`, `crates/wasm/tests/wasm_facade_snapshot_contract.rs::facade_declarations_hide_raw_wasm_bindgen_surface` | 2026-05-11 |
| `PROP-WB-017` | `cow-sdk-wasm` | Abort bridging removes JavaScript abort listeners on success, callback throw, callback rejection, parse failure, and timeout-overflow paths. Governed by [ADR 0040](docs/adr/0040-wallet-provider-callback-boundary-for-js-consumers.md). | Contract | Yes | `crates/wasm/tests/wasm_cancellation_contract.rs::abort_bridge_removes_listener_after_success`, `crates/wasm/tests/wasm_cancellation_contract.rs::abort_bridge_removes_listener_after_callback_throw`, `crates/wasm/tests/wasm_cancellation_contract.rs::abort_bridge_removes_listener_after_callback_reject`, `crates/wasm/tests/wasm_cancellation_contract.rs::abort_bridge_removes_listener_after_parse_error`, `crates/wasm/tests/wasm_cancellation_contract.rs::abort_bridge_removes_listener_after_timeout_overflow` | 2026-05-11 |
| `PROP-WB-018` | `cow-sdk-wasm` | JavaScript-visible envelopes carry `schemaVersion` and preserve an `__unknown` sentinel so newer variants remain parseable without widening known discriminants. Governed by [ADR 0047](docs/adr/0047-typescript-facade-architecture.md). | Contract | Yes | `crates/wasm/tests/wasm_envelope_contract.rs::envelope_serializes_schema_version_and_payload`, `crates/wasm/tests/wasm_envelope_contract.rs::envelope_preserves_unknown_schema_sentinel`, `crates/wasm/tests/wasm_error_abi_contract.rs::unknown_sentinel_round_trips_raw_payload` | 2026-05-11 |
| `PROP-WB-019` | `cow-sdk-wasm` | Per-flavor package builds expose default, orderbook, signing, and Cloudflare public subpaths with measured release-size budgets and without raw generated import paths. The standalone `web` target is built only for the Cloudflare flavor; `default`, `orderbook`, and `signing` ship the `bundler` and `nodejs` targets, whose raw builds back the facade ESM and CommonJS entries. The cloudflare flavor's gzip-compressed artifact is verified against an explicit byte budget that tracks Cloudflare's published Workers Free compressed-size limit on every release build. Governed by [ADR 0044](docs/adr/0044-bundle-size-profile-and-flavor-builds.md) and the [cow-sdk-wasm Comparative Benchmark Validation Note](docs/audit/cow-sdk-wasm-comparative-benchmark-validation-note.md). | Contract | Yes | `crates/wasm/tests/wasm_facade_snapshot_contract.rs::facade_declarations_match_flavour_matrix`, `crates/wasm/tests/wasm_fail_closed_contract.rs::flavour_descriptor_exposes_cloudflare_wasm_subpath`, `crates/wasm/npm/scripts/measure-wasm-size.mjs`, `crates/wasm/npm/flavours.json` | 2026-06-01 |
| `PROP-WB-020` | `cow-sdk-wasm` | JavaScript `TransportPolicyConfig` maps into the shared Rust transport policy for every HTTP-capable WASM client and rejects invalid policy values during construction. Governed by [ADR 0046](docs/adr/0046-transport-policy-js-exposure.md). | Contract | Yes | `crates/wasm/tests/wasm_transport_policy_contract.rs::all_client_constructors_accept_transport_policy`, `crates/wasm/tests/wasm_transport_policy_contract.rs::invalid_transport_policy_user_agent_is_rejected`, `crates/wasm/tests/wasm_snapshot_surface_contract.rs::generated_type_declarations_expose_transport_policy_config_for_http_flavours` | 2026-05-11 |
| `PROP-WB-021` | `cow-sdk-wasm` | The comparative tradeoffs measured between cow-sdk-wasm and the upstream `@cowprotocol/cow-sdk` TypeScript SDK at equivalent feature subsets are documented in the [cow-sdk-wasm Comparative Benchmark Validation Note](docs/audit/cow-sdk-wasm-comparative-benchmark-validation-note.md). The note is `Status: Current` and refreshes on its named refresh triggers (flavor changes, upstream SDK majors, Node LTS rotation, Cloudflare Workers limit changes, JavaScript ecosystem-reference major versions). | Contract | Yes | `docs/audit/cow-sdk-wasm-comparative-benchmark-validation-note.md`, `docs/adr/0039-typescript-callable-wasm-sdk-surface.md`, `docs/adr/0044-bundle-size-profile-and-flavor-builds.md` | 2026-05-12 |
| `PROP-WB-022` | `cow-sdk-wasm`, `cow-sdk-pure-helpers` | At the WASM order-input boundary an omitted `receiver` and an explicit zero-address `receiver` resolve to the same pay-to-owner sentinel, so both construct byte-identical `cow_sdk_core::OrderData` and therefore the same EIP-712 struct hash and order UID; a concrete receiver is never collapsed into the owner. Governed by [ADR 0061](docs/adr/0061-wasm-abi-receiver-pay-to-owner.md). | Contract | Yes | `crates/pure-helpers/src/dto.rs::tests::omitted_receiver_matches_explicit_zero_receiver`, `crates/pure-helpers/src/dto.rs::tests::concrete_receiver_differs_from_pay_to_owner_sentinel`, `crates/wasm/src/exports/dto/core.rs` | 2026-06-01 |
| `PROP-WB-023` | `cow-sdk-wasm` | The annotation-only response DTO mirrors (`OrderDto`, `TradeDto`, `OrderQuoteResponseDto` and its nested `QuoteDataDto`, `NativePriceResponseDto`) exist only to type the JavaScript ABI while the binding serializes the native `cow_sdk_orderbook` value directly, so the compiler never links the runtime serde shape to the declared TypeScript shape and the `.d.ts` snapshot only guards DTO-to-declaration. Each mirror round-trips the same upstream golden fixtures the native types are validated against in `crates/orderbook/tests/wire_contract.rs`, so a field rename, retype, or drop fails closed rather than shipping a TypeScript declaration the runtime JSON does not satisfy. Governed by [ADR 0039](docs/adr/0039-typescript-callable-wasm-sdk-surface.md) and [ADR 0031](docs/adr/0031-wire-dto-openapi-driven-with-order-auction-order-split.md). | Contract | Yes | `crates/wasm/tests/wasm_dto_parity_contract.rs::order_dto_mirrors_native_order_fixture`, `crates/wasm/tests/wasm_dto_parity_contract.rs::order_quote_response_dto_mirrors_native_fixture`, `crates/wasm/tests/wasm_dto_parity_contract.rs::trade_dto_mirrors_native_trade_fixture`, `crates/wasm/tests/wasm_dto_parity_contract.rs::native_price_response_dto_mirrors_native_shape`, `parity/fixtures/orderbook/order_with_full_metadata.json` | 2026-06-07 |
| `PROP-WB-024` | `cow-sdk-wasm` | A wallet timeout raised while signing surfaces as the typed `walletTimeout` error carrying `timeoutMs` on every signing path — typed-data, EIP-1193, and the `eth_sign` digest path for both order and cancellation signing — rather than collapsing into `walletRequest` at the `DigestSigner` adapter boundary, whose unconstrained associated error type would otherwise flatten the typed error to a message string and lose the discriminant. Governed by [ADR 0040](docs/adr/0040-wallet-provider-callback-boundary-for-js-consumers.md) and [ADR 0045](docs/adr/0045-async-signer-trait-narrowing.md). | Contract | Yes | `crates/wasm/tests/wasm_callback_contract.rs::wallet_config_timeout_rejects_pending_signer_callback`, `crates/wasm/tests/wasm_callback_contract.rs::eth_sign_digest_timeout_preserves_wallet_timeout_kind`, `crates/wasm/tests/wasm_callback_contract.rs::eth_sign_cancellation_timeout_preserves_wallet_timeout_kind` | 2026-06-03 |
| `PROP-WB-025` | `cow-sdk-wasm` | The JavaScript-visible `WasmError` `orderbook` variant carries a `retryable` boolean (always serialised, defaulting to `false` through `#[serde(default)]`) and an optional `retryAfterMs`, projecting the native `OrderbookError::is_retryable` / `backoff_hint` verdict to JavaScript so a consumer driving its own retry loop does not re-derive the retryable-status set. Governed by [ADR 0060](docs/adr/0060-uniform-error-classification.md). | Contract | Yes | `crates/wasm/src/exports/errors.rs`, `crates/wasm/tests/wasm_error_abi_contract.rs::orderbook_variant_carries_retry_hints`, `crates/wasm/tests/wasm_error_abi_contract.rs::orderbook_variant_defaults_retryable_and_omits_absent_backoff`, `crates/wasm/snapshots/raw/orderbook-nodejs.d.ts`, `examples/wasm/cow-gateway-cloudflare/tests/worker.spec.ts` | 2026-06-06 |
| `PROP-WB-026` | `cow-sdk-wasm` | When the `tracing` feature is enabled, every instrumented JavaScript export emits exactly one span carrying a stable `endpoint` label of the form `wasm.<area>.<method>` (covering the trading, orderbook, signing, eip1271, subgraph, and ipfs export areas). The spans use `skip_all`, so no JavaScript callback, signer, payload, or wallet input is captured, and `cow-sdk-wasm`'s feature does not enable the underlying Rust crates' spans, so each export call surfaces exactly one `wasm.*` span. Synchronous transaction-building exports carry no spans. | Contract | Yes | `crates/wasm/tests/wasm_telemetry_contract.rs::ipfs_fetch_export_emits_one_endpoint_labelled_span`, `crates/wasm/src/exports/ipfs.rs`, `crates/wasm/src/exports/signing.rs`, `crates/wasm/src/exports/subgraph.rs`, `.github/workflows/wasm.yml`, `docs/observability.md` | 2026-06-08 |
| `PROP-DOCS-002` | whole workspace | Workspace MSRV bump policy is documented at `docs/msrv-policy.md` with explicit cadence, notice window, and trigger criteria; the root workspace MSRV stays aligned with CI. | Contract | Yes | `docs/msrv-policy.md`, `README.md`, `CONTRIBUTING.md`, `tests/msrv_consistency.rs`, `.github/workflows/ci.yml` | 2026-05-01 |
| `PROP-DOCS-003` | `cow-sdk` | The `cow-sdk` facade ships no prelude: the crate root re-exports each leaf crate as a named module plus the cross-cutting aggregate error (`CowError` / `ErrorClass`) and the typed transport, registry, and EIP-1271 cache leaf surfaces, while every workflow and identity type is reached on its module path. The root public surface stays explicit and snapshot-pinned, and the workspace's only prelude is the opt-in `cow_sdk::core::prelude`. Governed by [ADR 0001](docs/adr/0001-multi-crate-sdk-family-with-thin-facade.md). | Public API | Yes | `crates/sdk/src/lib.rs`, `crates/sdk/tests/public_api.rs`, `crates/sdk/tests/public_api_default_features_only.rs`, `crates/sdk/tests/public_api_with_all_features.rs` | 2026-06-08 |
| `PROP-DOCS-004` | whole workspace | `SECURITY.md` documents the base-URL override and browser-wallet trust threat surfaces with operator-side mitigations. | Contract | Yes | `SECURITY.md` | 2026-04-25 |
| `PROP-CORE-008` | `cow-sdk-core` | `SellTokenSource` and `BuyTokenDestination` are distinct typed enums; no silent normalize-for-buy rewrite between sell-side and buy-side balance destinations. Governed by [ADR 0016](docs/adr/0016-split-sell-and-buy-token-balance-enums.md). | Contract | Yes | `crates/core/tests/token_balance_parity.rs`, `crates/core/tests/token_balance_ui.rs` | 2026-04-21 |
| `PROP-CORE-010` | `cow-sdk-core` | `Provider` is the read-only async chain-RPC trait, and `SigningProvider` is the signing-capable extension that owns signer creation. Wallet-capable providers implement both traits; read-only adapters implement only `Provider`, keeping signer dependencies out of read-only provider crates. Governed by [ADR 0024](docs/adr/0024-asyncprovider-asyncsigningprovider-capability-split.md). | Contract | Yes | `crates/core/tests/provider_capability_split_contract.rs`, `crates/core/src/traits/provider.rs` doctests | 2026-05-12 |
| `PROP-CORE-011` | `cow-sdk-core` | Typed-amount arithmetic stays on the typed boundary and is fallible-by-return: `cow_sdk_core::Amount` exposes `checked_add` / `checked_sub` / `checked_mul` (returning `Option`) and the explicit `saturating_*` clamps, and ship no bare `Add` / `Sub` / `Mul` (or `*Assign`) operators, so an overflow or underflow can never silently wrap or panic on a typed amount. A caller that needs raw wrapping reaches through `as_u256` / `into_u256`. A committed compile-fail witness pins that the operators stay removed. Governed by [ADR 0011](docs/adr/0011-typed-amount-boundary-and-typestate-ready-state-construction.md). | Contract | Yes | `crates/core/src/types/amount.rs` (`Amount::checked_add`, `Amount::checked_sub`, `Amount::checked_mul`), `crates/core/tests/types_contract.rs::amount_checked_arithmetic_preserves_option_shape`, `crates/core/tests/amount_arithmetic_ui.rs::typed_amount_arithmetic_surface_rejects_bare_operators_at_compile_time`, `crates/core/tests/ui/amount_arithmetic_operators_removed.rs` | 2026-05-28 |
| `PROP-CORE-012` | `cow-sdk-core`, `cow-sdk-transport-wasm` | Both native and browser HTTP adapters emit one transport-layer span per dispatch with the same method, path-only endpoint, request-byte, and response-byte field shape when the `tracing` feature is enabled; the browser adapter adds only its platform-specific `chain = "wasm32"` field. | Contract | Yes | `crates/core/tests/transport_contract.rs`, `crates/transport-wasm/tests/fetch_contract.rs`, `.github/workflows/wasm.yml`, `docs/observability.md` | 2026-06-02 |
| `PROP-CORE-013` | `cow-sdk-core` | `Cancellable::cancel_with` resolves a fired token through the crate-level `Cancelled` error path, emits a `debug` tracing event with target `cow_sdk::cancel` and `cancelled = true` when the `tracing` feature is enabled, and composes across every long-running public async method on `OrderbookApi`, `SubgraphApi`, and `Trading` through call-site wrapping. | Contract | Yes | `crates/core/tests/cancellation_contract.rs`, `crates/core/tests/cancellation_contract.rs::tracing_contract::cancellation_branch_emits_debug_event_with_target_and_field`, `crates/core/tests/cancellation_coverage_validator.rs::cancellation_tables_cover_every_public_async_method`, `crates/orderbook/tests/api_contract.rs::get_version_returns_cancelled_when_combinator_token_fires_before_send`, `crates/orderbook/tests/api_contract.rs::get_version_combinator_aborts_an_in_flight_request`, `crates/orderbook/tests/cancellation_composition_contract.rs`, `crates/orderbook/tests/request_contract.rs::retry_after_backoff_wait_can_be_cancelled_before_next_attempt`, `crates/subgraph/tests/api_contract.rs::get_totals_returns_cancelled_when_combinator_token_fires_before_send`, `crates/subgraph/tests/api_contract.rs::get_totals_combinator_aborts_an_in_flight_request`, `crates/subgraph/tests/cancellation_composition_contract.rs`, `crates/trading/tests/sdk_contract.rs::get_quote_only_returns_cancelled_when_combinator_token_fires_before_call`, `crates/trading/tests/sdk_contract.rs::get_quote_only_combinator_aborts_an_in_flight_quote`, `crates/trading/tests/cancellation_composition_contract.rs`, `examples/native/scenarios/cancel_in_flight.rs` | 2026-05-26 |
| `PROP-TRD-004` | `cow-sdk-trading` | Client-side submission validator rejects the stable services-authority preconditions it can check without the operator's deployment configuration (expired validTo, zero amount, buy-side same-token, non-EthFlow native sentinel, zero-address from, metadata.signer mismatch, owner mismatch) before wire dispatch, while its public documentation frames the check as defence-in-depth rather than a services-acceptance guarantee and leaves the exact, operator-tunable order-validity window to services. Governed by [ADR 0015](docs/adr/0015-client-side-order-bounds-validator.md). | Contract | Yes | `crates/trading/tests/validation_contract.rs`, `crates/trading/tests/property_contract.rs`, `crates/trading/src/validation.rs`, `crates/trading/tests/post_contract.rs::post_swap_order_appdata_from_mismatch_does_not_upload_or_sign`, `crates/trading/tests/post_contract.rs::post_swap_order_same_buy_sell_token_does_not_upload_or_sign`, `crates/trading/tests/post_contract.rs::post_swap_order_zero_amount_does_not_upload_or_sign`, `docs/audit/trading-order-bounds-validator-audit.md` | 2026-06-07 |
| `PROP-TRD-005` | `cow-sdk-trading` | `PartnerFee` policy round-trips for every services-supported shape (`Volume`, `Surplus`, `PriceImprovement`, plus `Single` and `Multiple` wrappers) with `u16` bps bounds and `#[non_exhaustive]` at both enum heads; quote-to-post app-data merging preserves partner-fee metadata supplied through advanced settings. | Property | Yes | `crates/app-data/tests/partner_fee_contract.rs`, `crates/app-data/src/types/partner_fee.rs (tests)`, `crates/trading/tests/app_data_merge_contract.rs::partner_fee_in_advanced_settings_appdata_merges_through_to_post` | 2026-05-13 |
| `PROP-TRD-006` | `cow-sdk-trading` | `TradingBuilder::build()` on `wasm32` targets fails at the terminal with `TradingError::MissingInjectedOrderbookClient` unless an orderbook client is injected through `TradingOptions::with_orderbook_client`. Governed by [ADR 0011](docs/adr/0011-typed-amount-boundary-and-typestate-ready-state-construction.md). | Contract | Yes | `crates/trading/tests/sdk_contract.rs::build_rejects_missing_injected_orderbook_client_on_wasm32`, `crates/trading/tests/sdk_contract.rs::build_succeeds_on_wasm32_with_injected_orderbook_client`, `crates/trading/tests/sdk_contract.rs::build_succeeds_on_native_without_injected_orderbook_client`, `.github/workflows/wasm.yml` | 2026-06-02 |
| `PROP-TRD-007` | `cow-sdk-trading` | EthFlow and pre-sign transaction gas estimates apply a documented 20% overhead with floor integer rounding (`gas + (gas * 20) / 100`). | Contract | Yes | `crates/trading/src/onchain.rs`, `crates/trading/src/slippage/amounts.rs`, `crates/trading/tests/onchain_contract.rs::eth_flow_gas_estimate_applies_documented_floor_overhead`, `crates/trading/tests/onchain_contract.rs::pre_sign_gas_estimate_applies_documented_floor_overhead`, `docs/audit/trading-order-bounds-validator-audit.md` | 2026-05-26 |
| `PROP-TRD-008` | `cow-sdk-trading` | `fuzz_order_bounds_validator` carries a documented seed-corpus contract anchored to `validation_contract.rs` happy path plus boundary-shaped inputs covering every `ClientRejection` variant and the documented `now`/`valid_to` extremes. | Property | Yes | `fuzz/fuzz_targets/fuzz_order_bounds_validator.rs`, `crates/trading/tests/property_contract.rs`, `docs/audit/trading-order-bounds-validator-audit.md` | 2026-06-07 |
| `PROP-TRD-009` | `cow-sdk-trading` | The order-bounds validator and the public `TradeParameters` and `LimitTradeParameters` validators mirror services' `AllowSell` same-token policy (the reviewed production configuration): sell-side same-token orders are accepted, buy-side same-token orders reject with `ClientRejection::SameBuyAndSellToken`, and the order-level WETH-paired-with-native-sentinel guard applies that same buy-only split. The SDK does not track the operator-configurable `Disallow` and `Allow` policies. | Contract | Yes | `crates/trading/src/validation.rs`, `crates/trading/src/parameters.rs`, `crates/trading/tests/validation_contract.rs::validate_same_token_matches_services_allow_sell_policy`, `crates/trading/tests/parameters_contract.rs::tradeparameters_validate_mirrors_services_allow_sell`, `crates/trading/tests/parameters_contract.rs::limittradeparameters_validate_mirrors_services_allow_sell`, `crates/trading/tests/post_contract.rs::post_swap_order_same_buy_sell_token_does_not_upload_or_sign`, `crates/trading/tests/post_contract.rs::post_swap_order_sell_side_same_buy_sell_token_uploads_signs_and_submits` | 2026-06-07 |
| `PROP-APP-004` | `cow-sdk-app-data` | `AppDataValidation` emits `ApproachingSizeLimit` warning at ≥ 75% of `APP_DATA_MAX_BYTES` and rejects `TooLarge` at 100%. | Contract | Yes | `crates/app-data/tests/validated_shape_contract.rs`, `crates/app-data/tests/property_contract.rs`, `crates/app-data/src/info.rs (tests)`, `fuzz/fuzz_targets/fuzz_app_data_size_limit.rs` | 2026-05-13 |
| `PROP-APP-005` | `cow-sdk-app-data` | The IPFS document-read helpers funnel through one shared leaf, `fetch_doc_from_cid_with_policy`, so each `fetch_doc_*` entry path emits exactly one span when the `tracing` feature is enabled: the non-policy `fetch_doc_from_cid` and the `fetch_doc_from_app_data_hex` variants delegate to the leaf without nesting a second span. The span carries `endpoint = "app_data.fetch_doc_from_cid"` and the requested `cid`, and never records the configured gateway read base URI, matching the `Redacted<String>` posture of `IpfsConfig`. | Contract | Yes | `crates/app-data/tests/fetch_telemetry_contract.rs::fetch_doc_from_cid_emits_one_span_with_cid_and_endpoint`, `crates/app-data/tests/fetch_telemetry_contract.rs::fetch_doc_from_app_data_hex_funnels_through_one_shared_leaf_span`, `crates/app-data/src/fetch.rs`, `docs/observability.md` | 2026-06-08 |
| `PROP-AD-001` | `cow-sdk-app-data` | App-data metadata hooks and signer values have typed slots on `AppDataParams`. Consumers can read the same underlying metadata fields through either typed slots or the open-ended metadata map; both paths expose the same data and are equally supported. | Contract | Yes | `crates/app-data/tests/hooks_contract.rs`, `crates/app-data/tests/metadata_signer_contract.rs::metadata_signer_typed_slot_and_open_map_path_yield_same_value`, `crates/app-data/src/metadata/hooks.rs`, `crates/app-data/src/types/params.rs` | 2026-05-13 |
| `PROP-AD-002` | `cow-sdk-app-data`, `cow-sdk-trading` | `fuzz_app_data_merge` carries a documented seed-corpus contract anchored to typed app-data merge fixtures covering empty bases, nested metadata, signer conflicts, partner-fee shapes, hooks replacement, flash-loan metadata, and key collisions. | Contract | Yes | `fuzz/fuzz_targets/fuzz_app_data_merge.rs`, `fuzz/fuzz_targets/fuzz_app_data_params_from_doc.rs`, `crates/trading/tests/app_data_merge_contract.rs`, `docs/audit/trading-app-data-merge-audit.md` | 2026-05-13 |
| `PROP-ORD-004` | `cow-sdk-orderbook` | `OrderbookRejection` decodes every services `errorType` to a typed variant; unknown codes route to `Unknown { code, message }` with `#[non_exhaustive]` at the enum head. Governed by [ADR 0017](docs/adr/0017-typed-orderbook-rejection-parser.md). | Contract | Yes | `crates/orderbook/tests/rejection_contract.rs::every_known_services_tag_parses_to_its_typed_variant`, `crates/orderbook/tests/rejection_contract.rs::app_data_invalid_tag_preserves_typed_message_and_display`, `crates/orderbook/tests/rejection_contract.rs::app_data_mismatch_tag_preserves_typed_message_and_display`, `crates/orderbook/tests/rejection_contract.rs::not_found_tag_preserves_typed_message_and_display` | 2026-04-23 |
| `PROP-ORD-005` | `cow-sdk-orderbook` | Orderbook quote-side, quote, order, trade, solver-competition, total-surplus, and solver-execution wire DTO amount fields store promoted amounts as `cow_sdk_core::Amount` while preserving the decimal-string JSON wire shape through custom serde and failing malformed amount strings at deserialization. | Contract | Yes | `crates/orderbook/src/types/quote.rs`, `crates/orderbook/src/types/order.rs`, `crates/orderbook/src/types/lists.rs`, `crates/orderbook/src/types/prices.rs`, `crates/orderbook/src/types/auction.rs`, `crates/orderbook/tests/wire_contract.rs::openapi_response_dtos_roundtrip_required_fixture_fields`, `crates/orderbook/tests/wire_contract.rs::order_quote_response_amount_fields_deserialize_through_typed_amount`, `crates/orderbook/tests/transform_contract.rs::total_fee_x_executed_fee_amount_matrix_holds_for_zero_legacy_zero_canonical_legacy_only_canonical_only`, `crates/orderbook/tests/api_contract.rs::solver_competition_response_decodes_typed_with_reference_scores_and_orders`, `crates/orderbook/tests/transform_contract.rs::solver_competition_response_fixture_roundtrips_upstream_producer_vector`, `scripts/parity-maintainer/tests/openapi_coverage.rs::openapi_coverage_validate_reports_structured_field_mismatches`, `fuzz/fuzz_targets/fuzz_calculate_total_fee.rs` | 2026-06-07 |
| `PROP-ORD-006` | `cow-sdk-orderbook` | Retry observability stays aligned with the public field registry: retry events carry `attempt_index`, `backoff_ms`, and either `status` or `transport_error_class`, and quote/order spans populate `quote_id`, `attempts`, and `status` when the `tracing` feature is enabled. | Contract | Yes | `crates/orderbook/tests/request_contract.rs::tracing_contract::execute_with_emits_retry_events_with_status_and_transport_error_fields`, `crates/orderbook/tests/request_contract.rs::tracing_contract::send_order_span_records_quote_id_attempts_and_status`, `docs/observability.md` | 2026-04-24 |
| `PROP-OBK-001` | `cow-sdk-orderbook`, `cow-sdk-trading` | `OrderQuoteRequest` models the orderbook quote `oneOf`s as typed Rust so an invalid request is unrepresentable rather than pre-validated at runtime: `QuoteValidity` carries `validTo` or `validFor` (never both, defaulting to the 30-minute relative window when neither is on the wire), `OrderQuoteSide` carries exactly one side with `SellAmount` distinguishing the before-fee and after-fee sell amount, and `QuoteSigningScheme` keeps `verificationGasLimit` on EIP-1271 only and makes an ECDSA (`Eip712` / `EthSign`) on-chain order unrepresentable while `Eip1271` and `PreSign` retain services-compatible on-chain behavior. Conflicting or malformed wire input is rejected during deserialization — the `SellAmount` deserializer requires exactly one sell-amount key and the `QuoteSigningScheme` `try_from` guard rejects an ECDSA on-chain scheme or a stray `verificationGasLimit` — so the retained `OrderQuoteRequest::validate` hook is infallible. App-data stays modeled as the `appData` / `appDataHash` pair (`QuoteAppData`) routed identically to the signed `OrderCreation` payload, and the trading layer rejects an attempted ECDSA on-chain override before transport with `OrderbookError::IncompatibleSigningScheme`. Governed by [ADR 0058](docs/adr/0058-typed-quote-request-response-surface.md). | Contract | Yes | `crates/orderbook/src/types/quote.rs`, `crates/orderbook/src/types/app_data.rs`, `crates/orderbook/tests/types_contract.rs::quote_signing_scheme_rejects_incompatible_onchain_ecdsa_wire_pairs`, `crates/orderbook/tests/types_contract.rs::quote_signing_scheme_rejects_verification_gas_limit_without_eip1271`, `crates/orderbook/tests/types_contract.rs::quote_request_app_data_routes_to_server_valid_wire_shapes`, `crates/orderbook/tests/types_contract.rs::quote_request_supports_buy_side_and_context_overrides`, `crates/trading/tests/quote_contract.rs::quote_request_validation_runs_before_orderbook_transport`, `docs/audit/quote-response-surface-audit.md`, `docs/adr/0058-typed-quote-request-response-surface.md` | 2026-06-06 |
| `PROP-OBK-002` | `cow-sdk-orderbook`, `cow-sdk-trading` | The `OrderbookClient` trait is owned by `cow-sdk-orderbook` and reachable as `cow_sdk_orderbook::OrderbookClient` without a trading-crate dependency. `cow-sdk-trading` re-exports the same trait as an additive convenience so trading-crate consumers can compose against it without an explicit orderbook-crate import. | Public API | Yes | `crates/orderbook/src/lib.rs`, `crates/trading/src/lib.rs`, `fuzz/fuzz_targets/fuzz_decoded_body_canonical_status_text.rs` | 2026-05-13 |
| `PROP-SEC-001` | whole workspace | Credential-bearing surfaces on `OrderbookApiBuilder`, `EnvBaseUrlOverrides`, and `SubgraphApiBuilder` debug-format as `[redacted]` and do not expose secret bytes through `Debug` or `Display`; the orderbook API-key header path returns a typed validation error rather than panicking on invalid header material. | Contract | Yes | `crates/orderbook/tests/builder_contract.rs::builder_debug_redacts_partner_api_key`, `crates/orderbook/tests/builder_contract.rs::env_base_url_overrides_debug_redacts_embedded_credentials`, `crates/orderbook/tests/builder_contract.rs::builder_debug_redacts_userinfo_in_custom_base_url_overrides`, `crates/orderbook/src/api.rs`, `crates/subgraph/tests/builder_contract.rs::builder_debug_redacts_partner_api_key`, `crates/subgraph/tests/builder_contract.rs::builder_debug_redacts_userinfo_in_custom_endpoint_url` | 2026-06-01 |
| `PROP-CORE-014` | `cow-sdk-core`, `cow-sdk-app-data` | Redacted URL maps, optional URL maps, API-context base URLs, IPFS config displays, and response-body snippets redact credential-bearing values before public formatting or serialization, while raw dispatch access remains explicit. | Contract | Yes | `crates/core/tests/redaction_contract.rs::redacted_url_map_public_representations_redact_values_and_preserve_keys`, `crates/core/tests/redaction_contract.rs::redacted_optional_url_map_public_representations_redact_some_values_and_keep_none`, `crates/core/tests/redaction_contract.rs::api_context_redacts_base_urls_in_debug_and_serialize_but_resolves_raw_url`, `crates/core/tests/redaction_contract.rs::response_body_redaction_strips_credential_shapes_without_regex_dependency`, `crates/app-data/tests/ipfs_config_redaction_contract.rs::ipfs_config_public_debug_and_serialize_redact_configured_uris` | 2026-06-01 |
| `PROP-CORE-015` | `cow-sdk-core` | `fuzz_transport_error_classify` carries a documented seed-corpus contract anchored to core transport-error fixtures covering every `TransportErrorClass` variant, malformed `Retry-After` headers, credential-bearing URL body snippets, and JSON-RPC error bodies. The credential firewall is also exercised through `fuzz_redact_response_body` against the body scanner directly, and the response-body scanner runs a layered detector pipeline (JWT, Bearer, strict URL, bare userinfo, credential-keyed value with recursive key prefix scanning) so a partner response that echoes a mangled scheme, an embedded JWT in a credential key, a bare `Bearer` token outside the `Authorization` header, or a credential-prefix variation cannot leak through. | Contract | Yes | `fuzz/fuzz_targets/fuzz_transport_error_classify.rs`, `fuzz/fuzz_targets/fuzz_redact_response_body.rs`, `crates/core/src/redaction/body.rs`, `crates/core/tests/transport_contract.rs::transport_error_class_labels_cover_every_documented_variant`, `crates/core/tests/redaction_contract.rs::response_body_redaction_strips_credential_shapes_without_regex_dependency` | 2026-05-14 |
| `PROP-CORE-016` | `cow-sdk-core` | Every cow primitive newtype that carries a canonical zero ships `pub const ZERO: Self` as the canonical accessor and does not ship a redundant `pub const fn zero() -> Self` constructor. The constants delegate to the underlying alloy primitive zero (`AlloyAddress::ZERO` for `Address`, `B256::ZERO` for `AppDataHash` and `Hash32`, `FixedBytes::<56>::ZERO` for `OrderUid`, `U256::ZERO` for `Amount`) so callsites can use the associated-constant form uniformly with the alloy precedent. Function-pointer callsites that previously passed `*::zero` as a `FnOnce() -> T` use `unwrap_or(*::ZERO)` because constants satisfy the `T` constraint directly. | Contract | Yes | `crates/core/tests/types_contract.rs::cow_primitive_newtype_zero_constants_equal_alloy_zero`, `crates/core/src/types/amount.rs`, `crates/core/src/types/identity.rs` | 2026-05-21 |
| `PROP-CORE-017` | `cow-sdk-core` | `Amount` exposes a narrow arithmetic surface that ships no bare `Add` / `Sub` / `Mul` (or `*Assign`) operators and no `pow`, and intentionally rejects silent-overflow and bit-population helpers (`wrapping_add` / `wrapping_sub` / `wrapping_mul` / `wrapping_pow`, `overflowing_*`, `count_ones` / `count_zeros` / `leading_zeros` / `trailing_zeros` / `is_power_of_two` / `next_power_of_two`) while shipping the canonical `checked_pow` / `saturating_pow` / `bit_len`/`bits` helpers backed by the inner alloy and ruint primitives. The only arithmetic is fallible-by-return (`checked_*` returning `Option`) or an explicit `saturating_*` clamp, so overflow can never silently wrap or panic; `checked_pow` is the genuine overflow-detecting variant (the inner `ruint` / `alloy` `pow` is wrapping). The narrowed surface is documented in the type-level rustdoc on the newtype so consumers find the rationale when they reach for a missing method. | Contract | Yes | `crates/core/src/types/amount.rs`, `crates/core/tests/types_contract.rs::amount_checked_pow_matches_for_small_inputs`, `crates/core/tests/types_contract.rs::amount_checked_pow_returns_none_on_overflow`, `crates/core/tests/types_contract.rs::amount_saturating_pow_saturates_to_max_on_overflow`, `crates/core/tests/types_contract.rs::amount_bit_len_returns_significant_bit_count_across_boundaries`, `crates/core/tests/types_contract.rs::amount_max_constant_equals_alloy_u256_max`, `crates/core/tests/property_contract.rs::amount_checked_pow_delegates_to_inner_uint_checked_pow`, `crates/core/tests/property_contract.rs::amount_saturating_pow_delegates_to_inner_uint_saturating_pow`, `crates/core/tests/property_contract.rs::amount_bit_len_widens_inner_uint_bit_len_losslessly` | 2026-05-28 |
| `PROP-CORE-018` | `cow-sdk-core` | `Amount::parse_units(value, decimals)` is the exact, integer-only token-amount constructor (the typed analogue of viem/ethers `parseUnits`) and fails closed where the raw `alloy_primitives::utils::parse_units` fails open. It scales `value` by `10^decimals` using integer arithmetic only — never `f64`, so no precision is lost — truncating fractional digits beyond `decimals` to the orderbook atomic-unit contract. It rejects empty or whitespace-only input with [`ValidationError::EmptyField`] (not alloy's fail-open `Ok(0)`), a leading `+`/`-` sign with [`ValidationError::InvalidNumeric`] (a leading `-` must never route to alloy's signed arm and widen into a huge positive through two's-complement), and `decimals` above `77` ([`alloy_primitives::utils::Unit::MAX`]) with [`ValidationError::DecimalsOutOfRange { actual, max }`]. The kept `DecimalsOutOfRange` boundary moved from the removed `DecimalAmount` constructors to this surface; every ERC-20 token across the supported chains ships `decimals <= 18`, so the bound is structurally satisfied in practice. Governed by [ADR 0011](docs/adr/0011-typed-amount-boundary-and-typestate-ready-state-construction.md). | Property | Yes | `crates/core/src/types/amount.rs`, `crates/core/src/errors.rs`, `crates/core/tests/types_contract.rs::amount_parse_units_is_exact_and_rejects_bad_input`, `crates/core/tests/property_contract.rs::amount_parse_units_is_integer_exact`, `crates/core/tests/property_contract.rs::amount_parse_units_never_panics_within_decimals_range`, `fuzz/fuzz_targets/fuzz_amount_parse_units.rs` | 2026-06-01 |
| `PROP-CORE-019` | `cow-sdk-core` | `Amount::format_units(decimals)` preserves trailing zeros in the fractional substring (the typed analogue of viem/ethers `formatUnits`, minus the trim): the fractional length always equals `decimals`, no decimal point appears when `decimals == 0`, exactly one decimal point appears when `decimals > 0`, and the integer part has no leading zero unless it is literally `"0"`. This format intentionally diverges from the JavaScript ecosystem's `formatUnits` helper (ethers, viem, the cow-protocol services utility chain), which trims trailing zeros to `"1.0"`; the cow format preserves the full precision so `format_units(decimals)` composed with `Amount::parse_units(_, decimals)` is the identity on every atomic value (the round-trip is byte-exact). Governed by [ADR 0011](docs/adr/0011-typed-amount-boundary-and-typestate-ready-state-construction.md). | Property | Yes | `crates/core/src/types/amount.rs`, `crates/core/tests/types_contract.rs::amount_format_units_preserves_trailing_zeros`, `crates/core/tests/property_contract.rs::amount_format_units_then_parse_units_round_trips_exactly` | 2026-06-01 |
| `PROP-CORE-020` | `cow-sdk-core`, `cow-sdk-alloy-provider` | `LogProvider` is an opt-in capability supertrait layered on `Provider`: a log-capable adapter implements it in addition to `Provider`, a read-only adapter implements only `Provider`, and adding it leaves `Provider`'s frozen method set unchanged. `get_logs` is the single bounded-call event scan (one backend query over the caller-bounded block range, never a watcher or indexer loop), and the `LogQuery` / `RawLog` / `LogMeta` types carry no provider or network dependency so a decoded `RawLog::data` feeds the fail-closed decoders directly. Governed by [ADR 0057](docs/adr/0057-log-provider-capability-trait.md). | Contract | Yes | `crates/core/tests/trait_evolution_contract.rs::log_provider_trait_shape`, `crates/core/tests/trait_evolution_contract.rs::provider_trait_shape_unchanged`, `crates/alloy-provider/src/conversion.rs` | 2026-05-29 |
| `PROP-CORE-021` | `cow-sdk-core` | `Amount::from_units(whole, decimals)` is the exact, checked numeric (no-string) token-amount constructor — the integer companion to `Amount::parse_units`. It scales a `u128` whole-unit count by `10^decimals` with checked integer arithmetic (never a string round-trip, never `f64`), rejecting `decimals` above `77` ([`alloy_primitives::utils::Unit::MAX`]) with [`ValidationError::DecimalsOutOfRange { actual, max }`] and an over-`uint256` product with [`ValidationError::NumericOverflow`], so it can never panic or silently wrap; it agrees byte-for-byte with `Amount::parse_units` applied to the same whole number. Governed by [ADR 0011](docs/adr/0011-typed-amount-boundary-and-typestate-ready-state-construction.md). | Property | Yes | `crates/core/src/types/amount.rs`, `crates/core/tests/types_contract.rs::amount_from_units_is_exact_and_agrees_with_parse_units`, `crates/core/tests/property_contract.rs::amount_from_units_matches_integer_oracle_and_parse_units` | 2026-06-01 |
| `PROP-ORD-007` | `cow-sdk-orderbook` | `InvalidTradeFilter`, `InvalidLimit`, and the `LIMIT_OUT_OF_BOUNDS` wire tag parse to dedicated `OrderbookRejection` variants; account-order pagination stays within the documented services limit; the enum surface remains 49 variants including the forward-compatible `Unknown` fallback. | Contract | Yes | `crates/orderbook/tests/rejection_contract.rs::every_known_services_tag_parses_to_its_typed_variant`, `crates/orderbook/tests/api_contract.rs::account_orders_pagination_boundary_table`, `crates/orderbook/src/rejection.rs` | 2026-05-01 |
| `PROP-ORD-008` | `cow-sdk-orderbook` | `fuzz_orderbook_rejection_decode` carries a documented seed-corpus contract anchored to services rejection fixtures covering every known `errorType` tag plus malformed-envelope and non-UTF-8 body boundaries. The classifier sanitizer surface is also exercised through `fuzz_orderbook_rejection_code` against the public allowlist gate. | Contract | Yes | `fuzz/fuzz_targets/fuzz_orderbook_rejection_decode.rs`, `fuzz/fuzz_targets/fuzz_orderbook_rejection_code.rs`, `crates/orderbook/tests/rejection_contract.rs::every_known_services_tag_parses_to_its_typed_variant` | 2026-05-13 |
| `PROP-ORD-012` | `cow-sdk-orderbook` | `OrderbookRejection::category()` returns a coarse, action-oriented `OrderbookRejectionCategory` (`Authorization`, `InsufficientFunds`, `InvalidOrder`, `NotFound`, `Conflict`, `Unfulfillable`, `Server`, `Unknown`) as an additive accessor over the typed taxonomy: it keeps every variant, partitions all of them with no wildcard arm so a new wire tag must be assigned a category at the source, and returns a fieldless `Copy` enum carrying no message or code so it cannot re-expose a redacted rejection payload. Governed by [ADR 0017](docs/adr/0017-typed-orderbook-rejection-parser.md). | Contract | Yes | `crates/orderbook/tests/rejection_category_contract.rs::category_maps_representative_and_borderline_variants`, `crates/orderbook/src/rejection.rs` | 2026-06-08 |
| `PROP-ORD-013` | `cow-sdk-orderbook` | `OrderStatus::is_terminal()` returns `true` for `Fulfilled`, `Cancelled`, and `Expired`, and `is_open()` returns `true` for `Open` and `PresignaturePending`; the two `const fn` predicates partition every variant known today. Because `OrderStatus` is `#[non_exhaustive]` and services-owned, the predicates centralize terminal/live classification in the defining crate so a future status variant is reclassified at the source rather than silently misread by a hand-rolled consumer match. Governed by [ADR 0011](docs/adr/0011-typed-amount-boundary-and-typestate-ready-state-construction.md). | Contract | Yes | `crates/orderbook/tests/api_contract.rs::order_status_terminal_and_open_predicates_partition_known_variants`, `crates/orderbook/src/types/enums.rs` | 2026-06-08 |
| `PROP-SIG-006` | `cow-sdk-contracts`, `cow-sdk-signing` | Contracts and signing domain-separator helpers stay byte-identical against the shared EIP-712 parity fixture. | Contract | Yes | `crates/contracts/src/primitives.rs::tests::domain_separator_matches_shared_parity_fixture`, `crates/signing/src/domain.rs::tests::domain_separator_matches_shared_parity_fixture` | 2026-04-27 |
| `PROP-SIG-007` | `cow-sdk-core`, `cow-sdk-signing`, `cow-sdk-browser-wallet`, `cow-sdk-alloy-signer`, `cow-sdk-alloy` | Typed signer errors classify EIP-1193 user rejections through the `cow_sdk_core::SignerError::user_rejection_code` trait, and `cow_sdk_signing::signer_error` routes a `Some(code)` result into `SigningError::SignerRejection { label, code }` carrying only the static operation label and the numeric provider code; every other signer-error class falls back to the redacted `SigningError::Signer` path. Implementers must adopt the trait against their typed variants (no string parsing of upstream `Display`). Governed by [ADR 0053](docs/adr/0053-typed-signer-rejection-classification.md). | Contract | Yes | `crates/core/src/traits/signer.rs`, `crates/signing/src/errors.rs`, `crates/signing/src/order_signing.rs::signer_error_routes_typed_rejection_to_structured_variant`, `crates/signing/src/order_signing.rs::signer_error_propagates_non_4001_rejection_codes_verbatim`, `crates/signing/src/order_signing.rs::signer_error_falls_back_to_redacted_signer_for_unclassified_errors`, `crates/signing/src/order_signing.rs::signer_rejection_display_renders_user_facing_label_and_code`, `crates/signing/src/order_signing.rs::signer_operation_label_maps_known_operations`, `crates/browser-wallet/tests/signer_error_trait_contract.rs::user_rejected_request_exposes_the_carried_provider_error_code`, `crates/browser-wallet/tests/signer_error_trait_contract.rs::user_rejected_request_preserves_any_eip1193_4xxx_code_the_wallet_returned`, `crates/browser-wallet/tests/signer_error_trait_contract.rs::non_rejection_variants_return_none_so_the_signer_helper_keeps_redaction`, `crates/browser-wallet/tests/signer_error_trait_contract.rs::unknown_rpc_payload_codes_do_not_classify_as_user_rejections`, `crates/alloy-signer/tests/signer_error_trait_contract.rs::every_variant_returns_none_so_the_signer_helper_keeps_redaction`, `crates/alloy/tests/signer_error_trait_contract.rs::every_variant_returns_none_so_the_signer_helper_keeps_redaction`, `tests/signer_rejection_propagation_invariant.rs::typed_data_signing_rejection_propagates_as_typed_signer_rejection`, `tests/signer_rejection_propagation_invariant.rs::typed_data_signing_rejection_renders_user_facing_label_and_code`, `tests/signer_rejection_propagation_invariant.rs::non_rejection_signer_errors_keep_the_redacted_display_path`, `crates/sdk/tests/error_redaction_contract.rs`, `docs/audit/signer-error-classification-audit.md` | 2026-05-19 |
| `PROP-CON-008` | `cow-sdk-contracts` | `verify_eip1271_signature_cached` emits the canonical tracing span and cache telemetry events without recording verifier addresses, digests, signatures, or response bytes. | Contract | Yes | `crates/contracts/tests/verify_telemetry_contract.rs::verifier_emits_canonical_span_and_safe_miss_store_events`, `crates/contracts/tests/verify_telemetry_contract.rs::verifier_emits_hit_event_without_reaching_provider`, `crates/contracts/tests/verify_telemetry_contract.rs::verifier_emits_skip_event_for_non_cacheable_errors` | 2026-05-27 |
| `PROP-SEC-002` | `cow-sdk-orderbook`, `cow-sdk-subgraph` | Orderbook and subgraph base-URL builders reject non-canonical external hosts by default and require explicit reviewed host-policy opt-in for custom, private, or loopback routes; partner routing composes with the same host-policy boundary. | Contract | Yes | `crates/orderbook/tests/host_policy_contract.rs::orderbook_builder_blocks_custom_hosts_by_default`, `crates/orderbook/tests/host_policy_contract.rs::orderbook_builder_accepts_explicit_allow_and_loopback_policy`, `crates/orderbook/tests/host_policy_contract.rs::partner_api_routing_x_host_policy_compose_correctly`, `crates/subgraph/tests/host_policy_contract.rs::subgraph_builder_blocks_custom_hosts_by_default`, `crates/subgraph/tests/host_policy_contract.rs::subgraph_builder_accepts_explicit_allow_and_loopback_policy`, `crates/subgraph/tests/host_policy_contract.rs::host_policy_treats_rfc_1918_private_ip_as_custom_host` | 2026-05-01 |
| `PROP-BWL-003` | `cow-sdk-browser-wallet` | Anonymous EIP-1193 provider transports fail closed unless they are discovered with origin metadata or constructed with an explicitly reviewed trusted origin using the documented origin schemes. | State machine | Yes | `crates/browser-wallet/tests/provider_contract.rs::anonymous_provider_builder_requires_trusted_origin`, `crates/browser-wallet/tests/provider_contract.rs::provider_builder_accepts_explicit_trusted_origin`, `crates/browser-wallet/tests/provider_contract.rs::trusted_origin_accepts_documented_schemes_and_rejects_others`, `crates/browser-wallet/tests/provider_contract.rs::wallet_add_chain_payload_urls_are_not_subject_to_external_host_policy`, `crates/browser-wallet/tests/wasm_bridge_contract.rs` | 2026-05-01 |
| `PROP-CON-009` | `cow-sdk-contracts` | `Signature::recover_ecdsa_address` recovers EIP-712 and EthSign ECDSA signers from the supplied digest, rejects non-ECDSA variants, and `Signature::declared_address` reports only declared-owner signature schemes. | Contract | Yes | `crates/contracts/tests/signature_contract.rs::signature_helpers_preserve_public_contract_surface`, `crates/contracts/tests/signature_contract.rs::recover_ecdsa_address_recovers_eip712_prehash_signer`, `crates/contracts/tests/signature_contract.rs::recover_ecdsa_address_eth_sign_recovers_signer_through_canonical_eip191_prehash`, `crates/contracts/tests/signature_contract.rs::recover_ecdsa_address_rejects_non_ecdsa_variants`, `fuzz/fuzz_targets/fuzz_decode_magic_value_response.rs`, `fuzz/fuzz_targets/fuzz_order_signature_classify.rs` | 2026-05-13 |
| `PROP-CON-012` | `cow-sdk-contracts` | `fuzz_recover_ecdsa_address` carries a documented seed-corpus contract anchored to the ECDSA v-normalization parity vectors covering canonical ECDSA recovery bytes plus all-zero, all-`0xff`, and rejected-`v` boundaries. | Contract | Yes | `fuzz/fuzz_targets/fuzz_recover_ecdsa_address.rs`, `parity/fixtures/ecdsa/v_normalization.json`, `crates/contracts/tests/signature_contract.rs` | 2026-05-01 |
| `PROP-CON-016` | `cow-sdk-contracts` | `fuzz_ethflow_create_order_encode` carries a documented seed-corpus contract anchored to contracts parity fixture data covering canonical EthFlow order ABI shapes plus zero-amount, maximum-timestamp, all-zero, and all-`0xff` boundaries. | Contract | Yes | `fuzz/fuzz_targets/fuzz_ethflow_create_order_encode.rs`, `parity/fixtures/contracts.json` | 2026-05-01 |
| `PROP-CON-017` | `cow-sdk-contracts` | `fuzz_erc20_permit_typed_data_hash` carries a documented seed-corpus contract anchored to contracts parity fixture data covering canonical EIP-2612 permit digests plus empty-domain, all-zero, all-`0xff`, and maximum-amount boundaries. | Contract | Yes | `fuzz/fuzz_targets/fuzz_erc20_permit_typed_data_hash.rs`, `parity/fixtures/contracts.json`, `crates/contracts/tests/erc20.rs` | 2026-05-01 |
| `PROP-CON-018` | `cow-sdk-contracts` | `EthFlowOrderData::new` and `EthFlowOrderData::from_unsigned_order` reject `receiver == Address::ZERO` with `ContractsError::ZeroReceiver`, pre-empting the deployed `CoWSwapEthFlow` contract's `ReceiverMustBeSet()` revert (selector `0xefc9ccdf`) raised by `EthFlowOrder.toCoWSwapOrder` on both the `createOrder` and `invalidateOrder` write paths. The rule lives in the private `reject_zero_receiver` helper invoked by the `EthFlowOrderData` construction paths; the general order hash path treats `address(0)` as the pay-to-owner sentinel and hashes it verbatim. Governed by [ADR 0020](docs/adr/0020-ethflow-owner-threading.md). | Contract | Yes | `crates/contracts/src/eth_flow.rs::tests::new_rejects_zero_receiver_with_zero_receiver_error`, `crates/contracts/src/eth_flow.rs::tests::from_unsigned_order_rejects_zero_receiver_with_zero_receiver_error`, `crates/contracts/src/eth_flow.rs::tests::zero_receiver_invariant_matches_ethflow_on_chain_revert_selector`, `crates/contracts/tests/property_contract.rs::ethflow_order_data_new_rejects_zero_receiver_iff_address_is_zero` | 2026-05-30 |
| `PROP-CON-019` | `cow-sdk-contracts` | `decode_order_placement` and `decode_order_invalidation` are fail-closed on untrusted `CoWSwapOnchainOrders` logs: topic count and topic-0, the on-chain signing scheme, the EIP-1271 owner-payload length, the eth-flow trailing-data length, and the 56-byte UID length are each validated; every malformed log returns a typed `ContractsError`; no input panics the decoder; `OrderPlacement` / `OrderInvalidation` topic-0 are byte-locked against an independent keccak of the canonical signatures; and a decoded order's UID reuses `compute_order_uid`. Governed by [ADR 0054](docs/adr/0054-onchain-order-event-decoding-is-fail-closed.md). | Contract | Yes | `crates/contracts/tests/onchain_orders.rs::order_placement_topic0_matches_canonical_hash`, `crates/contracts/tests/onchain_orders.rs::order_hash_matches_canonical_ethflow_foundry_vector`, `crates/contracts/tests/onchain_orders.rs::eip1271_placement_decodes_owner_uid_and_trailer`, `crates/contracts/tests/onchain_orders.rs::eip1271_owner_requires_twenty_byte_signature_payload`, `crates/contracts/tests/onchain_orders.rs::invalid_signing_scheme_is_rejected`, `crates/contracts/tests/onchain_orders.rs::order_invalidation_rejects_wrong_uid_length`, `crates/contracts/src/primitives.rs::tests::order_kind_marker_round_trips_and_rejects_unknown`, `fuzz/fuzz_targets/fuzz_onchain_order_log_decode.rs` | 2026-05-28 |
| `PROP-CON-020` | `cow-sdk-contracts` | The `IWrappedNativeToken` `deposit()` / `withdraw(uint256)` selectors are byte-locked against an independent keccak of the canonical signatures, and `wrap_interaction` / `unwrap_interaction` emit the canonical settlement interaction — `deposit()` with the wrapped amount as native value and `withdraw(amount)` with zero native value. Governed by [ADR 0012](docs/adr/0012-alloy-sol-bindings-and-registry-authority.md). | Contract | Yes | `crates/contracts/tests/weth.rs::deposit_selector_matches_canonical_keccak`, `crates/contracts/tests/weth.rs::withdraw_selector_matches_canonical_keccak`, `crates/contracts/tests/weth.rs::wrap_interaction_calls_deposit_with_amount_as_value`, `crates/contracts/tests/weth.rs::unwrap_interaction_calls_withdraw_with_zero_value` | 2026-05-28 |
| `PROP-CON-021` | `cow-sdk-contracts` | `decode_settlement_log` is fail-closed on untrusted `GPv2Settlement` logs: topic count and topic-0 are validated through the shared `check_topics` guard and the 56-byte order-UID length is checked before mapping; every malformed log returns a typed `ContractsError` and no input panics the decoder; the `Trade` / `Interaction` / `Settlement` / `OrderInvalidated` / `PreSignature` topic-0 are byte-locked against an independent keccak of the canonical signatures; and each event maps into the `#[non_exhaustive]` `SettlementEvent` domain type. Governed by [ADR 0056](docs/adr/0056-settlement-event-decoding-is-fail-closed.md). | Contract | Yes | `crates/contracts/tests/settlement_events_contract.rs::settlement_event_topic0_byte_locks_match_canonical_keccak`, `crates/contracts/tests/settlement_events_contract.rs::trade_round_trips`, `crates/contracts/tests/settlement_events_contract.rs::interaction_round_trips_including_bytes4_selector`, `crates/contracts/tests/settlement_events_contract.rs::settlement_invalidated_and_presignature_round_trip`, `crates/contracts/tests/settlement_events_contract.rs::unknown_topic0_is_rejected`, `crates/contracts/tests/settlement_events_contract.rs::missing_indexed_topic_is_rejected`, `crates/contracts/tests/settlement_events_contract.rs::wrong_order_uid_length_is_rejected`, `fuzz/fuzz_targets/fuzz_settlement_event_log_decode.rs` | 2026-05-29 |
| `PROP-CON-022` | `cow-sdk-contracts` | `decode_order_refund` and the unified `decode_eth_flow_log` dispatcher are fail-closed on untrusted eth-flow lifecycle logs: topic count and topic-0 are validated and the 56-byte order UID is length-checked before mapping; `decode_eth_flow_log` routes the `OrderPlacement` / `OrderInvalidation` / `OrderRefund` topic-0 to the matching decoder and returns the typed `EthFlowEvent`; every malformed log returns a typed `ContractsError` and no input panics the decoder; the `OrderRefund` topic-0 is byte-locked against an independent keccak of the canonical signature. Governed by [ADR 0054](docs/adr/0054-onchain-order-event-decoding-is-fail-closed.md). | Contract | Yes | `crates/contracts/tests/eth_flow_events_contract.rs::order_refund_topic0_matches_canonical_hash`, `crates/contracts/tests/eth_flow_events_contract.rs::order_refund_round_trips`, `crates/contracts/tests/eth_flow_events_contract.rs::decode_eth_flow_log_dispatches_all_three_events`, `crates/contracts/tests/eth_flow_events_contract.rs::order_refund_missing_indexed_topic_is_rejected`, `crates/contracts/tests/eth_flow_events_contract.rs::order_refund_wrong_uid_length_is_rejected`, `fuzz/fuzz_targets/fuzz_eth_flow_event_log_decode.rs` | 2026-05-29 |
| `PROP-CON-023` | `cow-sdk-contracts` | The order EIP-712 signing hash computed through the public `hash_order` entrypoint stays byte-stable against pinned per-chain fixtures (mainnet, Gnosis Chain, Sepolia, Arbitrum One, and Base), exercising the receiver field (including the pay-to-owner zero-address sentinel) and the order-kind and token-balance enum-to-string mapping, and the `keccak256(0x19 \|\| 0x01 \|\| domain_separator \|\| struct_hash)` composition path; the fixture header type hash matches `order_eip712_type_hash()`. | Contract | Yes | `crates/contracts/tests/order_digest_parity_contract.rs::order_digest_fixture_rows_hold`, `parity/fixtures/eip712/order_digests.json` | 2026-05-30 |
| `PROP-SEC-003` | whole workspace | Every reviewed public error family redacts or safely types secret-shaped payloads before those values reach `Display`, `Debug`, or JSON serialization through the SDK facade, and the `redact_response_body` scanner enforces a layered JWT, Bearer, strict-URL, bare-userinfo, and credential-keyed-value detector pipeline with recursive key-prefix scanning so partner response echoes cannot bypass the credential firewall through scheme mangling, mid-word Bearer tokens, JWTs embedded in credential keys, or partial credential-key spellings. App-data document validation surfaces failures as typed `AppDataError` values whose `Display` and the matching `ValidationResult::errors` field name only the offending public field and the canonical `ValidationReason`, never the caller-supplied value. The orderbook `Serialization` decode-failure variant surfaces only the serde failure category and the structural line/column position, the app-data document sub-metadata deserializer maps malformed `metadata.signer`/`metadata.flashloan`/`metadata.hooks` values to fixed field-tagged messages, and `AppDataError::Calculation` surfaces only a stable label, so none of these decode-failure or digest-calculation paths renders the raw serde error or boxed source that could echo decoded or caller-supplied bytes. Closed-set protocol identifiers — the HTTP status code on the unclassified orderbook `Api` fallback and EIP-1193 RPC method names on browser-wallet errors — are surfaced as the safely-typed, non-secret half of this contract while free-form response bodies and wallet messages stay redacted. | Contract | Yes | `crates/sdk/tests/error_redaction_contract.rs`, `crates/app-data/tests/schema_contract.rs::non_semver_version_is_rejected_without_leaking_the_value`, `crates/browser-wallet/tests/wallet_contract.rs`, `crates/contracts/tests/error_contract.rs`, `crates/core/tests/redaction_contract.rs`, `crates/core/src/redaction/body.rs`, `crates/orderbook/tests/error_variant_shape.rs`, `crates/signing/tests/eip1271_contract.rs`, `crates/subgraph/tests/error_redaction_contract.rs`, `crates/trading/tests/error_variant_shape.rs`, `crates/orderbook/src/error.rs`, `crates/app-data/src/types/params.rs`, `docs/audit/credential-surface-audit.md` | 2026-06-08 |
| `PROP-ORD-009` | `cow-sdk-orderbook` | `OrderCreation` accepts omitted or zero `feeAmount`, always serializes order-level `feeAmount` as `"0"`, and rejects inbound non-zero `feeAmount` with the stable diagnostic substring `non-zero feeAmount is not accepted for OrderCreation`; `QuoteData` keeps accepting non-zero network-cost `feeAmount`. | Contract | Yes | `crates/orderbook/tests/order_creation_fee_deserialize.rs`, `crates/orderbook/tests/fee_amount_is_not_a_public_builder_setter.rs`, `crates/orderbook/src/types/order.rs`, `docs/audit/wire-dto-coverage-audit.md`, `docs/parity.md` | 2026-05-12 |
| `PROP-ORD-010` | `cow-sdk-orderbook` | `OrderCreation` `Serialize` routes the `(app_data, app_data_hash)` pair onto the services `OrderCreationAppData` untagged-enum variants (`Both` / `Hash` / `Full`): present-full with present-hash emits both `appData` and `appDataHash` per the services `Both` variant, full-only emits `appData` per `Full`, hash-only keys the hash under the `appData` JSON key per `Hash` (rather than emitting a `appDataHash`-only object that matches no variant), and the all-absent case omits both fields. `OrderCreation::from_quote` keeps producing `(None, Some(quote.app_data))` and is routed through the services `Hash` variant by the custom `Serialize` impl without any `from_quote` body change. | Contract | Yes | `crates/orderbook/tests/types_contract.rs::order_creation_serialize_routes_app_data_combinations_to_services_variants`, `crates/orderbook/tests/types_contract.rs::order_creation_from_quote_serialize_emits_services_hash_variant`, `crates/orderbook/src/types/order.rs`, `parity/fixtures/orderbook-requests/order_creation.json`, `docs/audit/wire-dto-coverage-audit.md`, `docs/audit/contract-bindings-parity-audit.md` | 2026-05-21 |
| `PROP-ORD-011` | `cow-sdk-orderbook`, `cow-sdk-core` | `OrderbookApi::upload_app_data` enforces the content-addressed-write invariant at both boundaries. The SDK re-derives the digest with `AppDataHash::from_full_app_data` before dispatching and rejects with `OrderbookError::AppDataHashMismatch { stage: HashMismatchStage::ClientPrecheck }` when the caller-supplied hash disagrees, without performing a network call. On a successful response it decodes the bare hex-string body as `cow_sdk_core::AppDataHash` (matching the services PUT response schema for both HTTP 200 and HTTP 201) and rejects with `HashMismatchStage::ServerEcho` when the returned hash differs from the caller-supplied value. Governed by [ADR 0017](docs/adr/0017-typed-orderbook-rejection-parser.md) and [ADR 0031](docs/adr/0031-wire-dto-openapi-driven-with-order-auction-order-split.md). | Contract | Yes | `crates/orderbook/tests/api_contract.rs::upload_app_data_rejects_client_precheck_mismatch_without_network`, `crates/orderbook/tests/api_contract.rs::upload_app_data_rejects_server_echo_mismatch`, `crates/orderbook/tests/api_contract.rs::upload_app_data_accepts_status_200_for_already_existing_documents`, `crates/orderbook/tests/api_contract.rs::app_data_transport_helpers_use_get_and_put_hash_routes`, `crates/orderbook/tests/error_variant_shape.rs::app_data_hash_mismatch_carries_typed_hashes_and_stage_discriminator`, `crates/core/tests/types_contract.rs::app_data_hash_from_full_app_data_matches_keccak256_of_bytes`, `parity/fixtures/orderbook/app_data_upload_response.json` | 2026-05-28 |
| `PROP-TRD-010` | `cow-sdk-core` | `AppCode` validates application identifiers at the typed boundary by accepting source-backed examples and rejecting only empty strings, NUL bytes, and ASCII control characters. The type lives in `cow-sdk-core` alongside the other identity primitives (`Address`, `AppDataHash`, `OrderUid`) and is the sole source of truth for application-identifier construction across the workspace; `TradingBuilder` terminals surface validation failures as `TradingError::AppCode`. | Contract | Yes | `crates/trading/tests/app_code_contract.rs`, `crates/core/src/types/app_code.rs`, `crates/trading/src/sdk/builder.rs`, `docs/audit/typestate-builder-contract-audit.md`, `docs/audit/trading-sdk-runtime-prerequisites-audit.md` | 2026-06-07 |
| `PROP-TRD-012` | `cow-sdk-trading` | EthFlow native-sell posting and the EthFlow transaction helper accept only `LimitTradeParametersFromQuote` on their public entries. The newtype guarantees a non-`None` `quote_id` by construction, lifting the prior `MissingQuoteId` runtime check on the EthFlow path to the type system at the public boundary. The `TradingError::MissingQuoteId("EthFlow order posting")` diagnostic is preserved for callers that construct from a `LimitTradeParameters` value missing a quote id, and the `swap_params_to_limit_order_params` bridge produces the newtype from the pre-quote shape plus an orderbook quote response. Governed by [ADR 0011](docs/adr/0011-typed-amount-boundary-and-typestate-ready-state-construction.md). | Contract | Yes | `crates/trading/tests/limit_from_quote_contract.rs`, `crates/trading/tests/post_contract.rs::native_sell_posting_requires_quote_id_before_signing_or_submission`, `crates/trading/src/types/trade.rs`, `crates/trading/src/order.rs`, `crates/trading/src/post/native.rs`, `crates/trading/src/onchain.rs`, `crates/trading/src/sdk/post.rs`, `docs/audit/trade-parameter-lifecycle-audit.md`, `docs/getting-started.md` | 2026-05-27 |
| `PROP-TRD-WAIT-001` | `cow-sdk-trading` | `submit_and_wait_for_receipt` composes signer broadcast with provider receipt polling and returns the rich `TransactionReceipt` once observed, while `poll_for_receipt` supports hash-first workflows. `WaitOptions` controls poll interval, timeout, and revert handling; `WaitError` covers broadcast, lookup, timeout, reverted, and cancelled outcomes, and `WaitError::reverted()` returns the reverted receipt for the on-chain submission verdict (and `None` for the transient and environmental variants). Governed by [ADR 0038](docs/adr/0038-transaction-lifecycle-types.md) and [ADR 0010](docs/adr/0010-runtime-neutral-async-and-transport-posture.md). | Contract | Yes | `crates/trading/tests/wait_helper_contract.rs::submit_and_wait_returns_receipt_when_provider_yields_success`, `crates/trading/tests/wait_helper_contract.rs::submit_and_wait_returns_reverted_when_require_success_and_status_reverted`, `crates/trading/tests/wait_helper_contract.rs::submit_and_wait_returns_receipt_when_require_success_false_and_status_reverted`, `crates/trading/tests/wait_helper_contract.rs::submit_and_wait_returns_timeout_when_provider_never_yields_receipt`, `crates/trading/tests/wait_helper_contract.rs::submit_and_wait_propagates_signer_error_as_broadcast_variant`, `crates/trading/tests/wait_helper_contract.rs::submit_and_wait_propagates_provider_error_as_lookup_variant`, `crates/trading/tests/wait_helper_contract.rs::poll_for_receipt_returns_receipt_when_already_available`, `crates/trading/tests/wait_helper_contract.rs::cancellation_through_cancellable_propagates_through_helper`, `crates/trading/tests/wait_helper_contract.rs::reverted_accessor_reports_the_on_chain_revert_verdict` | 2026-06-08 |
| `PROP-TRD-CANCEL-WAIT-001` | `cow-sdk-trading` | `WaitError` carries `Cancelled(Cancelled)` and implements `From<cow_sdk_core::Cancelled>` so `Cancellable::cancel_with(submit_and_wait_for_receipt(...)).await?` propagates cooperative cancellation through the helper without erasing typed signer or provider failures. Governed by [ADR 0038](docs/adr/0038-transaction-lifecycle-types.md) and [ADR 0010](docs/adr/0010-runtime-neutral-async-and-transport-posture.md). | Contract | Yes | `crates/trading/tests/wait_helper_contract.rs::cancellation_through_cancellable_propagates_through_helper` | 2026-05-07 |
| `PROP-TRD-013` | `cow-sdk-trading` | Every documented constructor, `with_*` builder, accessor, trait impl, and serde round-trip on the trading-crate typed-parameter bundles is pinned at the public boundary: `SlippageToleranceRequest` and `SlippageToleranceResponse` builders preserve every optional field; `TraderParameters`, `PartialTraderParameters`, `QuoterParameters`, and `OrderTraderParameters` `new` and `with_*` setters preserve chain, env, app-code, and contract-override inputs and surface `AppCodeError` on invalid app codes; `PartialTraderParameters` carries no SDK-default owner field and the trading-crate builder does not expose `with_owner`; `TradeParameters` and `LimitTradeParameters` `new` seed documented defaults (`SellTokenSource::Erc20`, `BuyTokenDestination::Erc20`, no partial fills, no slippage, no validity overrides) and the ten `with_*` setters shared between the two structs are factored through one internal definition that emits identical inherent methods on each public type; `LimitTradeParameters::with_quote_id` remains an inherent method on the limit type; `PostTradeAdditionalParams`, `TradeAdvancedSettings`, and `TradingOptions` builders round-trip and their `Debug` impls reflect presence flags without leaking trait-object internals. | Contract | Yes | `crates/trading/tests/types_contract.rs` | 2026-06-07 |
| `PROP-TRD-015` | `cow-sdk-trading` | `LimitTradeParametersFromQuote` is a public newtype around `LimitTradeParameters` that guarantees a non-`None` `quote_id` by construction. `try_from_limit` returns `TradingError::MissingQuoteId("EthFlow order posting")` on `quote_id = None`; the canonical bridge `swap_params_to_limit_order_params` produces the newtype only when an orderbook quote response is supplied. The EthFlow native-currency submission entry `post_sell_native_currency_order` and the EthFlow transaction helper `eth_flow_transaction` accept only `LimitTradeParametersFromQuote` on their public entries. `quote_id()` returns `i64` without an `Option`. `Serialize`/`Deserialize` flatten the inner shape so the wire form is byte-equal to the underlying limit-trade payload while the type-level guarantee is preserved on the Rust API. Governed by [ADR 0011](docs/adr/0011-typed-amount-boundary-and-typestate-ready-state-construction.md) and [ADR 0023](docs/adr/0023-legacy-compatibility-shim-removal.md). | Contract | Yes | `crates/trading/tests/limit_from_quote_contract.rs`, `crates/trading/src/types/trade.rs`, `crates/trading/src/order.rs`, `crates/trading/src/post/native.rs`, `crates/trading/src/onchain.rs`, `docs/audit/trade-parameter-lifecycle-audit.md` | 2026-05-27 |
| `PROP-TRD-016` | `cow-sdk-trading` | `submit_and_wait_for_receipt` and `poll_for_receipt` emit a separated transaction-lifecycle telemetry pair when the `tracing` feature is enabled: the `transaction.submit` span records only the broadcast `tx_hash` and never a mined field, so a submission span never implies inclusion or execution success; the `transaction.receipt` span records `tx_status`, `block_number`, and `gas_used` only after a provider receipt is observed, and records no signature material, calldata, sender, or recipient. `poll_for_receipt`, which never broadcasts, carries only the receipt span. Governed by [ADR 0038](docs/adr/0038-transaction-lifecycle-types.md). | Contract | Yes | `crates/trading/tests/wait_telemetry_contract.rs::submit_and_wait_emits_separated_submit_and_receipt_spans`, `crates/trading/tests/wait_telemetry_contract.rs::poll_for_receipt_emits_only_the_receipt_span`, `crates/trading/src/wait.rs`, `docs/observability.md` | 2026-06-08 |
| `PROP-BWL-004` | `cow-sdk-browser-wallet` | `SigningProvider::create_signer` on `Eip1193Provider` covers every documented branch: empty and whitespace-only hints construct a signer that resolves through the wallet's selected-account fallback; a valid hint that matches the wallet's exposed accounts returns a signer whose `Signer::address` returns the hint; a valid hint that the wallet does not expose returns `BrowserWalletError::MalformedResponse` carrying method `"create_signer"` and the documented "wallet does not expose account" reason; a malformed hint surfaces the underlying `Address::new` validation error without lifting into `MalformedResponse`; when the cached session accounts are empty the implementation falls through to the documented `query_accounts(false)` path before validating. Governed by [ADR 0029](docs/adr/0029-browser-wallet-runtime.md). | Contract | Yes | `crates/browser-wallet/tests/signing_provider_contract.rs`, `crates/browser-wallet/src/provider/signing_provider_impl.rs` | 2026-05-14 |
| `PROP-BWL-005` | `cow-sdk-browser-wallet` | `Origin::new` accepts every documented URL scheme (`http`, `https`, `test`, `transport`) case-insensitively and reverse-DNS labels (EIP-6963 `rdns` form) without a scheme separator; it trims surrounding whitespace before validation; it rejects empty and whitespace-only inputs, inputs containing control characters, and inputs with undocumented schemes with `BrowserWalletError::InvalidProviderOrigin` carrying the documented rejection reason in the redacted message field. `Origin::Debug` and `Origin::Display` emit `"[redacted]"` exactly while `Origin::as_str` keeps the raw value reachable for callers that explicitly opt out of redaction. Governed by [ADR 0029](docs/adr/0029-browser-wallet-runtime.md). | Contract | Yes | `crates/browser-wallet/tests/origin_contract.rs`, `crates/browser-wallet/src/provider/origin.rs` | 2026-05-14 |
| `PROP-BWL-006` | `cow-sdk-browser-wallet` | `Eip1193Signer::with_expected_chain` and `expected_chain_id` round-trip; chain-bound signers reject session-chain mismatch on every signing entry point with `BrowserWalletError::SessionChainMismatch` carrying both the expected and session chain ids before any wallet dispatch; the typed-data domain chain id is validated against the bound chain id with `BrowserWalletError::TypedDataChainMismatch` before signing; the `account()` fallback path returns `BrowserWalletError::MalformedResponse` carrying method `"eth_accounts"` and the documented "wallet does not currently expose any account" reason when the wallet's exposed account list is empty; an explicit account hint at signer construction short-circuits the selected-account fallback. Governed by [ADR 0029](docs/adr/0029-browser-wallet-runtime.md). | Contract | Yes | `crates/browser-wallet/tests/signer_contract.rs`, `crates/browser-wallet/src/signer.rs` | 2026-05-14 |
| `PROP-BWL-007` | `cow-sdk-browser-wallet`, `cow-sdk-core` | The `cow_sdk_core::TypedDataDomain` `Serialize` impl emits the canonical EIP-1193 `eth_signTypedData_v4` second-parameter wire shape — numeric `chainId`, lowercase-hex 42-character `verifyingContract`, no `salt` field, canonical GPv2 `name` and `version` — byte-identically against a committed parity fixture covering six supported chains (mainnet, gnosis, polygon, base, arbitrum-one, sepolia) crossed with the `Order` and `OrderCancellations` primary types. Defense-in-depth shape-pinning assertions (`chainId` numeric, `verifyingContract` 42-char lowercase-hex, absent `salt`, canonical `name` and `version`, `primaryType` carry-through, `types` map declares the primary type, `message` is an object) compose with the per-row byte-equality gate against the fixture; an ignored regen helper prints a refreshed fixture body when the contract test reports drift. | Contract | Yes | `crates/browser-wallet/tests/signer_contract.rs::typed_data_payload_emits_canonical_eip1193_wire_shape_against_fixture`, `crates/browser-wallet/tests/signer_contract.rs::regen_eth_sign_typed_data_request_fixture`, `parity/fixtures/signing/eth_sign_typed_data_request.json`, `crates/core/src/traits/typed_data.rs`, `docs/audit/contract-bindings-parity-audit.md` | 2026-05-21 |
| `PROP-BWL-008` | `cow-sdk-browser-wallet` | `BrowserWallet::connect`, `request_accounts`, and `refresh_session` — the wallet-mediated connection and session operations that drive `eth_requestAccounts`, `eth_accounts`, and `eth_chainId` — each emit one span carrying an explicit `method` label (`browser_wallet.connect`, `browser_wallet.request_accounts`, `browser_wallet.refresh_session`) when the `tracing` feature is enabled, matching the `method` posture of the already-covered chain-management operations. These operations take no chain argument and so carry no `chain` field. | Contract | Yes | `crates/browser-wallet/tests/wallet_telemetry_contract.rs::connect_request_accounts_and_refresh_emit_method_labelled_spans`, `crates/browser-wallet/src/wallet/mod.rs`, `docs/observability.md` | 2026-06-08 |
| `PROP-SHED-002` | `cow-sdk-cow-shed` | `CowShedHooks::sign`, the crate's one signer-mediated async method, emits exactly one `sign` span carrying `chain`, `version`, and `endpoint = "cow_shed.sign"` when the `tracing` feature is enabled. The owner is resolved from the supplied signer and is not surfaced, and the span records no signer, signature, owner, nonce, deadline, or typed-data payload material; the deterministic proxy, EIP-712, and calldata helpers carry no spans. Governed by [ADR 0049](docs/adr/0049-cow-shed-account-abstraction-proxy.md). | Contract | Yes | `crates/cow-shed/tests/sign_telemetry_contract.rs::sign_emits_one_span_with_chain_version_and_endpoint`, `crates/cow-shed/src/hooks.rs`, `docs/observability.md` | 2026-06-08 |
| `PROP-SHED-001` | `cow-sdk-cow-shed` | COW Shed `executeHooks` calldata encoding is owner-agnostic: `encode_execute_hooks_calldata_with_signature` passes the owner signature through as the factory's `bytes` argument, so both a 65-byte EOA ECDSA signature and a variable-length EIP-1271 contract-signature blob encode and round-trip through `COWShedFactory::executeHooks`, while the typed EOA entry point `encode_execute_hooks_calldata_signed` is byte-identical to the general encoder fed the same 65-byte signature. This keeps the proxy's length-based on-chain dispatch — ECDSA recovery for a 65-byte signature, `isValidSignature` otherwise — reachable from the SDK for both externally-owned and smart-contract owners. Governed by [ADR 0049](docs/adr/0049-cow-shed-account-abstraction-proxy.md). | Contract | Yes | `crates/cow-shed/tests/signed_calldata_parity_contract.rs::signed_calldata_matches_reference_and_compact_builder`, `crates/cow-shed/tests/signed_calldata_parity_contract.rs::with_signature_covers_eoa_and_eip1271` | 2026-06-05 |
| `PROP-AUD-001` | whole workspace | Three named audit-lane docs cover the workspace `unsafe_code = deny` lint posture, the panic-free public surface contract, and the workflow security posture. | Contract | Yes | `docs/audit/unsafe-code-policy-audit.md`, `docs/audit/panic-free-public-surface-audit.md`, `docs/audit/workflow-security-audit.md`, `.github/workflows/_quality-gate.yml` | 2026-05-26 |
